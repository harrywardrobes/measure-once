# Test Suites

## Running `npm run test:ci` locally

`npm run test:ci` (via `scripts/run-ci.mjs`) requires `DATABASE_URL` to point
at an admin PostgreSQL connection before it is run.  The temp-database wrapper
(`scripts/with-test-db.js`) uses this connection to create and drop per-suite
`mo_testdb_*` databases.  Example:

```bash
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres npm run test:ci
```

The Replit environment exposes this automatically via the built-in PostgreSQL
integration.  When running locally or in another CI environment, set
`DATABASE_URL` to any Postgres connection whose role has `CREATEDB` privilege
before invoking the suite.

---

All suites accept either `DATABASE_URL_TEST=<disposable>` or, as a fallback,
`PRIVTEST_ALLOW_SHARED_DB=1` (synthetic rows are namespaced behind a
`privtest-` prefix and cleaned up on exit — a mid-run crash can leave stale
fixtures). Each suite writes a markdown report to `test-results/<name>.md`
and exits non-zero on failure. `npm run test:ci` runs all the suites listed
below.

## Isolated temp-database wrapper

`scripts/with-test-db.js` creates a fresh PostgreSQL database (named
`mo_testdb_<timestamp_ms>_<hex>`), sets `DATABASE_URL_TEST` for the child
process, runs the target script, then drops the temp DB on exit (including
SIGINT/SIGTERM). A startup pruning step runs before each new DB is created:
any existing `mo_testdb_*` database older than `TEST_DB_PRUNE_TTL_MS`
(default 2 hours) is dropped automatically, guarding against orphan databases
left by a force-killed CI runner. Databases using the legacy naming format
(`mo_testdb_<hex>`, no timestamp) are treated as infinitely old and always
pruned. All DB-dependent suites in `test:ci` run via a `:<suite>:ci` variant
that invokes this wrapper, so no external `DATABASE_URL_TEST` or
`PRIVTEST_ALLOW_SHARED_DB=1` flag is required when running `npm run test:ci`.

**Standalone orphan pruner:** `scripts/prune-test-dbs.js` (npm script:
`prune-test-dbs`) can be run independently as a CI post-step or on-demand to
drop all stale `mo_testdb_*` databases. Respects the same
`TEST_DB_PRUNE_TTL_MS` env override. Logs each dropped database name and a
final count summary.

## Privileges harness notes

**Harness behaviour:** strips `TURNSTILE_SECRET_KEY`, `HUBSPOT_TOKEN`,
`SMTP_*`, `GOOGLE_*`, `QB_*` so it runs without third-party credentials. Opt
any back in with `PRIVTEST_USE_<NAME>=1 <NAME>=… npm run test:privileges`.
`ADMIN_EMAILS` is an `optionalPassthrough` (`PRIVTEST_USE_ADMIN_EMAILS=1`).

**HubSpot-token-stripping side effect:** when `HUBSPOT_TOKEN` is stripped,
`GET /api/contacts/:id` 503s and customer-detail bootstrap replaces
`#workflow-view` with an error. Tests that need the page-level renderers
wait for that error, then re-inject the relevant mount (e.g.
`#design-visits-section`) and seed `state.selectedContact` directly. Lead statuses/sub-statuses/design visits come from PostgreSQL,
so the renderer paths under test still run faithfully.

**Test-only env overrides** (never set in production): `QB_API_BASE_OVERRIDE`
(`design-visits.js`), `HUBSPOT_API_BASE_OVERRIDE`, `MAIL_TRANSPORT_FILE_OVERRIDE`.

## Adding a new test suite

### Checklist

1. **Add the script** to `package.json` as `test:<name>` (and `test:<name>:ci`
   if it needs the isolated temp-database wrapper).
2. **Enroll in CI** by adding `test:<name>:ci` to both `scripts/run-ci.mjs`
   and `scripts/run-ci-parallel.mjs` — the `test:ci-runner-sync` lint will
   fail if they drift apart.
3. **Document the suite** by adding a row to the "Suite reference" table below.
   The `test:ci-doc-sync` and `test:suite-descriptions` lints enforce this.
4. **Declare `PROBE_LABELS`** — see the convention below.  The
   `test:suite-probe-counts` lint enforces it.

### PROBE_LABELS convention

Every suite that covers distinct named probes (labelled **(A)**, **(B)**,
**(ST-A)**, **(CC-A)**, etc.) must declare a `PROBE_LABELS` constant at the
top of its test file.  The constant is an array of string literals, one entry
per probe, where the first token in each string is the probe ID in parentheses
or square brackets:

```js
const PROBE_LABELS = [
  '(A) happy-path — widget renders with default props',
  '(B) error-state — widget shows "Something went wrong"',
  '(C) empty-state — zero items hides the list',
];
```

**ID format** — IDs must start with an uppercase letter and may contain digits,
hyphens, and further letters: `A`, `F1`, `ST-A`, `CC-A`, `A-open`,
`A-open-blocked`, `SKP-A`.  Lowercase-only tokens (e.g. `[pageerror]`,
`[setup]`) are ignored by the drift checker and should not be used for probe
IDs.

**Docs entry** — the matching row in the "Suite reference" table must list
every probe ID in bold-parentheses callout notation: `**(A)**`, `**(ST-A)**`,
etc.  When a single table cell is long, slash-separate related IDs:
`**(A-open/B-open/B2-open)**`.  The `test:suite-probe-counts` lint compares
the PROBE_LABELS array against the docs row in **both directions**:

- **Forward check** — every ID in the test file must appear in docs.
- **Reverse check** — every ID in docs must appear in the test file.

Both checks fail CI with a clear message naming the mismatched probe IDs.

**What if a suite has no named probes?** Omit `PROBE_LABELS` and leave the
docs row free of `**(X)**` callouts.  However, you must also add the suite to
`NO_PROBE_SUITES_ALLOWLIST` in `scripts/check-suite-probe-counts.mjs` with a
one-line reason — otherwise the lint emits a non-failing advisory on every CI
run until the suite is enrolled or given real probe labels.  Choose one of the
following reason categories:

- `static-lint` — binary pass/fail file scan (no discrete named probes)
- `static-check` — static build/artifact/drift check (binary pass/fail)
- `unit-flat` — flat unit assertions on a pure function (no named probes)
- `meta-test` — exercises the test infrastructure itself (probe labels N/A)
- `capability-matrix` — broad actor×route matrix (not named-probe format)
- `narrative` — integration/e2e with narrative flow (no discrete probe labels)
- `smoke` — binary presence/smoke check (no named probes)

Example entry:

```js
['test:my-static-lint', 'static-lint — binary pass/fail scan of …'],
```

**Rare edge case — `PROBE_LABELS_DOC_EXTRAS`** — when two doc IDs intentionally
map to a single implementation probe label (e.g. the doc lists `**(CC-A)**`
and `**(CC-A2)**` but a single label covers both), list the doc-only alias in a
`PROBE_LABELS_DOC_EXTRAS` constant to suppress the reverse-check failure:

```js
const PROBE_LABELS_DOC_EXTRAS = ['CC-A2'];
```

**Constraint:** every ID listed in `PROBE_LABELS_DOC_EXTRAS` must _not_ already
have a dedicated entry in `PROBE_LABELS`.  If the same ID appears in both
arrays the script fails CI with a **"redundant PROBE_LABELS_DOC_EXTRAS entry"**
error, because the suppression is unnecessary and signals a stale or
copy-paste mistake.

The script emits a non-failing advisory whenever `PROBE_LABELS_DOC_EXTRAS` is
used, as a reminder that the preferred fix is to give each probe a distinct
label.

**Legacy suites** that existed before this convention and have not yet been
migrated can be added to the `NO_PROBE_LABELS_ALLOWLIST` in
`scripts/check-suite-probe-counts.mjs` with a short reason comment.  New
suites should not use the allowlist.

## Suite reference

| Script | Covers |
| --- | --- |
| `test:ls-keys` | Static lint (`scripts/check-localstorage-keys.mjs`) that prevents raw string literals from being passed directly to `localStorage` / `sessionStorage` `.getItem` / `.setItem` / `.removeItem` calls outside the central key registry (`src/react/constants/localStorageKeys.ts`). Scans all `.ts`/`.tsx` files under `src/react/` — excluding the registry file itself and `*.stories.*` fixtures. A line is a violation when the Storage method's first argument begins with a quote character, the line is not a pure comment, and it lacks a `// ls-key-ok: <reason>` suppression. No server, no DB, no Puppeteer. |
| `test:test-only-guards` | Static lint (`scripts/check-test-only-guards.mjs`) that enforces the test-only hook guard convention on all root-level server-side `.js` modules. Any exported function whose name starts with `_` and whose nearest JSDoc block contains "test-only" or "Never call in production" must contain an explicit `if (process.env.NODE_ENV === 'production') { throw … }` guard in its body. A function is in scope only if it is exported via `module.exports`, `module.exports.<name>`, or `exports.<name>`. Suppress genuine exceptions with `// test-only-guard-ok: <reason>` on the function declaration line. Reference pattern: `_setPool` and `_forceStaleForTest` in `lead-status-guard.js`. No server, no DB, no Puppeteer. |
| `test:privilege-reads` | Static lint that blocks direct `.privilege_level` reads outside the canonical helpers. Scans all `.js` files under `public/` (excluding `core.js`, the compiled `react/`, and `storybook/` dirs) and all `.ts`/`.tsx` files under `src/react/` (excluding `usePrivilege.ts`, `usePrivilegeSync.ts`, and `*.stories.*` fixtures). A line is a violation when it contains `privilege_level`, is not a pure comment, lacks a `// privilege-read-ok: <reason>` suppression, and — for TypeScript — is not a bare property-type declaration. |
| `test:privileges` | 5-actor × 123-route capability matrix, the adversarial probe checklist, and a headless `/login → / → /admin` UI smoke. Required Turnstile-tampering probes fail closed unless opted in via `PRIVTEST_USE_TURNSTILE_SECRET_KEY=1`. **In `test:ci` this runs via `test:privileges:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:substatus-hubspot-label-format` | Integration test for the `syncLeadSubstatusesToHubSpot()` label format. Intercepts `PATCH /crm/v3/properties/contacts/hw_lead_substatus` via a local mock HubSpot server and asserts: **(S0)** startup sync PATCH is issued on boot; **(S1)** normal rows carry `"{ls_label} → {sub_label}"` labels; **(S2)** `__NULL__` sentinel rows emit just the sub-status label with no null prefix; **(S3)** the same sub-status name under two different lead statuses produces two distinct labels; **(S4)** value field format (`KEY__SUBKEY` for normal rows, plain `substatus_key` for `__NULL__`); **(S5)** `POST /api/admin/lead-substatuses` triggers a fresh sync whose PATCH carries the correct arrow-prefixed label; **(S6)** `PATCH /api/admin/lead-substatuses/:id` (relabel) triggers a fresh sync whose PATCH reflects the updated label; **(S7)** `DELETE /api/admin/lead-substatuses/:id` triggers a fresh sync whose PATCH no longer contains the deleted option. **In `test:ci` this runs via `test:substatus-hubspot-label-format:ci`**. |
| `test:lead-status-sync` | Customers-page filter dropdown sync after admin renames: **(A)** BroadcastChannel path, **(B)** `visibilitychange` path; **(C)** count-suffix format after re-render; **(D)** lead-status skeleton load/clear cycle; **(E)** sub-status skeleton load/clear cycle; sub-status chip live-rename: **(F)** BC path, **(G)** visibilitychange path; SSE broadcast: **(H)** PATCH path, **(I)** POST (create) path, **(J)** DELETE path; customer-card chip rename: **(K)** BC path, **(L)** visibilitychange path; race-guard probes **(M)** (BC during in-flight lead-status fetch) and **(N)** (visibilitychange during in-flight lead-status fetch), **(O)** (visibilitychange fires while `/api/lead-substatuses` is in-flight, concurrent-fetch race guard for the sub-status path), and **(P)** (BroadcastChannel fires while `/api/lead-substatuses` is in-flight, symmetric BC race guard for the sub-status path); `onContactsRefresh` callback probes: **(Q)** BC path — `src: 'hs_webhook'` triggers a `GET /api/contacts-all` re-fetch via `refreshNonce` bump, while `src: 'admin_mutation'` does not; **(R)** visibilitychange catch-up path — BC fires while tab is hidden, then tab becomes visible, contacts are re-fetched only when `src` is `'hs_webhook'`. **In `test:ci` this runs via `test:lead-status-sync:ci`**. |
| `test:lead-status-sync-customer-detail` | `CustomerDetailHeader` pill sync on customer-detail: **(H)** pill primary text updates in place after a sub-status rename via BroadcastChannel when `hw_lead_substatus` is pre-set; **(I)** same via a hidden→visible `visibilitychange` sequence. All assertions check `window.__renderToken` is preserved (no full-page reload). **In `test:ci` this runs via `test:lead-status-sync-customer-detail:ci`**. |
| `test:lead-status-sync-customer-detail-viewer` | **Isolated** viewer-role pill check: boots a fresh browser, navigates to `/customers/:id` as a viewer, and asserts the `.lead-status-badge` pill lacks `lsb-clickable`, has no onclick, and clicking it never opens `#card-picker-popup`. Regression guard for the `canEditPipeline()` gate. **In `test:ci` this runs via `test:lead-status-sync-customer-detail-viewer:ci`**. |
| `test:lead-status-sync-customer-detail-editable` | **Isolated** manager/admin-role pill check: boots a fresh browser, runs each role in its own incognito context, and asserts the `.lead-status-badge` pill carries `lsb-clickable`, has an onclick handler, and clicking it opens `#card-picker-popup`. Regression guard for the `canEditPrivilege()` gate. **In `test:ci` this runs via `test:lead-status-sync-customer-detail-editable:ci`**. |
| `test:design-visit-list` | `GET /api/design-visits?contactId=…` scoping + customer-detail "Design visits" rail (status pills, totals, admin Request-revision/Delete actions, member gating). **In `test:ci` this runs via `test:design-visit-list:ci`**. |
| `test:design-visit-qb-resubmit` | QuickBooks sparse-update vs create-new fallback branches in `runSubmitSideEffects` against a local mock QB server (uses test-only `QB_API_BASE_OVERRIDE`). **In `test:ci` this runs via `test:design-visit-qb-resubmit:ci`**. |
| `test:design-visit-submitter-name` | Regression guard for "Designer/Submitted by unknown" — captures HubSpot note + team email + customer email + sign-off approve/revision via `HUBSPOT_API_BASE_OVERRIDE` and `MAIL_TRANSPORT_FILE_OVERRIDE`. **In `test:ci` this runs via `test:design-visit-submitter-name:ci`**. |
| `test:design-visit-hubspot-retry` | HubSpot 429 retry and permanent-failure resilience for the design-visit and localdata pipelines. **(DV1)** note-creation retry — 429 on first POST to HubSpot notes, retry succeeds → 200; **(DV2)** lead-status PATCH retry — 429 on first PATCH, retry succeeds → 200; **(DV3)** note permanent failure — notes endpoint always 500; submission still returns 200 and DB row reaches `submitted`; **(DV4)** lead-status permanent failure — contacts PATCH always 500; all 4 attempts exhausted; submission still 200; **(LD)** localdata PATCH retry — 429 on first attempt, retry succeeds; **(LD2)** localdata PATCH permanent failure — all 4 attempts 500; endpoint still returns 200; **(RA1)** fitter PATCH retry — 429 on first attempt, retry succeeds → 200; **(RA2)** fitter PATCH permanent failure — all 4 attempts 500; endpoint returns 200 with `syncFailed: true` (sub-check RA2.4 verifies the body flag); **(RA3)** fitter PATCH happy path — succeeds immediately; response has `success: true` and no `syncFailed` flag. **In `test:ci` this runs via `test:design-visit-hubspot-retry:ci`**. |
| `test:card-action-handlers` | Admin handler create → cross-tab BC refresh, modal dispatch for bound `.eq-card-action` strips, and substatus-binding-wins-over-label precedence. Probe **(D)** covers the conflict-fix flow end-to-end (conflict banner, Fix button, resolver modal). Probe **(E)** POST handler with `action_name="send_quote"` returns 201. Probe **(F)** editor modal opens with `#cah-action-name` input. Probe **(H)** POST handler with `intermediateLeadStatus` returns 201. Probe **(I)** group for no-label/no-handler lead status is hidden. Probe **(J)** `[data-testid="no-label-warning"]` chip visible for bound-but-unlabelled slot. Probe **(M)** blank `action_label` sub-status produces no `.adm-handlers-slot-label` row. **In `test:ci` this runs via `test:card-action-handlers:ci`**. |
| `test:duplicate-phone-warnings` | Admin "Add team member" form and Trades modal duplicate-phone alerts (notice copy, disabled submit, jump-to-record link); Edit-mode company-phone closure check. **In `test:ci` this runs via `test:duplicate-phone-warnings:ci`**. |
| `test:lead-status-counts-rate-limit` | Single-flight + stale-cache + retry behaviour of `/api/contacts-lead-status-counts` against a mock HubSpot HTTP server. **In `test:ci` this runs via `test:lead-status-counts-rate-limit:ci`**. |
| `test:lead-status-guard` | Cache invalidation and DB-failure handling for `lead-status-guard.js`: **(A)** `assertLeadStatusKey` passes when the key exists (warms the cache); **(B)** after `invalidateLeadStatusCache()` the next call re-fetches from the DB and throws `LEAD_STATUS_REMOVED` for a deleted key; **(C)** DB unreachable with a warm stale cache — manual only, not automatable because the module-level `pg.Pool` is not externally stoppable (behavior is documented in the module JSDoc and covered by code review); **(D)** DB unreachable with no cache at all (fresh module instance pointing at an unreachable host) — throws `LEAD_STATUS_DB_UNAVAILABLE` / 503, ensuring the guard fails closed rather than silently passing. No server, no Puppeteer — exercises the module directly via `DATABASE_URL`. **In `test:ci` this runs via `test:lead-status-guard:ci`**. |
| `test:phone-directory` | Auth gating (member 403 / manager+admin 200) and payload coverage for the staff and trades sections of `GET /api/admin/phone-directory`. **In `test:ci` this runs via `test:phone-directory:ci`**. |
| `test:phone-directory-customers` | Mock-HubSpot coverage for the customers section of `GET /api/admin/phone-directory`: phone/mobilephone field mapping, two-entry split for contacts with both fields, name-fallback to email, and zero-entry guarantee for contacts with no phone data. **In `test:ci` this runs via `test:phone-directory-customers:ci`**. |
| `test:stale-bundle` | Static check (`scripts/check-stale-bundle.mjs`). Parses every `React.lazy()` import in `src/react/main.tsx` and verifies that a matching hashed chunk (`<Name>-*.js`) exists under `public/react/chunks/`. Exits non-zero when a chunk is missing so CI catches stale builds early. No server, no DB, no Puppeteer — reads the build output directly. |
| `test:resolve-action-label` | Unit tests for the `resolveActionLabel` pure function in `src/react/utils/resolveActionLabel.mjs`. Covers 10 critical paths: per-LS row with label, per-LS row with null label (must not fall through), no per-LS row (falls back to per-stage default), no lead status on contact, no matching row, substatus action_label priority, substatus with no action_label, case-insensitivity, legacy per-substageId fallback, and missing per-LS key. No server, no DB, no Puppeteer. |
| `test:typo-vars` | Verifies every `--typo-*` CSS custom property derived from `src/react/theme.ts` typography values is present in `public/tokens.css` with the correct value. Guards against typography token drift between theme.ts and the generated CSS file. No server, no DB, no Puppeteer. |
| `test:color-radius-vars` | Verifies every brand-colour, stage-colour, and radius CSS custom property defined in `src/react/theme.ts` (`BRAND_COLORS`, `STAGE_COLORS`, `RADIUS`) appears in `public/tokens.css` with the correct value. No server, no DB, no Puppeteer. |
| `test:css-hex-colors` | Static scan (`scripts/check-css-hex-colors.mjs`) over every `*.css` file directly under `public/` — excluding `tokens.css` and the generated `react/`/`storybook/` dirs — for raw hex colour literals. Suppress genuine exceptions with `/* hex-color-ok: <reason> */`. No server, no DB, no Puppeteer. |
| `test:var-hex-fallbacks` | Static scan (`scripts/check-var-hex-fallbacks.mjs`) over `public/*.js`, `public/*.html`, and `src/react/**/*.ts`/`.tsx` for CSS `var()` usages that still carry a raw hex fallback value (e.g. `var(--orchid, #8B2BFF)`). Since `tokens.css` is always loaded, hex fallbacks are redundant and create a second source of truth. Suppress genuine exceptions with `// var-hex-ok: <reason>`. No server, no DB, no Puppeteer. |
| `test:mui-select-click` | Static lint (`scripts/check-mui-select-click.mjs`). Scans every `test/**/run.js` file for the anti-pattern of opening a MUI Select by calling `.click()` directly on an `ElementHandle` rather than delegating to the shared `clickMuiSelect` helper. Detects windows of 15 lines containing a `page.$(` call, a variable `.click()` call, and a `MuiSelect` reference. No server, no DB, no Puppeteer. |
| `test:ci-runner-sync` | Static lint (`scripts/check-ci-runner-sync.mjs`). Asserts every `test:*:ci` entry present in `scripts/run-ci.mjs` also appears in `scripts/run-ci-parallel.mjs`. Fails with a clear error message when any entry is missing so drift between the two runners is caught automatically on every CI pass. No server, no DB, no Puppeteer. |
| `test:ci-doc-sync` | Static lint (`scripts/check-ci-doc-sync.mjs`). Asserts every `test:*:ci` entry enrolled in `scripts/run-ci.mjs` has a matching row in the "Suite reference" table in `docs/TEST_SUITES.md`. Strips the `:ci` suffix before comparing so `test:foo:ci` maps to the `test:foo` row. Prevents undocumented suites from being added to CI without a description. No server, no DB, no Puppeteer. |
| `test:suite-descriptions` | Static lint (`scripts/check-suite-descriptions.mjs`). Asserts every `test:*` entry present in `scripts/run-ci.mjs` has a matching row in the "Suite reference" table in `docs/TEST_SUITES.md`. Strips the `:ci` suffix before comparing so both `test:foo` and `test:foo:ci` map to the same base name. Fails with a clear message naming every undocumented suite. No server, no DB, no Puppeteer. |
| `test:suite-probe-counts` | Static lint (`scripts/check-suite-probe-counts.mjs`). For each suite row in `docs/TEST_SUITES.md` that documents probe labels (bold `**(X)**` callouts), locates the suite's test file via `package.json` scripts, finds its `PROBE_LABELS` array, and asserts probe IDs are consistent in **both directions**: every probe in the implementation is mentioned in docs (forward check), and every probe documented in docs exists in the implementation (reverse check). Fails with a clear message naming each mismatched probe. A test file may declare `PROBE_LABELS_DOC_EXTRAS = ['<id>', …]` to suppress known doc-only aliases from the reverse check (for cases where two distinct doc IDs intentionally map to a single implementation probe label); the preferred fix is to give each probe a distinct label so no suppression is needed. When `PROBE_LABELS_DOC_EXTRAS` is present in any scanned test file, the script emits a non-failing advisory message naming the suite and the suppressed IDs as a reminder that distinct labels are preferred. No server, no DB, no Puppeteer. |
| `test:suite-probe-counts-advisory` | Meta-test (`test/suite-probe-counts/run.js`). Verifies that `check-suite-probe-counts.mjs` correctly emits the `PROBE_LABELS_DOC_EXTRAS` advisory when a test file declares that constant, stays silent when absent, emits the missing-PROBE_LABELS warning when a test file has doc probe callouts but omits `PROBE_LABELS`, fails when `PROBE_LABELS_DOC_EXTRAS` contains a stale or redundant ID, suppresses the missing-array warning when the suite is in `NO_PROBE_LABELS_ALLOWLIST`, emits the no-probe-suite advisory when a suite has no probe callouts in docs and is NOT in `NO_PROBE_SUITES_ALLOWLIST`, and stays silent when the suite IS in `NO_PROBE_SUITES_ALLOWLIST`. Builds minimal synthetic fixtures in a temp directory, copies the script there so `ROOT` resolves to the fixture root, runs it as a subprocess, and asserts expected output. Eight scenarios total. No server, no DB, no Puppeteer. |
| `test:browser-launch-pattern` | Static lint (`scripts/check-browser-launch-pattern.mjs`). Scans every `.js` file under `test/` that calls `puppeteer.launch()` and verifies the browser-launch PROBE_LABELS pattern: (1) any `puppeteer.launch()` call that is preceded by a PROBE_LABELS variable declaration must be followed by a `for (const l of <PROBE_LABELS_VAR>)` loop so a launch failure is reported against every named probe; (2) no `puppeteer.launch()` call may appear as a bare `const <x> = await puppeteer.launch(` (i.e. outside a try/catch). Existing suites that predate the convention are listed in an `ALLOWLIST` in the script with a reason comment and produce a non-failing informational message. Any newly added file that violates either rule and is not in the allowlist fails CI immediately. No server, no DB, no Puppeteer. |
| `test:story-count-sync` | Static lint (`scripts/check-story-count-sync.mjs`). For each suite row in `docs/TEST_SUITES.md` that carries a `<!-- story-count: N -->` annotation, locates the test runner via `package.json` scripts, reads its `EXPECTED_COUNT` and `STORY_TITLE` constants, finds the matching `.stories.tsx` file by its `title:` field, and counts the exported `Story`-typed constants. Fails when any of the three values (doc claim, `EXPECTED_COUNT`, actual export count) disagree so story-count drift between the docs, the test runner, and the stories file is caught automatically. No server, no DB, no Puppeteer. |
| `test:offline-capability-sync` | Static lint (`scripts/check-offline-capability-sync.mjs`). Stops the admin "Offline support" capability matrix from drifting from real offline behaviour. **Write surfaces:** asserts a three-way equality between the `area:` codes used by real `sendOrQueue()` callers under `src/react/`, the union of `backedBy` codes on the `full` rows of the single-source-of-truth matrix in `src/react/lib/offlineCapabilities.ts`, and the `<!-- offline-areas: … -->` annotations on the "Covered write surfaces" rows in `docs/OFFLINE.md`. **Read caches (view-only):** the cached read routes are defined once in `scripts/offline-read-caches.mjs` (`OFFLINE_READ_CACHES`), which `scripts/build-sw.mjs` consumes to build its Workbox runtime caches — so the manifest *is* the real SW behaviour. The lint asserts a second three-way equality between that manifest, the union of `cachedBy` names on the `full`/`view` matrix rows, and the `<!-- offline-view-cache: <cache> routes: … -->` annotations in `docs/OFFLINE.md`, at **both** cache-name and route granularity (so adding/removing an individual cached GET route fails CI until the docs are updated). It also guards that `build-sw.mjs` builds its read caches from the manifest and hand-defines no same-origin read-route caches outside it. Also enforces that every `full` row declares `backedBy`, that `view`/`online` rows do not; that every `view` row declares `cachedBy`, that `online` rows do not; and that `OfflineSupportPage.tsx` imports (rather than redefines) `FEATURE_AREAS`. No server, no DB, no Puppeteer. |
| `test:bundle-sizes` | Post-build gzip-size check for every `.js` file under `public/react/`. Exits non-zero if any always-loaded chunk exceeds its threshold. Also appends a size snapshot to `test-results/bundle-sizes-history.jsonl` for trend tracking and emits a non-fatal spike warning when the total gzip size grows by more than `SPIKE_PCT` (5 %) — **this file must be cached/uploaded as a CI artefact** so trend data and spike detection are preserved across runs. Standalone only (not in the `test:ci` pipeline; use `test:bundle-size-trend` and `test:bundle-spike-warning` for CI). |
| `test:bundle-size-trend` | Unit tests for the bundle-size trend-regression warning logic in `scripts/bundle-size-trend.mjs`. No build step required. |
| `test:bundle-spike-warning` | Unit tests for the per-build spike-detection warning logic (`detectSpikeWarning` in `scripts/bundle-size-trend.mjs`). No build step required. |
| `test:chunk-cache-headers` | HTTP HEAD probes against a running Express server: asserts that `/react/chunks/<hashed>.js` and `/react/assets/<hashed>.<ext>` are served with `Cache-Control: … immutable`, and that `/react/main.js` is NOT. **In `test:ci` this runs via `test:chunk-cache-headers:ci`**. |
| `test:admin-tab-skeletons-new` | Puppeteer request-interception suite for the Settings, Card Actions, and Action Handlers admin tabs. Holds each tab's lazy JS chunk while React is mid-load, confirms the Suspense-fallback skeleton appears, then releases the chunk and asserts the skeleton is gone and the real component's static DOM is present. **In `test:ci` this runs via `test:admin-tab-skeletons-new:ci`**. |
| `test:admin-tab-skeletons-suspense` | Puppeteer request-interception suite for the Permissions, Requests, and Audit Log admin tabs — Suspense fallback layer. Complements `test:admin-tab-skeletons` which tests the in-component data skeletons (layer 2) for the same tabs. **In `test:ci` this runs via `test:admin-tab-skeletons-suspense:ci`**. |
| `test:customer-card-action-strip` | Puppeteer e2e for the action-label strip and QB invoice badge on customer cards in `CustomersPage.tsx`. All customers-page API calls stubbed via `evaluateOnNewDocument` fetch interception. Probes: **(A)** bound handler shows a coloured action strip; **(B)** no handler shows no strip; **(C)** `start_design_visit` handler + in-progress draft shows "Continue designing"; **(D)** clicking the strip does not navigate away; **(E)** QB invoice badge click opens `InvoiceDetailDrawer`; **(F)** `upload_photos_and_info` strip click opens `UploadPhotosModal` with link-status stubbed to 500 — asserts `[data-testid="cah-proceed-anyway"]` reads "Generate anyway" not "Send anyway"; **(G)** link-status returns `{ hasActiveLink: true }` — confirming phase reached, body contains "already has an active link", confirm button reads "Generate new link"; **(H1)** confirming phase with future `expiresAt` — body contains "expires"; **(H2)** confirming phase with past `expiresAt` — body contains "expires" (formatExpiry diffDays ≤ 0 branch); **(H3)** confirming phase with invalid `expiresAt` string — body contains "expiry date unavailable" (formatExpiry isNaN branch). **In `test:ci` this runs via `test:customer-card-action-strip:ci`**. |
| `test:icon-lint` | Static two-pass lint over every `.tsx`/`.ts` file under `src/react/` (excluding `.d.ts` and Storybook files). Pass 1: every `*Icon` identifier used as a JSX element or bare value must be imported from `@mui/icons-material`. Pass 2: every `@mui/icons-material` import must be used at least once. No server, no DB, no Puppeteer. |
| `test:mount-ids` | Four-pass static check (`scripts/check-mount-id-conflicts.mjs`). Pass 1: each React mount id appears in at most one `public/*.html` file. Pass 2: every page that loads the React bundle declares at least one mount element (suppress with `<!-- main-js-no-mount-ok: reason -->`). Pass 3: every `BOOTSTRAP_ONLY_IDS` entry appears in at least one HTML file. Pass 4: every `BOOTSTRAP_ONLY_IDS` entry carries a `// public/<file>.html — <description>` annotation and that file exists on disk. **Convention:** new error/restricted pages must add their id to `BOOTSTRAP_ONLY_IDS` with the annotation and a matching MOUNTS entry. |
| `test:public-island-bootstrap` | Five-invariant static check (`scripts/check-public-island-bootstrap.mjs`). Both `PUBLIC_ISLAND_IDS` and `BOOTSTRAP_ONLY_IDS` are canonical in `src/react/lib/publicIslands.ts`. Checks: **(A)** every `// public-island`-annotated MOUNTS id ∈ `PUBLIC_ISLAND_IDS`; **(B)** `PUBLIC_ISLAND_IDS` ⊆ annotated MOUNTS ids; **(C)** `BOOTSTRAP_ONLY_IDS` ∩ `PUBLIC_ISLAND_IDS` = ∅; **(D)** every `BOOTSTRAP_ONLY_IDS` id ∈ all MOUNTS ids; **(E)** every `BOOTSTRAP_ONLY_IDS` entry carries a file annotation and that file exists. |
| `test:inline-styles` | Static scan (`scripts/check-inline-styles.mjs`) over every `public/*.html` file for `style="…"` attributes. Fails without an explicit `<!-- inline-style-ok: reason -->` suppression comment on the same line. |
| `test:story-hex-colors` | Static scan (`scripts/check-story-hex-colors.mjs`) over every `*.stories.tsx` file for raw hex colour values in style-related properties: `bgcolor`, `background`, `backgroundColor`, `color`, `borderColor`, `fill`, `stroke`, `outlineColor`, `textDecorationColor`, `caretColor`, and `boxShadow`. Lines carrying `// story-hex-ok: <reason>` are exempt. |
| `test:component-hex-colors` | Static scan (`scripts/check-component-hex-colors.mjs`) over every `*.ts`/`*.tsx` file under `src/react/` — excluding `*.stories.*`, `*.d.ts`, and `theme.ts` — for raw hex colour values in style-related properties: `bgcolor`, `background`, `backgroundColor`, `color`, `borderColor`, `fill`, `stroke`, `outlineColor`, `textDecorationColor`, `caretColor`, and `boxShadow`. New code must use semantic theme tokens; suppress genuine literals with `// hex-color-ok: <reason>`. |
| `test:workflow-js-no-dups` | Static guard (`scripts/check-workflow-js-duplicates.mjs`) that none of the 11 picker-cluster functions moved to `workflow-core.js` appear as top-level function declarations in `public/workflow.js`. |
| `test:nav-key-sync` | Static lint asserting the two server-side nav-key allow-lists and the `BottomNav.tsx` NAV array are not drifting apart. Three invariants: `VALID_NAV_KEYS_SERVER` (auth.js) equals `VALID_NAV_KEYS` (server.js); NAV ⊆ both server sets. No server, no DB, no Puppeteer. |
| `test:no-config-handler-types` | Static lint (`scripts/check-no-config-handler-types.mjs`). Parses `NO_CONFIG_HANDLER_TYPES` from `ActionHandlersPage.tsx` and asserts none of those types appear as explicit config-block cases or handler-type comparisons. Catches contradictions where a type is simultaneously declared to have no config and given a dedicated config block. |
| `test:status-key-fields` | Static lint (`scripts/check-status-key-fields.mjs`). Cross-checks `KNOWN_STATUS_KEY_FIELDS` in `HandlerConfigBlocks.tsx` against every exported `*ConfigProps` interface. Fails when a new handler config block adds an `*Invalid` companion prop but forgets to add the corresponding field to the shared list. |
| `test:info-card-review-resend` | Regression guard for the Review vs Resend-link button branching logic in `SubmissionCard`. Probes: **(RR-A)** submitted submission → "Review" button visible, resend absent; **(RR-B)** pending submission → resend button visible, "Review" absent; **(RR-C)** clicking "Review" expands the Collapse detail panel — anchored on `[data-testid="submission-card-body"]` becoming visible (`getBoundingClientRect().height > 0`). **In `test:ci` this runs via `test:info-card-review-resend:ci`**. |
| `test:masked-email-backfill` | Regression guard for `backfillMaskedEmails()` in `customer-info.js`. Seeds four rows and calls the function directly: **(A)** `masked_email IS NULL` → filled; **(B)** old-format masked email → rewritten; **(C)** already-correct → unchanged; **(D)** `submitted_at IS NOT NULL` → not touched. **In `test:ci` this runs via `test:masked-email-backfill:ci`**. |
| `test:contact-attempt-history` | Regression guard for `contact_attempt_history_log` write-ordering: a failed `advance-status` call must NOT insert a phantom history row. **(A)** schema probe — `contact_attempt_history_log` table exists with all expected columns; **(B)** advance-status returning 503 (no HubSpot token gate) leaves the history log empty; **(C)** advance-status returning 502 (fake HubSpot token → auth rejection by HubSpot, `patchContactProperties` fails) still leaves the history log empty, confirming the INSERT runs only after a successful status patch. **In `test:ci` this runs via `test:contact-attempt-history:ci`**. |
| `test:customer-info-generate-link-reuse` | Integration test verifying `POST /api/customer-info/by-contact/:contactId/generate-link` reuses the existing active row. Probes: **(A)** first call inserts exactly one active row; **(B)** second call updates the row in place; **(C)** token rotation; **(D)** duplicate cleanup — when three active rows exist, a single generate-link call expires two. **In `test:ci` this runs via `test:customer-info-generate-link-reuse:ci`**. |
| `test:active-link-expires` | Regression guard for the "expire stale links on send" logic. Calls `POST /api/card-actions/upload-photos-and-info` twice for the same contact and verifies: **(A)** first send — 1 active row; **(B)** second send — still 1 active row; **(C)** stale row's `expires_at` is in the past; **(D)** exactly 2 total rows. **In `test:ci` this runs via `test:active-link-expires:ci`**. |
| `test:active-link-warning-ordering` | Regression guard for the "confirm before expiring an active link" ordering in `UploadPhotosModal`. Probes: **(A)** warning-visible — dialog title reads "Active link exists" and MUI Alert with warning severity is present; **(B)** no-premature-call — `generate-link` has NOT been called at that point; **(C)** confirm-triggers — clicking "Send new link anyway" results in exactly one POST to `generate-link`; **(D)** cancel-no-call / cancel-closes — clicking "Cancel" neither calls `generate-link` nor leaves the dialog open. **In `test:ci` this runs via `test:active-link-warning-ordering:ci`**. |
| `test:customer-info-resend` | Integration test for `POST /api/customer-info/:token/resend-expired` self-serve resend flow. Six probes: **(A)** expired token → 410 with `maskedEmail`; **(B)** successful resend — new row + audit log entry; **(C)** per-token rate limit — 4th attempt returns 429; **(D)** submitted token → 400; **(E)** non-existent token → 404; **(F)** fresh token is valid. **In `test:ci` this runs via `test:customer-info-resend:ci`**. |
| `test:upload-photos-copyable-link` | Regression guard for the "Copy & close" button in `UploadPhotosModal`. Probes: **(CC-A)** button appears once the link is ready; **(CC-A2)** button is enabled; **(CC-B)** clicking the button closes the MUI dialog. **In `test:ci` this runs via `test:upload-photos-copyable-link:ci`**. |
| `test:photo-storage-errors` | Regression guard for storage error sanitisation across upload, delete, and download paths. Tests constructor-throws and `ok:false` SDK response stubs across all photo-related routes. Friendly error copy, no raw SDK internals leaked. **In `test:ci` this runs via `test:photo-storage-errors:ci`**. |
| `test:turnstile-signout` | Regression guard for the bfcache restore fix and the `?signed_out=1` redirect. Verifies: POST `/api/logout` redirects to `/login?signed_out=1`; "You've been signed out." banner visible; Turnstile widget renders and re-renders on `pageshow(persisted:true)`. **In `test:ci` this runs via `test:turnstile-signout:ci`**. |
| `test:invoice-panel-hidden` | CSS visibility regression guard for `#inv-panel` across six pages. Guards against MUI emotion, Tailwind, or other stylesheet overrides of the `.inv-panel` hiding rules in `app-styles.css`. **In `test:ci` this runs via `test:invoice-panel-hidden:ci`**. |
| `test:projects-top-spacing` | Layout regression guard for the Projects page top spacing. Probes: **(PTS-A)** at least 16 px of vertical space between the header bottom and the first stage-filter tab; **(PTS-B)** an `h1` with text "Projects" is visible above the first tab. **In `test:ci` this runs via `test:projects-top-spacing:ci`**. |
| `test:onboarding-conflicts` | End-to-end coverage for the onboarding conflict detection and admin resolution flow. API probes: `POST /api/onboarding/complete` → `pending_profile_updates` stored; `POST /api/admin/users/:id/resolve-profile-conflicts` → cleared. UI probes: `DifferenceIcon` badge visible; Edit dialog shows "Onboarding discrepancies" Alert; badge gone after resolution. **In `test:ci` this runs via `test:onboarding-conflicts:ci`**. |
| `test:open-leads-stale-visibility` | Tab-visibility pause for the open-leads stale badge. **(F1)** Hidden → stale response deferred until `visibilitychange → visible`. **(F2)** Hidden → fresh response arrives while hidden — badge persists until `visibilitychange → visible`. **In `test:ci` this runs via `test:open-leads-stale-visibility:ci`**. |
| `test:ideas` | Puppeteer + API coverage for the Ideas & Feedback page. API probes: CRUD for ideas and comments with correct privilege gating. UI probes: posting an idea prepends a card; comment chip lazy-fetches comments; admin sees delete buttons and confirm dialog; member sees none. **In `test:ci` this runs via `test:ideas:ci`**. |
| `test:keyboard-shortcuts` | Headless Chromium smoke for `window.getShortcut()` in `public/chrome.js`. Exercises four scenarios across `userAgentData` and legacy `navigator.platform` paths for Mac and Windows. No server or database required. |
| `test:conflict-digest-settings` | Puppeteer integration test for the Conflict digest panel in Admin Settings. Three scenarios: **(CD-1a/CD-1b/CD-1c/CD-1d)** SMTP not configured — heading visible, "Last sent" text visible, "Send now" disabled, SMTP-missing Alert shown; **(CD-2a/CD-2b/CD-2c)** SMTP configured — button enabled, no SMTP Alert, "Last sent" reads "Never"; **(CD-3)** clicking "Send now" updates the "Last sent" span to the formatted timestamp. **In `test:ci` this runs via `test:conflict-digest-settings:ci`**. |
| `test:settings-tab-load` | Regression guard for the race condition fixed by replacing early-return null checks with `waitForElement`. Asserts `#hubspot-status-badge` text becomes `"Connected"` and `#lead-statuses-table-wrap` contains a `<table>`. **In `test:ci` this runs via `test:settings-tab-load:ci`**. |
| `test:room-stale-banner-visibility` | Tab-visibility pause for the room-assignments stale banner: **(F1)** stale response deferred until `visibilitychange → visible`; **(F2)** pending clear deferred until visibility restores. **In `test:ci` this runs via `test:room-stale-banner-visibility:ci`**. |
| `test:contacts-all-stale-fallback` | Stale-cache behaviour and hard-cap enforcement for `GET /api/contacts-all`. **(A)** HubSpot 503 → contacts served stale; **(B)** network drop → still served stale; **(C)** stale cap exceeded → 502; **(D)** recovery — fresh data fetched and subsequent requests served from cache. **In `test:ci` this runs via `test:contacts-all-stale-fallback:ci`**. |
| `test:contacts-stale-visibility` | Puppeteer tab-visibility pause for the stale-data banner on `CustomersPage`. **(F1)** stale response deferred while hidden; **(F2)** pending clear deferred while hidden. **In `test:ci` this runs via `test:contacts-stale-visibility:ci`**. |
| `test:login` | Autofill crash guard for the `/login` React island. Simulates browser credential autofill via the native `HTMLInputElement.prototype` value setter. UI probes: **(UI-1)** `LoginPage` renders with both inputs visible; **(UI-autofill)** native autofill on both inputs → zero console errors. **In `test:ci` this runs via `test:login:ci`**. |
| `test:open-deal` | Backend integration tests for the OpenDealActionModal flows in `quickbooks.js`. Mock QB HTTP server (`QB_API_BASE_OVERRIDE`) + mock HubSpot + mail capture (`MAIL_TRANSPORT_FILE_OVERRIDE`). **(A)** default 10% deposit calculated from estimate TotalAmt; **(B)** custom `depositPercent` from `qb_settings` applied on accept-deal; **(C)** `BillEmailBcc`/`BillEmailCc` set on every QB send when `copy_me_email` configured (both accept-deal and standalone invoice send); **(D)** `qb_settings` round-trip: GET returns defaults, PUT takes effect, accept-deal uses updated values; **(E)** accept-deal full flow: estimate Accepted, others Rejected, deposit invoice created/sent, app email sent, lead status → `DEPOSIT_INVOICE`; **(F)** decline flow: estimates Rejected, thank-you email sent (or skipped), lead status → `DECLINED_DEAL`; **(G)** failed invoice send does not advance lead status — no HubSpot PATCH fired, contact stays `OPEN_DEAL`; **(H)** amendments path: data-load endpoint (`POST /api/card-actions/open-deal`) is non-mutating and lead status stays `OPEN_DEAL`; **(I)** idempotency: second sequential accept-deal call for the same estimate returns the existing invoice without creating a duplicate; **(J)** concurrent idempotency: two simultaneous accept-deal calls for the same estimate (serialised by a `pg_advisory_lock`) produce exactly one QB invoice and both callers receive the same `invoiceId`; **(K)** concurrent-retry send guard: two simultaneous retries (invoice already exists) produce exactly one QB send and the losing caller gets `sendSkipped=true`; **(L)** concurrent-decline guard: two simultaneous decline-deal calls produce exactly one thank-you email; **(M)** sequential-retry decline: a second decline call (`sendThankYou=false`) after a prior successful send returns `emailAlreadySent=true`. **In `test:ci` this runs via `test:open-deal:ci`**. |
| `test:invoice-admin-controls` | Privilege-gate and data-scoping tests for QuickBooks invoice endpoints. Manager gets 403 on all QB routes; unauthenticated gets 401; admin gets non-403. Data-scoping: no raw token fields in error responses. **In `test:ci` this runs via `test:invoice-admin-controls:ci`**. |
| `test:qb-payment-history` | Integration tests for `GET /api/quickbooks/contacts/:contactId/payments`. Mock QB server (`QB_API_BASE_OVERRIDE`). **(AUTH1)** Unauthenticated → 401; **(AUTH2)** Member → 403; **(AUTH3)** Invalid (non-numeric) contactId → 400; **(A)** QB not connected → `{ qbConnected: false }`; **(B)** Connected with no data → empty arrays + zero summary; **(C)** Fully paid invoice → `status=paid`; **(D)** Partially paid invoice → `status=partial`, amounts correct; **(E)** Unpaid invoice → `status=unpaid`; **(F)** Deposit label — invoice matching `design_visits.deposit_invoice_id` labelled `"Deposit"`; **(G)** Ownership guard — payment with wrong `CustomerRef` excluded; **(H)** Summary totals correct across mixed invoices; **(I)** 60-second cache — second request within TTL issues no new QB query; **(J)** Manager can call the endpoint (requireManagerOrAdmin). **In `test:ci` this runs via `test:qb-payment-history:ci`**. |
| `test:payment-history-component` | Vitest unit tests for the `PaymentHistory` React component. Covers banner and list variants; loading skeletons; paid/partial/unpaid alert copy; `onPaidStateChange` callback for all states (true/false/null); QB-not-connected hint visible to admin only; error state; overall-summary banner (no invoiceId); invoice-label and status-chip rendering in list variant. No server, no DB, no Puppeteer. |
| `test:invoice-bc-sync` | Puppeteer BroadcastChannel refresh test for invoice saves. Probes: **(BC-A)** `InvoicesSection` on `CustomerDetailPage` re-fetches after save without full reload; **(BC-B)** `StandaloneInvoicesPage` on `/invoices` re-fetches without reload. **In `test:ci` this runs via `test:invoice-bc-sync:ci`**. |
| `test:bottom-nav-default` | Regression guard for the admin-configured `__default__` nav layout reaching users with no `job_role`. **(DEF-API)** GET `/api/nav-role-config` returns default primary_keys and `default_is_customized=false` for a no-role user before any admin change; **(DEF-API-CUSTOM)** after admin PATCHes `__default__`, the endpoint returns the new keys and `default_is_customized=true`; **(DEF-UI/DEF-UI-RESTORE)** BottomNav renders the custom `__default__` keys for the no-role user / falls back to privilege-level defaults after `__default__` is reset. **In `test:ci` this runs via `test:bottom-nav-default:ci`**. |
| `test:nav-customise-reset` | Nav-customisation end-to-end test. API probes: **(API)** GET/PATCH `/api/users/me/prefs` status codes and auth gating. Default-row probes: **(DEF-OPEN)** `__default__` dialog opens and pre-selects seeded keys; **(DEF-SAVE)** saving PATCHes `/api/admin/nav-role-config/__default__`. Reset probes: **(RST-DISABLED)** Reset disabled when selection matches defaults; **(RST-RESET/RST-SAVE)** Reset pre-checks correct defaults and saves them. Inherit-banner probes: **(INHERIT-BANNER-ON/INHERIT-BANNER-OFF)** "inherits the default layout" Alert shown/hidden correctly. Note: CUST-OPEN/SAVE/PERS/FALL/CANCEL probes are omitted because the More drawer requires more than four visible nav items (FIT_THRESHOLD=4 means allFit is always true with the current four-tab nav). **In `test:ci` this runs via `test:nav-customise-reset:ci`**. |
| `test:admin-tab-skeletons` | Puppeteer request-interception suite for the Permissions, Requests, and Audit Log admin tabs — in-component data skeleton layer (layer 2). Complements `test:admin-tab-skeletons-suspense` which tests layer 1. **In `test:ci` this runs via `test:admin-tab-skeletons:ci`**. |
| `test:room-stale-banner` | Puppeteer coverage for the room-assignments stale-data banner on the Projects page. Probes: **(STALE)** banner appears; **(FRESH)** banner absent; **(DISMISS)** clicking × dismisses the banner. **In `test:ci` this runs via `test:room-stale-banner:ci`**. |
| `test:permissions-ui` | Puppeteer regression guard for the correct icons in the Permissions admin tab. Probes: **(PERMISSIONS-TAB)** Permissions tab activates and job roles list renders; **(ICON-DELETE)** "Remove role" renders the MUI Delete (trash-can) SVG; **(ICON-NOT-CLOSE)** no Close icon in "Remove role" buttons; **(ICON-TUNE)** Tune icon is distinct from both Delete and Close; **(RUNTIME)** no JS errors during Permissions tab load. **In `test:ci` this runs via `test:permissions-ui:ci`**. |
| `test:project-contacts-unknown-status` | Regression guard for the orphan-check logic. Verifies contacts with room data but an absent HubSpot lead status appear in `GET /api/project-contacts` with `_statusUnknown: true` rather than being silently dropped. **In `test:ci` this runs via `test:project-contacts-unknown-status:ci`**. |
| `test:profile-google-calendar` | Puppeteer coverage for `GoogleCalendarCard` on `/profile`. Probes: **(GC-A)** disconnected state; **(GC-B)** connected state; **(GC-C)** clicking "Disconnect" calls `POST /auth/logout-google` and transitions to disconnected state. **In `test:ci` this runs via `test:profile-google-calendar:ci`**. |
| `test:scheduling-past-time-guard` | Puppeteer coverage for the past-time confirmation dialog and 15-minute start-time warning for `ScheduleVisitModal`. **(VCM)** VCM modal opens with DateTimePicker and Schedule button; **(VCM-PAST/VCM-BACK/VCM-PROCEED)** past-confirm dialog appears / "Go back" dismisses / "Schedule anyway" fires POST for `ScheduleVisitModal`; **(WARN-VCM)** 15-minute start-time warning Alert in edit mode. **In `test:ci` this runs via `test:scheduling-past-time-guard:ci`**. |
| `test:upload-photos-resend-mode` | Regression guard for resend-mode detection in `UploadPhotosModal.tsx`. Probes: **(A)** pending row → resend mode; **(B)** no pending rows → send mode; **(C)** network error → falls back to send mode. **In `test:ci` this runs via `test:upload-photos-resend-mode:ci`**. |
| `test:upload-photos-modal-labels` | Regression guard for button-label / `data-testid` drift in `UploadPhotosModal`. When `link-status` returns `hasActiveLink: true` the modal enters the confirming phase. Probes: **(L-A)** `[data-testid="cah-confirm-generate"]` is present; **(L-B)** button visible text includes "Generate new link"; **(L-C)** the stale id `[data-testid="cah-confirm-resend"]` is absent. Guards against a rename of one attribute without the other silently breaking automated tests. **In `test:ci` this runs via `test:upload-photos-modal-labels:ci`**. |
| `test:hubspot-credential-audit` | Integration test for `admin_audit_log` entries on HubSpot credential changes. Covers member 403 gating, `PATCH`/`DELETE` audit rows, all three valid credential keys, and validation errors producing no audit rows. **In `test:ci` this runs via `test:hubspot-credential-audit:ci`**. |
| `test:storybook-output-clean` | Build verification (`scripts/check-storybook-output-clean.mjs`). Asserts that no application HTML pages or JS modules were copied into the Storybook output root. Guards the `viteFinal: config.publicDir = false` hook in `.storybook/main.ts`. |
| `test:lead-status-delete-substatus-clear` | Focused integration test for `clearOrphanedSubstatusesForDeletedStatus` in `server.js`. When a lead status is deleted via `DELETE /api/admin/lead-statuses/:key`, a background job must search HubSpot contacts with that status, PATCH each to clear their substatus, then re-query to verify the clear took effect. **In `test:ci` this runs via `test:lead-status-delete-substatus-clear:ci`**. |
| `test:customer-info` | Regression coverage for the `upload_photos_and_info` card-action handler. Exercises the full customer-info flow end-to-end via HTTP probes: token creation, form data retrieval by masked email/name, file upload and listing, form submit, admin notification email contents, and expiry guards. **In `test:ci` this runs via `test:customer-info:ci`**. |
| `test:customer-info-email-attachments` | Regression guard for the photo-attachment path in `sendAdminNotificationEmail` in `customer-info.js`. Downloads each submitted photo from object storage and attaches it to the admin notification email; verifies attachments appear correctly and attachment-download errors are handled cleanly. **In `test:ci` this runs via `test:customer-info-email-attachments:ci`**. |
| `test:customer-info-rail` | End-to-end test for the `CustomerInfoSubmissionsRail` React component on the customer-detail page. Stubs all `/api/*` calls via `evaluateOnNewDocument` fetch interception, then asserts submission card rendering, review/resend button branching, and file-upload state across multiple submission states. Probes: **(A)** expired card hidden + count badge; **(B)** active card with `form_link` shows Copy + Open; **(C)** Copy click swaps icon; **(D)** submitted card shows Review only; **(E)** active card with null `form_link` shows neither Copy nor Open; **(F1)** skipped-email Alert renders with the correct count when `email_skipped_count` is non-zero; **(F2)** skipped-email Alert is absent when `email_skipped_count` is 0; **(G)** clicking Review opens the submitted card body and renders the address and room-count text in the expanded body; **(G4)** an isolated page with `corrected_email` set — "Email: corrected@example.com" is visible with a non-zero bounding height in the expanded Corrections section; **(G5)** "Mobile: 07700900123" is visible with a non-zero bounding height in the same Corrections section; **(G6a)** "Notes" section heading visible with non-zero height after clicking Review on a submitted card with `room_notes` set; **(G6b)** the exact `room_notes` string "Master bedroom has sloped ceiling on north wall" is visible with a non-zero bounding height in the expanded card body. **In `test:ci` this runs via `test:customer-info-rail:ci`**. |
| `test:customer-info-conflict-warning` | End-to-end regression guard for the active-link conflict warning in `CustomerInfoSubmissionsRail`. When a staff member clicks "Copy link" or "Open link", the component fetches `/api/customer-info/by-contact/:id/link-status` and compares the returned `expiresAt` with the displayed card's value. Probes: **(A)** matching expiresAt → no conflict Alert, clipboard written immediately; **(B)** differing expiresAt → conflict Alert appears, Alert text verified; **(B2)** "Copy anyway" dismisses Alert; **(B3)** "Cancel" dismisses without copying, Alert text verified; plus equivalent **(A-open/B-open/B2-open/B3-open)** probes for the "Open link" path (including Alert-text checks for B-open and B3-open) and **(A-open-blocked)** popup-blocked fallback. **In `test:ci` this runs via `test:customer-info-conflict-warning:ci`**. |
| `test:customer-info-stale-rail` | Regression guard for the `customer-info-link-generated` CustomEvent wiring in `CustomerInfoSubmissionsRail`. Verifies that dispatching the event on `window` (with the matching `contactId`) triggers a second GET to `/api/customer-info/by-contact/:id` and re-renders the rail with updated data. Probes: **(A1)** initial fetch happened (fetchCount = 1); **(A2)** rail renders initial submission card; **(B)** re-fetch triggered by the event; **(C)** rail re-renders with the new card; **(D)** event dispatched with a foreign `contactId` does not trigger an extra fetch; **(E)** submitted card body shows address text after re-render; **(F)** same expanded card body shows `corrected_email` and `corrected_mobile` values. The complementary emitter side (dispatching `customer-info-link-generated` from `UploadPhotosModal`) is covered by `test:upload-photos-modal-emitter`. **In `test:ci` this runs via `test:customer-info-stale-rail:ci`**. |
| `test:customer-info-live-badge` | Regression guard for the SSE → re-fetch → "Photos received" badge flow on the projects board. Stubs `/api/project-contacts` via Puppeteer request interception: first call returns a contact with `AWAITING_PHOTOS` status and no `AWPH_RECEIVED` substatus; subsequent calls return the same contact with `AWPH_RECEIVED`. Probes: **(CI-LB-1)** badge absent on initial page load; **(CI-LB-2)** `POST /api/customer-info/:token` returns `200 ok=true`; **(CI-LB-3)** server pushes a `customer_info_submitted` SSE event, `useProjectsData` increments `fetchNonce`, re-fetches project-contacts, and the "Photos received" badge appears without a page reload. **In `test:ci` this runs via `test:customer-info-live-badge:ci`**. |
| `test:upload-photos-modal-emitter` | End-to-end guard for the full `UploadPhotosModal → rail refresh` signal chain. Opens the real modal via `window.openCardActionModal`, stubs `link-status → { hasActiveLink: true }` to reach the confirming phase, stubs `generate-link` POST to return a synthetic link, then clicks "Generate new link". Probes: **(A)** initial rail render; **(A2)** initial fetchCount = 1; **(B)** modal opens to "Active link exists" confirming phase; **(C)** clicking "Generate new link" calls the generate-link API; **(D)** the `generateLink` success handler dispatches `customer-info-link-generated` → rail re-fetches (fetchCount = 2); **(E)** rail re-renders with both cards. Guards the emitter side (`window.dispatchEvent` in `UploadPhotosModal.generateLink`) that `test:customer-info-stale-rail` does not exercise. **In `test:ci` this runs via `test:upload-photos-modal-emitter:ci`**. |
| `test:superseded-tooltip` | Regression guard for the Superseded chip and its tooltip in `CustomerInfoSubmissionsRail`. When two active-pending rows exist, the older row is marked superseded and its chip is wrapped in a MUI Tooltip. Probes: **(ST-A)** "Superseded" chip label is present in the rail; **(ST-B)** hovering the chip reveals tooltip text "A newer link has been generated — this one is no longer active"; **(ST-E)** exactly 2 submission-card roots are rendered (early guard — catches regressions where no active card is rendered at all, before per-card probes run); **(ST-C)** the superseded card has no `copy-link-btn`, `open-link-btn`, or `resend-link-btn` action buttons; **(ST-D)** the active (non-superseded) card has at least one of `copy-link-btn`, `open-link-btn`, or `resend-link-btn` present. **In `test:ci` this runs via `test:superseded-tooltip:ci`**. |
| `test:audit-log-scrolling` | Infinite-scroll regression guard for the Audit Log admin tab. Probes: **(A)** initial 50 rows load; **(B)** scrolling to bottom triggers next-page fetch; **(C)** skeleton appears during fetch; **(D)** rows 51-100 append correctly. Standalone only — not enrolled in `test:ci`. |
| `test:photo-approval-notification` | Regression test for richer audit log entries on `approve_profile_photo` / `reject_profile_photo`. Guards against regressions where notification status, reviewer identity, or rejection reason are dropped from the `details` column. **In `test:ci` this runs via `test:photo-approval-notification:ci`**. |
| `test:photo-reviews` | Integration tests for the `review-customer-photos` card-action-handler routes in `photo-reviews.js`. Covers `GET` returning `{ submission: null }` when no submitted photos exist, approve/reject flows with HubSpot note creation, privilege gating, and audit-log entries. **In `test:ci` this runs via `test:photo-reviews:ci`**. |
| `test:start-design-visit` | End-to-end live test for the `start_design_visit` card-action handler. Boots a disposable server via the privileges harness, drives the multi-step wizard via Puppeteer, and verifies the wizard state, form persistence, and HubSpot side effects. **(A)** wizard submit — POST `/api/design-visits` returns `{ ok, designVisitId }`; DB rows confirmed in `design_visits`, `design_visit_rooms`, and `design_visit_room_images`; status flips to `submitted`; **(A2)** POST with mixed images — only the valid image row is inserted; **(A3)** all-missing-key images — zero image rows inserted, no crash; **(A4)** submit with no images — image-array absent, visit DB row still created; **(B)** sign-off approve — GET sign-off summary; POST approve flips status to `signed_off`; second POST returns 404; **(C)** sign-off revision — POST revision flips status to `revision_requested`; re-submit flips back to `submitted`; **(D)** token security — expired, wrong, and already-consumed tokens return 404; **(E)** admin catalogue CRUD — POST/PATCH/DELETE handles, furniture ranges, and door styles via admin API; non-admin mutations return 403; **(F)** BroadcastChannel refresh in wizard — catalogue-changed BC from a second tab refreshes the dropdown without closing the wizard; **(G)** privilege gates (REST) — unauthenticated POST `/api/design-visits` → 401; non-admin DELETE → 403; public sign-off routes reachable without session; **(T)** terms-conditions version stamping — `design_visits.terms_condition_version_id` is set at submit time. **In `test:ci` this runs via `test:start-design-visit:ci`**. |
| `test:design-visit` | End-to-end live test for the design-visit wizard and sign-off flow. Boots a disposable server, drives the full wizard, submission, and customer-facing sign-off page via Puppeteer, and verifies HubSpot note creation and QuickBooks estimate side effects. **In `test:ci` this runs via `test:design-visit:ci`**. |
| `test:visit-edit-cancel` | End-to-end test for the edit and cancel actions on delivery/installation visit cards in the customer-detail page. Covers `PATCH /api/visits/:id`, visit-card re-render after edit, and cancel confirmation dialog. (`DELETE /api/visits/:id` was removed; the cancel flow now drives the Google Calendar delete path.) **In `test:ci` this runs via `test:visit-edit-cancel:ci`**. |
| `test:dv-catalogue-admin` | End-to-end live test for the Design Visit admin catalogue modals (Handles, Furniture Ranges, Door Styles). Boots a disposable server, drives CRUD operations for each catalogue type via Puppeteer, and verifies modal open/close, item create/edit/delete, and ordering state. **(H)** door-style dropdown value persisted in DB and returned by API after admin create. **In `test:ci` this runs via `test:dv-catalogue-admin:ci`**. |
| `test:dv-catalogue-image-upload` | End-to-end test for the Design Visit Handle image-upload flow within the catalogue admin modal. Verifies file selection, upload progress, and the resulting image preview in the admin UI. **In `test:ci` this runs via `test:dv-catalogue-image-upload:ci`**. |
| `test:dv-catalogue-reorder` | End-to-end test for the up/down arrow reorder controls in the DV catalogue admin modal. Asserts that clicking the arrows changes item order both in the UI and in the persisted API response. **In `test:ci` this runs via `test:dv-catalogue-reorder:ci`**. |
| `test:window-ui-smoke` | Smoke test: static chrome mount points (`#bottom-nav`, `#header-bar`, etc.) must be present on every dashboard page. Boots a disposable server, loads each protected page via Puppeteer, and asserts all expected mount elements are in the DOM. **In `test:ci` this runs via `test:window-ui-smoke:ci`**. |
| `test:sign-off-stale-link` | End-to-end live test for the "superseded" sign-off path. Verifies that a design-visit sign-off link keeps working after the designer re-opens the visit or resubmits, serving the correct superseded-state UI rather than a broken page. **In `test:ci` this runs via `test:sign-off-stale-link:ci`**. |
| `test:react-admin-tabs` | End-to-end smoke test for the React island mounted into the admin panel (`src/react/main.tsx`). Confirms the bundle renders the Search (`#tab-search`) and Team (`#tab-team`) panels with expected React-owned markup. Guards against Vite build regressions and missing `MOUNTS` entries. **In `test:ci` this runs via `test:react-admin-tabs:ci`**. |
| `test:workflow-tab` | End-to-end smoke test for the Workflow tab accordion tree (`WorkflowPage.tsx`). All five WorkflowPage API calls are intercepted at the browser layer with fixture data so the suite runs against an empty isolated database. Probes: **(A)** Stage-level accordion items render with "Sales", "Design Visit", and "Survey" headings; **(B)** expanding the "Sales" accordion reveals its "New Lead" Lead Status row; **(C)** clicking "Go to Action handlers →" calls `window.adminSwitchToTab("actionhandlers")`; **(D)** clicking "Go to Stages →" calls `window.adminSwitchToTab("stages")`. **In `test:ci` this runs via `test:workflow-tab:ci`**. |
| `test:new-customer-flow` | End-to-end live test for the "New customer" entry point on `/customers`. Boots a disposable server, drives the new-customer modal via Puppeteer (name/phone/email fields, submit, and the resulting contact card), and verifies the HubSpot contact-creation side effects. **In `test:ci` this runs via `test:new-customer-flow:ci`**. |
| `test:new-customer-counts-retry` | Focused integration test for the `attemptCreatedCounts` retry logic inside the `onCreated` callback of `CustomersPage.tsx`. Verifies the helper retries `loadLeadStatusCounts` up to `MAX_CREATED_RETRIES` times when the count endpoint returns stale data, then stops. **In `test:ci` this runs via `test:new-customer-counts-retry:ci`**. |
| `test:bottom-nav-lint` | Static lint: every entry in the `NAV` array exported from `BottomNav.tsx` must have both an `Icon` and an `IconOutlined` field defined. Guards against accidentally shipping a tab without an outlined icon variant. No server, no DB, no Puppeteer. |
| `test:hubspot-credentials` | CI check (`scripts/check-hubspot-credentials.mjs`). Fails immediately if `HUBSPOT_APP_ID` or `HUBSPOT_CLIENT_SECRET` is absent. When `HUBSPOT_ACCESS_TOKEN` is also present, performs a live GET to the subscriptions endpoint to verify the credentials are still valid. No server, no DB, no Puppeteer. |
| `test:hubspot-429-retry` | Integration test confirming the urgency endpoint recovers from a transient HubSpot 429 via `hubspotRequestWithRetry`, and that `dvHubspotRequestWithRetry` in the design-visit pipeline retries on transient errors and exhausts gracefully on persistent failures. **In `test:ci` this runs via `test:hubspot-429-retry:ci`**. |
| `test:hubspot-429-retry-contacts` | Integration test confirming `GET /api/contacts-all` and `GET /api/open-leads` recover from a transient HubSpot 429 via `hubspotSearchWithRetry`, that contacts-all behaves correctly when all retries are exhausted (both no-cache error path and stale-cache fallback), and that the endpoint recovers and fetches fresh data once HubSpot comes back. **In `test:ci` this runs via `test:hubspot-429-retry-contacts:ci`**. |
| `test:room-assignments-outage` | Integration test verifying `GET /api/localdata/all` continues to serve room-assignment data during a prolonged HubSpot outage — even after the 1-hour stale cap that governs `GET /api/contacts-all` has been exceeded. **In `test:ci` this runs via `test:room-assignments-outage:ci`**. |
| `test:change-password` | End-to-end test for the Change Password dialog on `/profile`. Drives the dialog via Puppeteer: wrong current-password error, mismatched new-password error, and successful update that re-authenticates the session. **In `test:ci` this runs via `test:change-password:ci`**. |
| `test:set-password` | End-to-end test for the `/set-password` React page. Primary focus is the autofill crash guard — simulates browser credential autofill via the native `HTMLInputElement` value setter and a bubbling `input` event, which bypasses React's synthetic `onChange` tracking, and asserts zero console errors. **In `test:ci` this runs via `test:set-password:ci`**. |
| `test:bottom-nav` | End-to-end test for the "More" drawer in the bottom navigation bar across roles (member, manager, admin). Verifies correct visible tabs, hidden tabs, and that the More drawer opens, scrolls, and dispatches navigation correctly for each role. **In `test:ci` this runs via `test:bottom-nav:ci`**. |
| `test:nav-active-tab` | Regression guard for the `matchPath` prefix-route logic in `BottomNav.tsx`. Verifies the correct tab is highlighted for `/customers`, `/customers/:id`, `/projects`, and other prefix-ambiguous routes. **In `test:ci` this runs via `test:nav-active-tab:ci`**. |
| `test:nav-customise` | End-to-end test for the nav tab customisation dialog. Covers `GET`/`PATCH /api/users/me/prefs` auth gating, opening the dialog from the More drawer, reordering tabs, saving, and verifying the new order persists. **In `test:ci` this runs via `test:nav-customise:ci`**. |
| `test:trades` | End-to-end test suite for the Trades page (React + MUI). Covers CRUD operations (create, edit, delete trade company), role gating (member read-only, manager/admin write), duplicate-phone detection, and full Puppeteer UI interaction via incognito contexts. **In `test:ci` this runs via `test:trades:ci`**. |
| `test:customers-pagination` | Verifies the Customers page pagination controls and count label. Probes: **(A)** 30 contacts → MUI Pagination appears and page-2 click sends `?page=2` with differing results; **(C)** "Showing X–Y of Z" count label visible on page 1; **(D)** changing a filter (sort, search, or lead-status) resets from page 2 to page 1. **In `test:ci` this runs via `test:customers-pagination:ci`**. |
| `test:dev-mode-bc-sync` | Regression guard for the `dev_mode_changed` BroadcastChannel wiring. Exercises the full code path: admin toggle Switch → `POST /api/admin/hubspot/dev-mode` → BC message → receiving tab updates its dev-mode indicator without a page reload. **In `test:ci` this runs via `test:dev-mode-bc-sync:ci`**. |
| `test:skipped-photo-warning` | Regression guard for the skipped-photo warning alert in `CustomerInfoSubmissionsRail`. When photos are too large to attach, the server stores `email_skipped_count` on the submission row; asserts the Rail renders an MUI Alert with that count. **(SKP-A)** plural warning Alert visible with correct text (email_skipped_count=2); **(SKP-B)** singular warning Alert visible with correct text (email_skipped_count=1); **(SKP-C)** no warning Alert when email_skipped_count=0; **(SKP-D)** "still viewable here" anchor href correct in both plural and singular cases; **(SKP-E)** clicking the anchor makes photos visible in the section. **In `test:ci` this runs via `test:skipped-photo-warning:ci`**. |
| `test:skipped-photo-dashboard-link` | Regression guard for the dashboard link included in admin notification emails when photos are skipped (too large / not found in object storage). Verifies `sendAdminNotificationEmail` embeds a clickable link so admins can still view the photos. **In `test:ci` this runs via `test:skipped-photo-dashboard-link:ci`**. |
| `test:active-projects-hubspot-outage` | Regression guard for the Active Projects error branch when `GET /api/contacts-all` returns 502. Verifies the "Active Projects" section header is still visible and an appropriate error UI (not a blank page or crash) is shown. **In `test:ci` this runs via `test:active-projects-hubspot-outage:ci`**. |
| `test:nav-role-config` | End-to-end test for the nav role configuration admin API and UI. **(API-DEFAULT)** GET returns `__default__` keys for a user with no `job_role`; **(API-UNAUTH)** GET requires authentication; **(API-ROLE)** GET returns role-specific `primary_keys` when the user has a matching config; **(API-PATCH-ADMIN/API-PATCH-IS-CUSTOMIZED)** PATCH succeeds for admin and sets `is_customized=true`; **(API-PATCH-MEMBER/API-PATCH-MANAGER/API-PATCH-VALIDATE)** PATCH returns 403 for member/manager, 400 for invalid bodies; **(API-LIST/API-LIST-MEMBER)** GET `/api/admin/nav-role-configs` is admin-only; **(API-JOB-ROLE-CLONE)** POST `/api/admin/job-roles` creates a `nav_role_configs` row with `is_customized=false`; **(API-INHERIT)** a role with `is_customized=false` returns `__default__` keys; **(API-DELETE)** DELETE sets `is_customized=false` and the user inherits `__default__`; **(API-DEFAULT-PROPAGATE)** changing `__default__` primary_keys propagates immediately to uncustomised roles; **(API-DELETE-DEFAULT-REJECT)** DELETE `__default__` returns 400; **(UI-ROLE-NAV)** BottomNav renders the role-specific primary tabs when the user's job_role has a custom nav config. **In `test:ci` this runs via `test:nav-role-config:ci`**. |
| `test:stage-scoped-pills` | Verifies stage-scoped lead-status and substatus pill filtering on the Customers page updates correctly when switching stage tabs. All `/api/*` calls are intercepted at the browser level; the Express server is still used for auth and static assets. **In `test:ci` this runs via `test:stage-scoped-pills:ci`**. |
| `test:project-contacts-dev-mode` | Regression guard for the dev-mode filtering in `GET /api/project-contacts`. Verifies: **(A)** dev mode OFF → all contacts returned (including `hw_test_user=true`); **(B)** dev mode ON → `hw_test_user=true` contacts excluded; **(C)** dev mode OFF again → all contacts visible once more; **(D)** UI smoke — projects board renders correctly after dev-mode toggle. **In `test:ci` this runs via `test:project-contacts-dev-mode:ci`**. |
| `test:handler-config-blocks` | Vitest unit tests for the `HandlerConfigBlocks` React components (`ScheduleVisitConfig`, `ShowMessageConfig`, etc.). Renders each config block in isolation and asserts correct field rendering, validation states, and callback signatures. **In `test:ci` this runs standalone via `test:handler-config-blocks`** (no DB required). |
| `test:conflicts-review-logic` | Vitest unit tests for the pure diff/restore logic in `ConflictsReview.tsx`. Covers `buildFieldDiff` (determines which fields and rooms changed for the conflict-resolution UI) and `buildRestoreBody` (assembles the write-body from per-field/per-room choices), including room add/remove end-to-end scenarios with ID-based pairing. No server, no DB, no Puppeteer. |
| `test:formatters` | Vitest unit tests for `compactRelativeTime` and `latestTimestamp` in `src/react/utils/formatters.ts`. Covers all time buckets (now, Xmin, Xhr, Xd, Xw, Xmo), null/undefined/unparseable inputs, future timestamps, epoch-ms string inputs, and multi-value latest-wins selection. No server, no DB, no Puppeteer. |
| `test:storybook-smoke` | Builds Storybook to a temporary directory, spins up a lightweight static HTTP server, then loads every story via Puppeteer and asserts none throw render errors or remain blank. Guards against regressions in the design-system component library. |
| `test:admin-grouped-tabs-bar-stories` | Puppeteer story-render probe for the five `AdminGroupedTabsBar` stories (`PeopleGroupActive`, `ConfigurationGroupActive`, `WithBadges`, `DeveloperGroupHidden`, `IntegrationsGroupActive`). Loads each from the pre-built Storybook static output and asserts **(A)** no uncaught JS exceptions; **(B)** no Storybook error-boundary overlay; **(C)** `#storybook-root` is non-empty; **(D)** no React error-boundary console rethrow. Requires `STORYBOOK_OUT_DIR` — set automatically by `run-ci.mjs` and `run-ci-parallel.mjs` after the shared `build:storybook` step. <!-- story-count: 5 --> |
| `test:tabbar-stories` | Puppeteer story-render probe for the five `TabBar` stories (`Default`, `ActiveSecondTab`, `WithBadges`, `Interactive`, `ActiveIndicatorVisible`). Loads each from the pre-built Storybook static output and asserts **(A)** no uncaught JS exceptions; **(B)** no Storybook error-boundary overlay; **(C)** `#storybook-root` is non-empty; **(D)** no React error-boundary console rethrow. For `ActiveIndicatorVisible` only, also asserts **(E)** that the plum active-tab indicator is visually present: `.ui-tabbar` must have `overflow-y: clip` (not `hidden`), and the active button must have a non-zero, non-transparent `border-bottom`. This catches the overflow-clip regression that silently hides the indicator even when all CSS classes are applied correctly. Requires `STORYBOOK_OUT_DIR` — set automatically by `run-ci.mjs` and `run-ci-parallel.mjs` after the shared `build:storybook` step. <!-- story-count: 5 --> |
| `test:design-system-page-story` | Puppeteer story-render probe for the `DesignSystemPage` Storybook story (`AllSkeletons`). Loads the story from the pre-built Storybook static output and asserts **(A)** no uncaught JS exceptions; **(B)** no Storybook error-boundary overlay; **(C)** `#storybook-root` is non-empty; **(D)** no React error-boundary console rethrow; **(E)** exactly 14 `[data-component-showcase]` cards are present and each contains at least one `.MuiSkeleton-root` element. Catches regressions where a skeleton variant is removed from the gallery or fails to render its MUI Skeleton nodes. Requires `STORYBOOK_OUT_DIR` — set automatically by `run-ci.mjs` and `run-ci-parallel.mjs` after the shared `build:storybook` step. <!-- story-count: 1 --> |
| `test:workflow-js-no-dups` | Static guard (`scripts/check-workflow-js-duplicates.mjs`) that none of the 11 picker-cluster functions moved to `workflow-core.js` appear as top-level function declarations in `public/workflow.js`. Standalone only — not in the `test:ci` pipeline. |
| `test:retired-tokens` | Static lint (`scripts/check-retired-tokens.mjs`). Scans `src/react/**/*.{ts,tsx,css}` and `public/**/*.{js,css,html}` (excluding compiled `react/` and `storybook/` dirs) for retired CSS custom-property names that were renamed during the status-colour migration: `--status-danger` (standalone form, without `-bg`/`-text`/`-border` suffix), `--status-warn-bg`, `--status-warn-border`, and `--status-warn-text`. Prevents the old names from being reintroduced accidentally. Suppress genuine exceptions with `// retired-token-ok: <reason>`. No server, no DB, no Puppeteer. |
| `test:tokens-css` | Static lint (`scripts/check-tokens-css.mjs`). Reads `public/tokens.css` and fails if any `--status-*` custom property name contains an uppercase letter. Guards against `STATUS_COLORS` keys in `src/react/theme.ts` that do not convert cleanly to all-lowercase kebab-case via `camelToKebab()` (e.g. a key like `warningLight` would silently produce `--status-warningLight-bg`, breaking any `var()` reference that expects the lowercase form). No server, no DB, no Puppeteer. |
