// visits.js — retired. The `visits` table and all its API routes have been
// removed. This stub is kept so that any cached `require('./visits')` in
// server.js continues to resolve without error while the import is cleaned up.
const express = require('express');
const router = express.Router();

module.exports = { router };
