# Measure Once

Project management dashboard (HubSpot CRM integration).

## User preferences
- **Tests:** Only create tests for crucial backend functionality — auth, data
  integrity, API error handling. Do NOT create tests for UI behaviour, CI
  documentation, or test infrastructure. Creating new testing tasks is not a
  priority — only add tests when a specific feature task explicitly requires
  them.
- **UX first:** Before planning or building any feature, ask clarifying
  questions about how users will actually use it — expected workflows, edge
  cases, frequency of use, and pain points. Delivering an easy, intuitive
  experience is the top priority.
- **Form persistence:** All in-progress forms and multi-step data inputs must
  persist their state so that a network disconnection, accidental navigation,
  or page refresh does not lose the user's work. Use localStorage (or
  sessionStorage where appropriate) to draft-save form values and restore them
  on re-mount. Clear the draft only after a successful submit.
- **Component reuse:** Always reuse existing site components before writing new
  ones. When a genuinely new MUI component type is required, reference the
  official MUI documentation before implementing it. If something outside MUI
  or the existing component set appears necessary, confirm with the user before
  proceeding.
- **Follow-up tasks:** Always propose follow-up tasks for complex work. Keep
  them as small and focused as possible. Never pre-approve anything that would
  dramatically change application functionality — always surface it for the
  user to review and approve first.
- **Design changes:** Before making any UI/design change, reference the
  existing design system docs (theme.ts, ICONS.md, src/react/README.md) and
  ask as many clarifying questions as needed to understand what the user wants.
  Any new component or significant visual update must have a Storybook story
  in `src/react/stories/` (or co-located with the component). The design
  system gallery is Storybook, accessible via the Design System card in the
  admin Settings tab or at `/storybook/`.

## Stack
- Node.js 20 + Express; single `server.js` serves API and static assets.
- Static frontend in `public/` (vanilla JS + Tailwind CDN) being progressively
  migrated to React + MUI islands in `src/react/`.

## Replit Setup
- Workflow: `Start application` runs `npm start`.
- Server binds `0.0.0.0:5000` (`PORT` env override).
- Deployment: VM target, `node server.js`.
- Build step: `npm run build:react && npm run build:storybook`.
  - `build:react` → `public/react/main.js` (stable filename, self-mounting).
  - `build:storybook` → `public/storybook/`, served as static assets.
  - Both output dirs are gitignored.

## Local development workflow for React changes

Two patterns for iterating on `src/react/` without a manual rebuild each time:

### Option A — Vite dev server (recommended)
Run two terminals in parallel:

```
# Terminal 1 — Express API server
npm run dev          # nodemon, restarts on server-side changes

# Terminal 2 — Vite dev server with HMR
npm run dev:react    # http://0.0.0.0:5173, proxies /api → Express on 5000
```

Open the app on **port 5173** (not 5000) to get instant Hot Module Replacement.
All `/api/*` calls pass through to Express automatically. TypeScript errors
surface in the Vite terminal in real time.

### Option B — Vite build watch (simpler, no HMR)
Use this when you want to keep loading the app through the normal Express
server on port 5000 but still get auto-rebuilds on save:

```
# Terminal 1 — Express server (with pre-built bundle)
npm run dev          # nodemon on 5000

# Terminal 2 — Vite in watch / incremental-build mode
npm run watch:react  # rebuilds public/react/ on every src/react/ save
```

Reload the browser manually after each rebuild. Useful when testing
server-rendered pages or middleware that must run through Express.

### Option C — Storybook dev server (for story/design-system work)
Use this when actively editing stories in `src/react/stories/` and you want
the Design System gallery to update automatically:

```
# Terminal 1 — Express API server (optional, only needed for API-backed stories)
npm run dev          # nodemon on 5000

# Terminal 2 — Storybook dev server with HMR
npm run watch:storybook  # http://0.0.0.0:6006, auto-reloads on story changes
```

Open the gallery on **port 6006**. Every save to `src/react/stories/` or any
component it imports reflects instantly via Hot Module Replacement. This has
no effect on `public/storybook/` (the static build served by Express at
`/storybook/`) — run `npm run build:storybook` to refresh that.

> **Note:** Both `npm start` (`prestart`) and `npm run dev` (`predev`) skip the
> React build when `public/react/main.js` **already exists**, so Replit workflow
> restarts and nodemon restarts are instant for server-side-only changes.
>
> On a **clean checkout** (no bundle present) the two differ:
> - `prestart` runs the full CI-quality build (typecheck + vite build +
>   bundle-size check) so production always starts with a verified bundle.
> - `predev` runs only `vite build` (no typecheck, no bundle-size check) to get
>   the dev server up as fast as possible.
>
> To rebuild React manually during development use:
> - `npm run build:react:dev` — fast Vite-only rebuild (no typecheck/size check)
> - `npm run build:react` — full CI-quality build (typecheck + size check)

## React island conventions
- Material UI is the standard component framework; every mount is wrapped
  in `AppThemeProvider`. See `src/react/README.md` and `src/react/ICONS.md`.
- Mounts are declared in `src/react/main.tsx`. Brand tokens (palette, stage
  colours, radii, typography) live in `src/react/theme.ts` — the canonical
  source of design tokens. CSS custom-property tokens are generated into
  `public/tokens.css` by `scripts/generate-tokens-css.mjs` and loaded by every
  HTML page. All shared app styles live in `public/app-styles.css`.
  (`public/style.css` has been retired and deleted.)
- Icons: named imports from `@mui/icons-material`, no inline `<svg>` in React.

## Privilege checks
- **React components:** use `usePrivilege()` from `src/react/hooks/usePrivilege.ts`.
  Returns `{ privilegeLevel, isAdmin, isManager, isViewer }`.
- **Vanilla-JS files:** call `getPrivilegeLevel()` (defined in `public/core.js`).
  It reads `window.__moHeaderUser || state.user` and defaults to `'member'`.
- **Server route code:** call `getReqPrivilege(req)` (exported from `auth.js`).
  Returns `req.user?.privilege_level || 'member'` — the session-cached value.
  For route-level gating prefer `requireAdmin` / `requirePrivilege(minLevel)` /
  `requireManagerOrAdmin` (also in `auth.js`) — those re-query the database and
  are always up-to-date after a privilege change.
- **Deprecated:** reading `window.__moHeaderUser?.privilege_level`,
  `state.user?.privilege_level`, or `req.user?.privilege_level` directly is
  deprecated. Route all privilege checks through one of the helpers above.
- **Lint guard:** `npm run test:privilege-reads` (script:
  `scripts/check-privilege-reads.mjs`) fails CI if any guarded file contains
  a privilege bypass outside an approved context. Three surfaces are scanned:
  - `.js` files under `public/` — except `core.js`, `react/`, and `storybook/`
    (auto-generated). Flags any `privilege_level` read.
  - `.ts`/`.tsx` files under `src/react/` — except `hooks/usePrivilege.ts` and
    `hooks/usePrivilegeSync.ts` (canonical implementations) and `*.stories.*`
    (Storybook fixtures). TypeScript property-declaration lines
    (`privilege_level?: string`) are skipped automatically.
  - Server-side modules (`server.js`, `design-visits.js`, etc.) — `auth.js`
    is excluded as the canonical owner. Flags direct `req.user?.privilege_level`
    reads (the narrower server-side pattern; DB-query results and data-management
    code are not flagged).
  Lines that legitimately reference another user's field are annotated with a
  trailing `// privilege-read-ok: <reason>` comment (suppresses all surfaces).
  Run standalone or via `npm run test:ci`.

## Authentication
Email + password via `auth.js` (bcrypt + Passport session in PostgreSQL — no
Replit OIDC). Public pages: `/login`, `/set-password`, `/onboarding`. Key API:
- `POST /api/login`, `POST /api/logout`, `GET /api/auth/user`
- `GET /api/set-password/validate?token=…`, `POST /api/set-password`
- `POST /api/onboarding/complete`
- `POST /api/admin/users/:email/resend-set-password`

Protect routes by importing `isAuthenticated` from `./auth`. Admin routes
also use `requireAdmin`. Sessions, users, and `password_set_tokens` tables
are auto-created on boot.

### Approval & onboarding flow
1. Admin approves an access request (or adds a team member directly).
2. Server creates the `users` row with `onboarding_status = 'more_info_required'`
   and emails a one-time set-password link (24h TTL, hash in
   `password_set_tokens`). User shows a **More info required** badge.
3. User sets a password via the link, then signs in.
4. First login is gated to `/onboarding` (gate returns 403 `ONBOARDING_REQUIRED`)
   until profile fields are filled in.
5. Completing onboarding flips status to `active`.

**Pre-auth migration note:** legacy users were backfilled to `active` with
no password set. An admin must click **Resend set-password** on the Team tab
for each of them before they can log in.


## Dev-only admin features
The admin **Dev environment** tab (`#tab-devenv` in `public/admin.html`)
lists every feature suppressed when `NODE_ENV=production`. **Convention:**
whenever you add anything gated on `NODE_ENV !== 'production'` to the admin
panel, add a matching entry to `#tab-devenv` (name, location, why excluded).
A maintainer comment above the panel restates this rule.

## Design visits — QuickBooks estimate sync
`runSubmitSideEffects` in `design-visits.js` is idempotent across
re-submissions. When a `revision_requested` visit is resubmitted and already
has `qb_estimate_id`, the pipeline fetches that estimate and:
- If `TxnStatus` is `Pending` (or absent) with a `SyncToken`, issues a
  **sparse update** (`{ Id, SyncToken, sparse: true, … }`). Visit row keeps
  the same `qb_estimate_id` / `qb_estimate_doc_num`.
- Otherwise (404, voided/deleted/auth error, or `TxnStatus` ≠ Pending) a new
  estimate is created and the prior id is appended to a JSONB
  `design_visits.qb_estimate_history` column with `replaced_at`,
  `replaced_by`, and `reason: prior_estimate_not_updatable`.

## Test suites
All suites accept either `DATABASE_URL_TEST=<disposable>` or, as a fallback,
`PRIVTEST_ALLOW_SHARED_DB=1` (synthetic rows are namespaced behind a
`privtest-` prefix and cleaned up on exit — a mid-run crash can leave stale
fixtures). Each suite writes a markdown report to `test-results/<name>.md`
and exits non-zero on failure. `npm run test:ci` runs all the suites listed
below.

| Script | Covers |
| --- | --- |
| `test:privilege-reads` | Static lint that blocks direct `.privilege_level` reads outside the canonical helpers. Scans all `.js` files under `public/` (excluding `core.js`, the compiled `react/`, and `storybook/` dirs) and all `.ts`/`.tsx` files under `src/react/` (excluding `usePrivilege.ts`, `usePrivilegeSync.ts`, and `*.stories.*` fixtures). A line is a violation when it contains `privilege_level`, is not a pure comment, lacks a `// privilege-read-ok: <reason>` suppression, and — for TypeScript — is not a bare property-type declaration. |
| `test:privileges` | 5-actor × 123-route capability matrix, the adversarial probe checklist, and a headless `/login → / → /admin` UI smoke. Required Turnstile-tampering probes fail closed unless opted in via `PRIVTEST_USE_TURNSTILE_SECRET_KEY=1`. **In `test:ci` this runs via `test:privileges:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:substatus-hubspot-label-format` | Integration test for the `syncLeadSubstatusesToHubSpot()` label format (task #1748 regression guard). Intercepts `PATCH /crm/v3/properties/contacts/hw_lead_substatus` via a local mock HubSpot server and asserts: **(S0)** startup sync PATCH is issued on boot; **(S1)** normal rows carry `"{ls_label} → {sub_label}"` labels; **(S2)** `__NULL__` sentinel rows emit just the sub-status label with no null prefix; **(S3)** the same sub-status name under two different lead statuses produces two distinct labels; **(S4)** value field format (`KEY__SUBKEY` for normal rows, plain `substatus_key` for `__NULL__`); **(S5)** `POST /api/admin/lead-substatuses` triggers a fresh sync whose PATCH carries the correct arrow-prefixed label. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:substatus-hubspot-label-format:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:lead-status-sync` | Customers-page filter dropdown updates via BroadcastChannel + `visibilitychange` after admin renames, count-suffix format, skeleton load/clear cycle, and sub-status chip live-rename via BC and visibilitychange. **In `test:ci` this runs via `test:lead-status-sync:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:lead-status-sync-customer-detail` | Per-contact tracker on customer-detail (rail order, current marker, sub-status panel) re-renders via BC + `visibilitychange` without full reload. Also covers probes **(H)** and **(I)**: when the contact already has `hw_lead_substatus` pre-set, the pill's primary text (sub-status label) updates in place after a sub-status rename fired via BroadcastChannel (H) and via a hidden→visible `visibilitychange` sequence (I) — render token preserved, no full page reload. **In `test:ci` this runs via `test:lead-status-sync-customer-detail:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:lead-status-sync-customer-detail-viewer` | **Isolated** viewer-role pill check: boots a fresh browser (no prior admin pages or request-interception), navigates to `/customers/:id` as a viewer, and asserts the `.lead-status-badge` pill lacks `lsb-clickable`, has no onclick, and clicking it never opens `#card-picker-popup`. Regression guard for the `canEditPipeline()` gate in `_renderWorkflowHeaderImpl`. **In `test:ci` this runs via `test:lead-status-sync-customer-detail-viewer:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:lead-status-sync-customer-detail-editable` | **Isolated** manager/admin-role pill check: boots a fresh browser, runs each role in its own incognito context, and asserts the `.lead-status-badge` pill carries `lsb-clickable`, has an onclick handler, and clicking it opens `#card-picker-popup`. Regression guard for the `canEditPrivilege()` gate — catches any change that accidentally strips the editable path from manager or admin users. **In `test:ci` this runs via `test:lead-status-sync-customer-detail-editable:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:design-visit-list` | `GET /api/design-visits?contactId=…` scoping + customer-detail "Design visits" rail (status pills, totals, admin Request-revision/Delete actions, member gating). **In `test:ci` this runs via `test:design-visit-list:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:design-visit-qb-resubmit` | QuickBooks sparse-update vs create-new fallback branches in `runSubmitSideEffects` against a local mock QB server (uses test-only `QB_API_BASE_OVERRIDE` in `design-visits.js`). **In `test:ci` this runs via `test:design-visit-qb-resubmit:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:design-visit-submitter-name` | Regression guard for "Designer/Submitted by unknown" — captures HubSpot note + team email + customer email + sign-off approve/revision via test-only `HUBSPOT_API_BASE_OVERRIDE` and `MAIL_TRANSPORT_FILE_OVERRIDE`. **In `test:ci` this runs via `test:design-visit-submitter-name:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:design-visit-hubspot-retry` | HubSpot 429 retry and permanent-failure resilience for the design-visit and localdata pipelines. Covers: note-creation retry (DV1), lead-status PATCH retry (DV2), note permanent failure (DV3), lead-status permanent failure (DV4), localdata PATCH retry (LD), localdata permanent failure (LD2), room-assignment fitter PATCH retry (RA1), fitter PATCH permanent failure with `syncFailed: true` body (RA2/RA2.4), and fitter PATCH happy path with no `syncFailed` flag (RA3). Uses a local mock HubSpot server via `HUBSPOT_API_BASE_OVERRIDE`. **In `test:ci` this runs via `test:design-visit-hubspot-retry:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:card-action-handlers` | Admin handler create → cross-tab BC refresh, modal dispatch for bound `.eq-card-action` strips, and substatus-binding-wins-over-label precedence. Probe **(D)** covers the conflict-fix flow end-to-end: the ⚠ Fix button appears for a duplicate-bound slot (D.banner-1/2/3: conflict banner appears, its Fix button opens the resolver modal, banner disappears after the duplicate is removed), and D.modal-1 asserts the resolver is a `MuiDialog-root` whose `DialogTitle` reads "Fix conflicting handlers" (regression guard for task #1673 React Dialog migration). Probe **(Q)** verifies `ensureSubstatusHandlerBindings` auto-binds on boot (task #1818). Probe **(R)** verifies the `default_handler_type` column drives the bound handler type and that `SUBSTATUS_HANDLER_MAP` still wins when the column is empty (task #1825). Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:card-action-handlers:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:duplicate-phone-warnings` | Admin "Add team member" form and Trades modal duplicate-phone alerts (notice copy, disabled submit, jump-to-record link); Edit-mode company-phone closure check (clear duplicate → notice hides → submit re-enables). **In `test:ci` this runs via `test:duplicate-phone-warnings:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:lead-status-counts-rate-limit` | Single-flight + stale-cache + retry behaviour of `/api/contacts-lead-status-counts` against a mock HubSpot HTTP server. **In `test:ci` this runs via `test:lead-status-counts-rate-limit:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:phone-directory` | Auth gating (member 403 / manager+admin 200) and payload coverage for the staff and trades sections of `GET /api/admin/phone-directory`: `allowed_emails`-only vs users-joined team rows, `kind:'company'` vs `kind:'contact'` trades entries, correct field/label/phone/email/userId/companyName/contactName values, and customers always an array. **In `test:ci` this runs via `test:phone-directory:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:phone-directory-customers` | Mock-HubSpot coverage for the customers section of `GET /api/admin/phone-directory`: phone/mobilephone field mapping, two-entry split for contacts with both fields, name-fallback to email, and zero-entry guarantee for contacts with no phone data. **In `test:ci` this runs via `test:phone-directory-customers:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:bundle-sizes` | Post-build gzip-size check for every `.js` file under `public/react/`. Exits non-zero if any always-loaded chunk exceeds its threshold. Also appends a size snapshot to `test-results/bundle-sizes-history.jsonl` for trend tracking and emits a non-fatal **spike warning** when the total gzip size grows by more than `SPIKE_PCT` (5 %) relative to the immediately preceding history entry (a large dependency may have been added) — **this file must be cached/uploaded as a CI artefact** (e.g. `actions/cache@v4` + `actions/upload-artifact@v4`) so trend data and spike detection are preserved across runs; see the header comment in `scripts/check-bundle-sizes.mjs` for example steps. |
| `test:bundle-size-trend` | Unit tests for the bundle-size trend-regression warning logic in `scripts/bundle-size-trend.mjs`. Covers: growth above threshold warns, below threshold is silent, exactly at threshold is silent (strict >), single-entry window is silent, empty window is silent, zero oldest value is silent, window-slicing correctness, shrinkage is silent, and multi-entry window comparison. No build step required. |
| `test:bundle-spike-warning` | Unit tests for the per-build spike-detection warning logic (`detectSpikeWarning` in `scripts/bundle-size-trend.mjs`). Covers: growth above SPIKE_PCT warns, below threshold is silent, exactly at threshold is silent (strict >), single entry is silent, empty history is silent, zero previous value is silent (avoids division by zero), shrinkage is silent, only the last two entries are compared. No build step required. |
| `test:chunk-cache-headers` | HTTP HEAD probes against a running Express server: asserts that `/react/chunks/<hashed>.js` and `/react/assets/<hashed>.<ext>` are served with `Cache-Control: … immutable`, and that `/react/main.js` is NOT. A synthetic hashed CSS file is written to `public/react/assets/` when no real build output exists there so the ASSETS probe always runs and is never silently skipped. Guards the task-#956 static-middleware configuration against accidental revert. **In `test:ci` this runs via `test:chunk-cache-headers:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:admin-tab-skeletons-new` | Puppeteer request-interception suite for the Settings, Card Actions, and Action Handlers admin tabs. Holds each tab's lazy JS chunk (`SettingsPage-*.js`, `CardActionsPage-*.js`, `ActionHandlersPage-*.js`) while React is mid-load, confirms the Suspense-fallback skeleton (`AdminSettingsPageSkeleton` / `CardActionsPageSkeleton` / `ActionHandlersPageSkeleton`) appears within the panel, then releases the chunk and asserts the skeleton is gone and the real component's static DOM is present. **In `test:ci` this runs via `test:admin-tab-skeletons-new:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:admin-tab-skeletons-suspense` | Puppeteer request-interception suite for the Permissions, Requests, and Audit Log admin tabs — Suspense fallback layer. Holds each tab's lazy JS chunk (`AdminPermissionsPage-*.js`, `AdminRequestsPage-*.js`, `AdminAuditLogPage-*.js`) while React is mid-load, confirms the Suspense-fallback skeleton (`AdminPermissionsPageSkeleton` / `AdminRequestsPageSkeleton` / `AdminAuditLogPageSkeleton`) appears within the panel, then releases the chunk and asserts the skeleton is gone and the real component's static DOM anchor (`#roles-list` / `#requests-content` / `#audit-feed`) is present. Complements `test:admin-tab-skeletons` which tests the in-component data skeletons (layer 2) for the same tabs. **In `test:ci` this runs via `test:admin-tab-skeletons-suspense:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:calendar-empty-state` | CalendarSection empty-state branch: `connected: true` + zero events → "Upcoming" heading + "No upcoming events" visible; `connected: false` → section absent. Intercepts `/api/calendar/upcoming` via Puppeteer; stubs all other home-page API calls so the test runs without Google OAuth credentials. **In `test:ci` this runs via `test:calendar-empty-state:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:customer-card-action-strip` | Puppeteer e2e for the action-label strip and QB invoice badge on customer cards in `CustomersPage.tsx`. All customers-page API calls are stubbed via `evaluateOnNewDocument` fetch interception — no HubSpot token required. Probes: **(A)** card matching a bound handler shows a coloured action strip with the correct `action_name` label; **(B)** card with no handler shows no strip; **(C)** `start_design_visit` handler + in-progress draft shows "Continue designing"; **(D)** clicking the strip does not navigate away from `/customers`; **(E)** QB invoice badge click opens `InvoiceDetailDrawer` (`data-testid="invoice-detail-drawer"` present and not aria-hidden) — regression guard for the accidental deletion of `handleOpenInvoice` (task #1920). **In `test:ci` this runs via `test:customer-card-action-strip:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:workflow-map` | Puppeteer e2e for the Workflow Map section on the Card Actions admin tab. Stubs the four card-actions APIs so the ReactFlow chart renders the fixed stage nodes without HubSpot data. Probes: **(A)** map starts expanded when `localStorage` has no saved state; **(B)** clicking the header toggles collapse and persists the state across reloads (`mo:card-actions:map-collapsed`); **(C)** clicking a stage node opens the MUI Drawer; **(D)** Drawer shows the correct label and key; **(E)** close button dismisses the Drawer. **In `test:ci` this runs via `test:workflow-map:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:icon-lint` | Static two-pass lint over every `.tsx`/`.ts` file under `src/react/` (excluding `.d.ts` and Storybook files). Pass 1: every `*Icon` identifier used as a JSX element or bare value must be imported from `@mui/icons-material` in that file. Pass 2: every `@mui/icons-material` import must be used at least once. No server, no DB, no Puppeteer — reads source files directly. |
| `test:mount-ids` | Four-pass static check (`scripts/check-mount-id-conflicts.mjs`). Pass 1: each React mount id in the `MOUNTS` table of `src/react/main.tsx` appears in at most one `public/*.html` file — a duplicate causes `mountKnown()` to render the wrong island on the wrong page. Pass 2: every `public/*.html` that loads `/react/main.js` must also declare at least one element id from the MOUNTS table — a page that loads the bundle with no mount element is silently loading dead JS. Pages where mounts are injected dynamically (e.g. by `chrome.js`) may suppress Pass 2 with `<!-- main-js-no-mount-ok: reason -->`. Pass 3: every id in `BOOTSTRAP_ONLY_IDS` (`src/react/lib/publicIslands.ts`) must appear in at least one `public/*.html` file — error/restricted pages carry chrome mount ids that would satisfy Pass 2 even if their own container id is misspelled or removed, so Pass 3 checks each entry individually. Pass 4 (tasks #1947, #1955): **every** `BOOTSTRAP_ONLY_IDS` entry must carry a `// public/<file>.html — <description>` annotation (Pass 4a, CI failure if missing), and each annotated entry is verified against *that specific file* (Pass 4b) — catches the gap where a typo in the correct file goes undetected because the original id still survives in a different HTML file. Pass 4b uses `existsSync` before reading the annotated file, so a filename typo in the annotation produces a clean "annotation file not found" CI failure naming the id and path, rather than a raw Node.js error. **Convention:** when adding a new error/restricted page, you must (1) add its id to `BOOTSTRAP_ONLY_IDS` with the required `// public/<file>.html — description` comment on the same line naming an existing file; (2) ensure a matching MOUNTS entry exists in `src/react/main.tsx`. Omitting the annotation is a CI failure. No server, no DB, no Puppeteer — reads source files directly. |
| `test:public-island-bootstrap` | Five-invariant static check (`scripts/check-public-island-bootstrap.mjs`) guarding the single-source-of-truth contract for public islands (task #1909, extended in tasks #1919, #1965, #1969). Both `PUBLIC_ISLAND_IDS` and `BOOTSTRAP_ONLY_IDS` are now canonical in `src/react/lib/publicIslands.ts`; `CONN_TOAST_EXCLUDED` (main.tsx) and `BOOTSTRAP_EXCLUDED` (AppBootstrapContext.tsx) are derived from them at runtime. **Check A** (every `// public-island`-annotated MOUNTS id ∈ `PUBLIC_ISLAND_IDS`): catches annotation-without-set-entry. **Check B** (`PUBLIC_ISLAND_IDS` ⊆ annotated MOUNTS ids): catches set-entry-without-annotation or stale entry after MOUNTS removal. **Check C** (`BOOTSTRAP_ONLY_IDS` ∩ `PUBLIC_ISLAND_IDS` = ∅): error/restricted pages must never appear in `PUBLIC_ISLAND_IDS`. **Check D** (every `BOOTSTRAP_ONLY_IDS` id ∈ all MOUNTS ids): catches typos in `BOOTSTRAP_ONLY_IDS` or stale entries left after an error page's MOUNTS entry is removed. **Check E** (every `BOOTSTRAP_ONLY_IDS` entry carries a `// public/<file>.html` annotation): mirrors the Pass 4a rule in `test:mount-ids` — catches a new error/restricted page added without the required file annotation. **Check E (file existence)** (every annotated `public/<file>.html` path exists on disk): mirrors Pass 4b in `test:mount-ids` — catches a filename typo in the annotation that satisfies the syntax check but names a file that does not exist. To add a new public island: (1) add its id to `PUBLIC_ISLAND_IDS`; (2) annotate its MOUNTS entry with `// public-island`. To add a new error/restricted page: (1) add its id to `BOOTSTRAP_ONLY_IDS` with the required `// public/<file>.html — <description>` annotation naming an existing file; (2) ensure a matching MOUNTS entry exists in `src/react/main.tsx`. No server, no DB, no Puppeteer — reads source files directly. |
| `test:inline-styles` | Static scan (`scripts/check-inline-styles.mjs`) over every `public/*.html` file for `style="…"` attributes. Fails if any element carries an inline style without an explicit `<!-- inline-style-ok: reason -->` suppression comment on the same line. Lines inside `<script>…</script>` blocks are skipped (JS string literals, not HTML attributes). Enforces the stylesheet-first convention and catches regressions like the survey-board inline styles. No server, no DB, no Puppeteer — reads source files directly. |
| `test:story-hex-colors` | Static scan (`scripts/check-story-hex-colors.mjs`) over every `*.stories.tsx` file under `src/react/` for raw hex colour values (`#[0-9a-fA-F]{3,6}`) in style-related properties: `bgcolor`, `background`, `color`, `borderColor`, and `fill`. Flags any line containing both a hex colour and a style prop name; lines carrying a `// story-hex-ok: <reason>` suppression comment are exempt (used for Swatch stories that display colour values as content). Prevents task-#1949 theme-token replacements from silently regressing as new stories are written. No server, no DB, no Puppeteer — reads source files directly. |
| `test:component-hex-colors` | Static scan (`scripts/check-component-hex-colors.mjs`) over every `*.ts` / `*.tsx` file under `src/react/` — excluding `*.stories.*`, `*.d.ts`, and `theme.ts` (the canonical token source) — for raw hex colour values (`#[0-9a-fA-F]{3,6}`) in style-related properties: `bgcolor`, `background`, `color`, `borderColor`, and `fill`. Complements `test:story-hex-colors` to give complete design-token coverage across the full React codebase. Pre-existing violations carry a `// hex-color-ok: pre-existing raw hex` suppression and are tracked for future token migration. New code must use semantic theme tokens (STAGE_COLORS / STATUS_COLORS from `theme.ts`, MUI palette strings, or CSS custom properties). Suppress genuinely necessary literals (third-party brand colours, SVG fill attributes) with `// hex-color-ok: <reason>`. No server, no DB, no Puppeteer — reads source files directly. |
| `test:workflow-js-no-dups` | Static guard (`scripts/check-workflow-js-duplicates.mjs`) that none of the 11 picker-cluster functions moved to `workflow-core.js` in task #1109 (`closeCardPicker`, `openLeadStatusPicker`, `openCardStagePicker`, `openCardSubstagePicker`, `quickSetLeadStatus`, `_quickSetLeadStatusWithSub`, `_fetchLocaldataForCard`, `_lastCompletedSubstageLabel`, `_saveCardRoomMutation`, `_substatusesForStatus`, `_currentSubstatusFor`) appear as top-level function declarations in `public/workflow.js`. Prevents silent re-introduction of duplicate definitions. No server, no DB, no Puppeteer — reads source files directly. |
| `test:nav-key-sync` | Static lint that asserts the two server-side nav-key allow-lists and the `BottomNav.tsx` NAV array are not drifting apart. Three invariants: **(1)** `VALID_NAV_KEYS_SERVER` in `auth.js` equals `VALID_NAV_KEYS` in `server.js` — both sets validate key submissions on the server and must stay identical; **(2)** every key in `BottomNav.tsx`'s `NAV` array is present in `VALID_NAV_KEYS_SERVER` (auth.js) — otherwise `PATCH /api/admin/nav-role-config` silently rejects it; **(3)** every key in `BottomNav.tsx`'s `NAV` array is present in `VALID_NAV_KEYS` (server.js) — otherwise the user-prefs PATCH route rejects it. The check is NAV ⊆ server sets (not strict equality) because the server sets intentionally include extra keys beyond the NAV array. No server, no DB, no Puppeteer — reads source files directly. |
| `test:no-config-handler-types` | Static lint (`scripts/check-no-config-handler-types.mjs`). Parses the `NO_CONFIG_HANDLER_TYPES` Set from `ActionHandlersPage.tsx`, then asserts: (1) none of those types appear as explicit `case '…':` labels in the `configBlockForType` switch in `ActionHandlerConfigBlocks.stories.tsx`; (2) none appear as explicit `handlerType === '…'` comparisons in `ActionHandlersPage.tsx` (the render-block boolean flags and `buildPayload` if/else chain). Catches contradictions where a type is simultaneously declared to have no config and given a dedicated config block. No server, no DB, no Puppeteer — reads source files directly. |
| `test:workflow-map-ordering` | Static lint (`scripts/check-workflow-map-ordering.mjs`). Guards the Workflow Map stage-ordering contract introduced in task #1868. Four invariants: **(1)** `CARD_ACTION_STAGES` in `WorkflowMapChart.tsx` contains only keys present in `workflow.json` stages; **(2)** `CARD_ACTION_STAGES` keys appear in the same relative order as in `workflow.json`; **(3)** `CardActionsPage.tsx`'s `allStages` builder uses `Object.keys(wfStages)` (not a hardcoded list or sort call) to preserve JSON insertion order; **(4)** `buildFlowGraph` in `WorkflowMapChart.tsx` does not call `.sort()` or `.reverse()` on the supplied `allStages` array. No server, no DB, no Puppeteer — reads source files directly. |
| `test:status-key-fields` | Static lint (`scripts/check-status-key-fields.mjs`). Cross-checks `KNOWN_STATUS_KEY_FIELDS` in `HandlerConfigBlocks.tsx` against every exported `*ConfigProps` interface in the same file. The convention: if a props interface declares `<field>Invalid?: boolean`, that boolean is the staleness flag for the status-key string prop `<field>`, which must be registered in `KNOWN_STATUS_KEY_FIELDS` so the JSON fallback editor in `ActionHandlersPage.tsx` can detect stale references uniformly. Fails when a new handler config block adds an `*Invalid` companion prop but forgets to add the corresponding field to the shared list. No server, no DB, no Puppeteer — reads source files directly. |
| `test:info-card-review-resend` | Regression guard for the Review vs Resend-link button branching logic in `SubmissionCard` (`CustomerInfoSubmissionsRail`). Three probes via Puppeteer with request-intercepted stubs: **(RR-A)** submitted submission (`submitted_at` non-null) — "Review" button visible, `[data-testid="resend-link-btn"]` absent; **(RR-B)** pending submission (`submitted_at` null, `expires_at` in the future, admin role) — `[data-testid="resend-link-btn"]` visible, "Review" button absent; **(RR-C)** clicking "Review" in the submitted card expands the Collapse detail panel and "Submitted" date text appears. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:info-card-review-resend:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:masked-email-backfill` | Regression guard for `backfillMaskedEmails()` in `customer-info.js` (task #1915). Seeds four `customer_info_submissions` rows and calls the function directly (no server boot): **(A)** `masked_email IS NULL` → filled in with new format (e.g. `har***son@g**.com`); **(B)** old-format `masked_email` (`h***@***.com`, three literal asterisks before the dot) → rewritten to new format; **(C)** already-correct new-format `masked_email` → unchanged; **(D)** `submitted_at IS NOT NULL` row → not touched even when `masked_email` is NULL (backfill WHERE clause requires `submitted_at IS NULL`). Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:masked-email-backfill:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:customer-info-generate-link-reuse` | Integration test verifying the `POST /api/customer-info/by-contact/:contactId/generate-link` resend path reuses the existing active row rather than inserting a new one (task #1956 / #1961). Boots a disposable Express server with a mock HubSpot server and exercises four probes: **(A)** first call for a fresh contact inserts exactly one active row (`isResend=false`); **(B)** second call for the same contact updates the row in place — row count stays at 1, the same row `id` is preserved, `isResend=true`; **(C)** token rotation — the raw token returned by the second call differs from the first call's token and the DB row now carries the new `token_hash`; **(D)** duplicate cleanup — when three active rows exist for the same contact (simulating a data-migration or race condition), a single generate-link call expires two of them, leaving exactly one non-expired row. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:customer-info-generate-link-reuse:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:active-link-expires` | Regression guard for the "expire stale links on send" logic (task #1968 / #1971). Boots a disposable Express server with a mock HubSpot server and calls `POST /api/card-actions/upload-photos-and-info` twice for the same contact: **(A)** first send — exactly 1 active row (`expires_at > NOW()`, `submitted_at IS NULL`) exists; **(B)** second send — still exactly 1 active row (the route must have expired the prior one); **(C)** stale row — the original row's `expires_at` is confirmed to be in the past; **(D)** total rows — exactly 2 `customer_info_submissions` rows exist for the contact. Uses `HUBSPOT_API_BASE_OVERRIDE` and `MAIL_TRANSPORT_FILE_OVERRIDE` so no real HubSpot token or SMTP credentials are needed. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:active-link-expires:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:active-link-warning-ordering` | Regression guard for the "confirm before expiring an active link" ordering in `UploadPhotosModal` (task #1970). Before task #1970, the modal called `POST generate-link` immediately on open, which expired the customer's existing active link without warning. The fix added a confirmation step: when `GET link-status` returns `hasActiveLink:true` the modal pauses in a `'confirming'` phase and only calls `generate-link` AFTER the staff member clicks "Send new link anyway". Boots a disposable test server; uses Puppeteer Node-side request interception to stub `link-status` and count `generate-link` calls; opens the modal via the global `window.openCardActionModal` bridge so no handler seeding is required. Three probes: **(A)** warning-visible — after `link-status` returns `hasActiveLink:true` the dialog title reads "Active link exists" and an MUI Alert with warning severity is present; **(B)** no-premature-call — `generate-link` has NOT been called at that point (the old active link has not been expired yet); **(C)** confirm-triggers — clicking `[data-testid="cah-confirm-resend"]` ("Send new link anyway") results in exactly one POST to `generate-link`. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:active-link-warning-ordering:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:customer-info-resend` | Integration test for the `POST /api/customer-info/:token/resend-expired` self-serve resend flow (task #1911). Boots a disposable Express server against an isolated test database and exercises six probes: **(A)** expired token — `GET /api/customer-info/:token` returns 410 with `status:"expired"` and a `maskedEmail`; **(B)** successful resend — creates a new `customer_info_submissions` row and logs the event in `customer_info_resend_log`; **(C)** per-token rate limit — the 4th resend attempt within a 24-hour window returns 429 (prior attempts pre-seeded via direct DB insert to avoid consuming the IP-rate-limit budget); **(D)** submitted token — returns 400, no new link issued; **(E)** non-existent token — returns 404; **(F)** new-link validity — the fresh raw token extracted from the `MAIL_TRANSPORT_FILE_OVERRIDE` invitation email is accepted by `GET /api/customer-info/:freshToken` with a 200 and full form data. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:customer-info-resend:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:upload-photos-copyable-link` | Regression guard for the "Copy & close" button in `UploadPhotosModal` (task #1929). Opens `CustomerDetailPage` via Puppeteer with request-intercepted stubs and opens the modal programmatically via the global `window.openCardActionModal` bridge. POST `/api/customer-info/by-contact/:id/generate-link` is stubbed to respond immediately with a synthetic link; `navigator.clipboard.writeText` is overridden to resolve in headless Chrome. Three probes: **(CC-A)** `[data-testid="cah-copy-close"]` appears once the link is ready; **(CC-A2)** the button is enabled (not disabled); **(CC-B)** clicking the button causes `MuiDialog-root` to leave the DOM. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:upload-photos-copyable-link:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:photo-storage-errors` | Regression guard for storage error sanitisation across upload, delete, and download paths. Two server phases — phase 1 uses a constructor-throws stub (all routes), phase 2 uses an `uploadFromBytes → { ok: false }` / `delete → { ok: false }` / `downloadAsBytes → { ok: false }` stub: **(STO-1)** `POST /api/customer-info/:token/photos` with constructor-throws stub → HTTP 500, friendly "temporarily unavailable" copy, no raw SDK internals leaked; **(STO-2)** `POST /api/design-visits/uploads` with constructor-throws stub → HTTP 503, same guarantees; **(STO-5)** `DELETE /api/design-visits/uploads/:key` with constructor-throws stub → HTTP 500, no SDK leak; **(STO-6)** `GET /api/design-visit-images/:key` (public HMAC-signed download) with constructor-throws stub → HTTP 500, no SDK leak; **(STO-7)** `GET /api/customer-info-photos/:key` (authenticated HMAC-signed) with constructor-throws stub → HTTP 500, no SDK leak; **(STO-3)** customer-info upload with `ok:false` SDK response → HTTP 500, friendly copy, no leak; **(STO-4)** design-visits upload with `ok:false` SDK response → HTTP 503, friendly copy, no leak; **(STO-8)** `DELETE /api/design-visits/uploads/:key` with delete `ok:false` → HTTP 500, no SDK leak; **(STO-9)** `GET /api/design-visit-images/:key` with downloadAsBytes `ok:false` → HTTP 500, no SDK leak. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:photo-storage-errors:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:turnstile-signout` | Regression guard for the bfcache restore fix and the `?signed_out=1` redirect from command-palette.js. Verifies: POST `/api/logout` redirects to `/login?signed_out=1`; `window._cpRun['sign-out']` source contains the correct redirect pattern; the "You've been signed out." banner is visible on `/login?signed_out=1`; the `#turnstile-login` iframe renders (via Turnstile stub + request-intercepted config); and `pageshow(persisted:true)` re-renders the widget. **In `test:ci` this runs via `test:turnstile-signout:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:invoice-panel-hidden` | CSS visibility regression guard for `#inv-panel`. Visits home, trades, projects, survey, invoices, and calendar pages as an admin and asserts `visibility: hidden` via computed style on each page before the panel is opened. On the invoices page, also adds the `inv-panel-open` class and confirms visibility becomes `visible`, then removes it and confirms visibility returns to `hidden` after the transition delay. Guards against MUI emotion, Tailwind, or other stylesheet overrides of the `.inv-panel` hiding rules in `app-styles.css`. **In `test:ci` this runs via `test:invoice-panel-hidden:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:projects-top-spacing` | Layout regression guard for the Projects page top spacing (task #1879). Logs in as admin, navigates to `/projects`, waits for stage-filter `.MuiTab-root` elements to render, then asserts: **(PTS-A)** at least 16 px of vertical space exists between the bottom of `#app-header-mount` and the top of the first stage-filter tab (equivalent to MUI `pt: 2`); **(PTS-B)** an `h1` element with text "Projects" is visible in the viewport above the first tab. Guards against regressions where the fixed-overlay layout causes the heading to be hidden or the filter tabs to collide with the navbar. **In `test:ci` this runs via `test:projects-top-spacing:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:onboarding-conflicts` | End-to-end coverage for the onboarding conflict detection and admin resolution flow (task #1108). API probes: `POST /api/onboarding/complete` with values that differ from admin pre-fills → 200 and `pending_profile_updates` stored on `allowed_emails`; `POST /api/admin/users/:id/resolve-profile-conflicts` → 200 and `pending_profile_updates` cleared. UI probes: admin Team tab shows the `DifferenceIcon` badge on the conflict user's row; Edit dialog shows the "Onboarding discrepancies" Alert with the correct admin and user values; after resolution save the badge is gone from the row. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:onboarding-conflicts:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:open-leads-stale-visibility` | Tab-visibility pause for the open-leads stale badge. **(F1)** Hidden → stale response deferred: intercepts `/api/open-leads` returning `X-Cache-Status: stale` while `document.hidden=true`; badge must NOT appear immediately but MUST appear after a synthetic `visibilitychange → visible`. **(F2)** Hidden → fresh response, badge persists: with the badge visible, sets `document.hidden=true` and uses `window.__setTestPendingOpenLeadsStale(false)` to simulate a fresh response arriving while hidden; badge must STILL show and only disappear after `visibilitychange → visible`. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:open-leads-stale-visibility:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:ideas` | Puppeteer + API coverage for the Ideas & Feedback page (task #1094). API probes: `POST /api/ideas` → 201 with correct fields; `GET /api/ideas` most-recent order; `GET /api/ideas/:id/comments` empty array; `POST /api/ideas/:id/comments` → 201; `DELETE /api/ideas/:id` → 403 for member / 404 admin fake-id; `DELETE /api/ideas/:id/comments/:cid` → 403 member / 200 admin. UI probes: posting an idea prepends a card; comment chip lazy-fetches comments; replying adds comment inline; admin sees `[data-testid="delete-idea-btn"]` buttons and confirm dialog; member sees no delete buttons. Uses `PRIVTEST_ALLOW_SHARED_DB=1` or `DATABASE_URL_TEST`. **In `test:ci` this runs via `test:ideas:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:keyboard-shortcuts` | Headless Chromium smoke for `window.getShortcut()` in `public/chrome.js`. Exercises four scenarios: (1) `userAgentData` path with `platform="macOS"` → ⌘K, (2) `userAgentData` path with `platform="Windows"` → Ctrl K, (3) legacy `navigator.platform` fallback `"MacIntel"` → ⌘K, (4) legacy fallback `"Win32"` → Ctrl K. No server or database required — reads the production source directly and evaluates it in a data-URL page. |
| `test:conflict-digest-settings` | Puppeteer integration test for the Conflict digest panel in Admin Settings. Three scenarios — **(CD-1)** intercepts `GET /api/admin/conflict-digest-settings` → `{ smtpConfigured: false, lastSentAt: null }`: asserts "Conflict digest" heading visible, "Last sent:" label present, "Send now" button disabled, and the SMTP-missing Alert shown; **(CD-2)** same endpoint → `{ smtpConfigured: true, lastSentAt: null }`: asserts button enabled, no SMTP warning, "Last sent" reads "Never"; **(CD-3)** also intercepts `POST /api/admin/conflict-digest/send-now` → `{ sent: true, lastSentAt: … }`: clicks "Send now" and confirms the "Last sent" span updates to the formatted timestamp. Each scenario uses a fresh Puppeteer page with its own `evaluateOnNewDocument` fetch override. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:conflict-digest-settings:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:settings-tab-load` | Regression guard for the race condition fixed in task #1110 (`waitForElement` replacing early-return null checks). Logs in as admin, intercepts `GET /api/hubspot/status` → `{ connected: true }` and `GET /api/admin/lead-statuses` → one-row fixture, then opens the Settings tab via `switchTab`. Asserts (ST-A) `#hubspot-status-badge` text becomes `"Connected"` (not `"Checking…"`) and (ST-B) `#lead-statuses-table-wrap` contains a `<table>` element. **In `test:ci` this runs via `test:settings-tab-load:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:room-stale-banner-visibility` | Tab-visibility pause for the room-assignments stale banner: F1 confirms `#room-stale-banner` is absent when `/api/localdata/all` returns `X-Cache-Status: stale` while `document.hidden=true`, then appears after `visibilitychange → visible`; F2 confirms a pending clear (via `__setTestPendingRoomStale(false)`) is also deferred until visibility restores. **In `test:ci` this runs via `test:room-stale-banner-visibility:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:storybook-smoke` | Puppeteer smoke test for every Storybook story. Builds Storybook to a temp directory, spins up a lightweight static HTTP server, then loads each story via `iframe.html?id=<storyId>&viewMode=story` in headless Chromium (up to 4 concurrent pages). A story is considered broken when: **(A)** an uncaught exception fires in the page; **(B)** a Storybook error-boundary overlay (`#error-message`, `.sb-errordisplay`, `[data-error-boundary]`) is visible in the DOM; **(C)** `#storybook-root` is still empty after the render timeout; or **(D)** React's error-boundary rethrow message ("The above error …") appears in `console.error`. Catches the auth-fetch hang class of bug (task #1535) via check **(C)**: any story that hangs waiting for an unauthenticated API call times out and its root remains empty. No database required. |
| `test:room-assignments-outage` | Integration test for `GET /api/localdata/all` resilience against a prolonged HubSpot outage. **(E) Prolonged outage**: warms the contacts cache with fixture contacts carrying `measure_once_rooms`, busts the fresh cache, ages the snapshot past `ALL_CONTACTS_STALE_MAX_MS_OVERRIDE`, then confirms `GET /api/localdata/all` still returns 200 with a non-empty room map (`X-Cache-Status: stale`) — the no-cap fallback in `server.js`. **(F) Contrast**: under the same conditions, `GET /api/contacts-all` returns 502 (`HUBSPOT_ERROR`), confirming the stale cap is enforced for the main customer list. **(G) Recovery**: after HubSpot comes back online, `GET /api/localdata/all` returns 200 with `X-Cache-Status: fresh` and the mock HubSpot is actually called, confirming the cache is refreshed rather than short-circuited to the stale snapshot. **In `test:ci` this runs via `test:room-assignments-outage:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:bottom-nav` | End-to-end coverage for the "More" drawer in the bottom navigation bar. Verifies role-specific primary-tab sets ([M-BAR] member: Home/Calendar/Trades/More; [MG-BAR] manager: Home/Sales/Projects/More), drawer open/close behaviour ([M-DRAW]/[MG-DRAW]), More-tab selection when an overflow route is active ([M-ACT]/[MG-ACT]), tap-to-navigate closes drawer and updates pathname ([M-NAV]/[MG-NAV]), and backdrop click closes drawer ([CLO]). Requires `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:bottom-nav:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:nav-active-tab` | Regression guard for the `matchPath` prefix-route logic in `BottomNav.tsx`. Verifies: (CUST-LIST) `/customers` → `#bnav-customers` has `Mui-selected` and More tab is active; (CUST-DETAIL) `/customers/:id` full page load → `#bnav-customers` still selected; (CUST-PS) `history.pushState` from `/customers` to `/customers/:id` → tab stays selected, then deselects when pushed to `/`; (TRADES-DETAIL) `/trades/:id` → `#bnav-trades` (a primary bar item) has `Mui-selected` and More tab is NOT active; (TRADES-PS) pushState `/trades` → `/trades/:id` → `#bnav-trades` stays selected. **In `test:ci` this runs via `test:nav-active-tab:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:nav-customise` | End-to-end coverage for the nav tab customisation dialog: API auth-gating (`GET`/`PATCH /api/users/me/prefs`), dialog opens from the More drawer for managers and is absent for members, selecting and saving 3 tabs updates the bar immediately, preference persists across a page reload, fallback to role defaults when `nav_primary_keys` is absent, and Cancel discards unsaved changes. **In `test:ci` this runs via `test:nav-customise:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:nav-role-config` | End-to-end coverage for the admin role-based nav config API (task #968): `GET /api/nav-role-config` returns `__default__` keys when user has no `job_role`; returns role-specific keys when a matching `nav_role_configs` row exists; requires auth. `PATCH /api/admin/nav-role-config/:roleName` succeeds for admin, returns 403 for member/manager, validates body (wrong length / duplicates / invalid key → 400). `GET /api/admin/nav-role-configs` lists all configs for admin, 403 for non-admin. `POST /api/admin/job-roles` seeds a `nav_role_configs` row by cloning `__default__`. Puppeteer UI probe: BottomNav renders the role-specific primary tabs when the user's `job_role` has a custom config. **In `test:ci` this runs via `test:nav-role-config:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:change-password` | End-to-end coverage for the Change Password dialog on `/profile`: API probes (missing body → 400, wrong current password → 401, same password → 400, valid credentials → 200); UI probes (ProfilePage mounts and Password card renders, dialog opens with three fields and Submit/Cancel buttons, zxcvbn strength meter does not crash on input — regression guard for task #870, browser-autofill simulation via native value setter + input event, empty-form submission surfaces MUI helper texts, full submit flow succeeds); and a page-level error check confirming no console errors occur during the flow. **In `test:ci` this runs via `test:change-password:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:set-password` | End-to-end coverage for the `/set-password` React island (`SetPasswordPage`). API probes: `GET /api/set-password/validate` with missing token → `{valid:false}` (API.1), with a valid token → `{valid:true, email}` (API.2); `POST /api/set-password` with invalid token → 410 (API.3), with valid token + strong password → 200 `{ok:true}` (API.4). UI probes: `[UI.1]` page renders React form with two password inputs (`#pw1`, `#pw2`) and the `#set-password-root` island is populated; `[UI-autofill]` native `HTMLInputElement.prototype` value setter + bubbling `input` event on `#pw1` causes `StrengthMeter` to render `role="progressbar"` (indeterminate, zxcvbn not yet loaded) — regression guard for the NaN/LinearProgress autofill crash (task #870). Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:set-password:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:trades` | End-to-end Trades page coverage: API CRUD + role-gating (admin/manager/viewer/member), Puppeteer-driven UI checks (search filter, category chip, Add Company / Submit for Approval visibility, duplicate-phone warning, Snackbar pause on hidden tab, ContactSlot field wiring). Requires `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:trades:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:sales-board-error-state` | Error and stale-data states for `SalesBoardPage`. **(A)** Bootstrap failure (cold cache, no HubSpot token): `core.js` dispatches `sales-board-bootstrap-failed` without destroying `#sales-board-mount`; component shows "HubSpot is currently unavailable" heading + "Reload page" button, no contact cards. **(B)** Pre-seeded `window.__salesBoardBootstrapFailed` via `evaluateOnNewDocument`: component reads flag synchronously in `useState` initialiser → error card renders immediately on mount (race-condition path). **(C)** Stale contacts: intercepts `/api/contacts-all` returning contacts with `X-Cache-Status: stale`; seeds `window.state` and dispatches `sales-board-cache-status` + `sales-board-data-ready`; asserts contact card present and no error card. **In `test:ci` this runs via `test:sales-board-error-state:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:sales-board-stage-labels` | Regression guard for task #983 re-fetch fix. Navigates to `/sales`, re-injects `#sales-board-mount` after bootstrap fails (no HubSpot token), remounts `SalesBoardPage` via `window.__reactIslandMount()`, and seeds `window.state.workflow` with known stage labels. Exercises two update paths: **(A) BroadcastChannel** — installs `window.loadWorkflow` and `window.loadLeadStatuses` spies, posts `lead_statuses_changed` from a second same-browser tab, and asserts both spy call-counts are ≥ 1 (guards the re-fetch requirement) plus the column header shows the renamed label without a page reload; **(B) visibilitychange** — installs the same spies, synthesises a hidden→visible transition, and asserts both functions are called and the header updates. **In `test:ci` this runs via `test:sales-board-stage-labels:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:survey-board` | End-to-end Puppeteer coverage for `SurveyBoardPage`: **(A)** single-column layout with "Survey" header and Filter button; **(B)** card content (name, postcode, stage pill, substage pill, source pill, stage-trail labels, "Updated" timestamp); **(C)** terminal card de-emphasis (opacity ≈ 0.55 for `statusId` in `SURVEY_TERMINAL_SUBSTAGES`); **(D)** card click navigates to `/customers/:id`; **(E)** substage filter — popover opens, unchecking a substage hides matching cards, re-checking restores them; **(F)** bootstrap-failure error state — dispatching `survey-board-bootstrap-failed` shows the warning icon, "HubSpot is currently unavailable" heading, and "Reload page" button with no `[data-contact-id]` cards; recovery via `survey-board-data-ready` clears the error and renders the board. **In `test:ci` this runs via `test:survey-board:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:customers-pagination` | Puppeteer end-to-end coverage for Customers page server-side pagination. Seeds 30 fake contacts via a mock HubSpot server and 3 `lead_status_config` rows (`PAGTEST_LS_A/B/C`) so the lead-status filter always has real options. Seven probes: **(A.1)** MUI Pagination control appears when total contacts exceeds the page limit (25); **(A.2)** clicking page 2 causes the URL to include `?page=2` and a `/api/contacts-all?page=2` request is made; **(A.3)** page-2 results differ from page-1 results (server-side slicing confirmed); **(C.1)** "Showing X–Y of Z" count string is visible on page 1; **(D.1)** changing the sort select while on page 2 resets the URL back to page 1; **(D.2)** typing in the search field while on page 2 resets to page 1; **(D.3)** selecting a value in the lead-status `<select id="lead-status-filter">` while on page 2 resets to page 1. Accepts `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:customers-pagination:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:dev-mode-bc-sync` | Puppeteer end-to-end regression guard for the `dev_mode_changed` BroadcastChannel wiring added in task #1837. Two pages share a single browser (same origin) so BC messages route across them as real tabs would. The listener page loads `/customers` with API calls request-intercepted. The sender page opens `/admin`, switches to the Dev Environment tab, and clicks the real MUI Switch — exercising the full production path: `handleDevModeToggle` → `POST /api/admin/hubspot/dev-mode` → `BroadcastChannel('dev_mode_changed').postMessage({ devMode })`. Ten probes: **(setup.mount)** React CustomersPage mounts; **(setup.no-banner-initially)** banner absent before toggle; **(setup.devenv-ready)** DevEnvironmentPage Switch becomes enabled; **(BC-A.1)** banner appears on listener tab after Switch clicked ON; **(BC-A.2)** page-load token unchanged (no full reload); **(BC-A.3)** banner text reads "Dev mode is ON"; **(BC-A.4)** sender Switch reflects ON state; **(BC-B.1)** banner disappears after Switch clicked OFF; **(BC-B.2)** no reload on disable; **(BC-B.3)** sender Switch reflects OFF state. Accepts `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:dev-mode-bc-sync:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:stage-scoped-pills` | Puppeteer coverage for stage-scoped lead-status and substatus pill filtering on the Customers page. All `/api/*` calls are intercepted at the browser level so no live HubSpot connection is needed. Five probes: **(A)** stage tabs appear — workflow API response populates All, Sales, and Design Visit tabs in the MUI ToggleButtonGroup; **(B)** clicking a stage tab fires a scoped request — `/api/contacts-lead-status-counts?stage=sales` is called after clicking the Sales tab; **(C)** zero-count pills are hidden — after switching to Sales only the OPEN_DEAL pill (count 5) is visible while NEW (0) and DORMANT (0) are absent; **(D)** substatus counts are stage-scoped — clicking a lead status with an active stage filter sends `/api/contacts-substatus-counts` with both `leadStatus` and `stage` query params; **(E)** zero-count substatus chips hidden — only HOT (3) appears; WARM (0) is absent. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:stage-scoped-pills:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:project-contacts-dev-mode` | Regression guard for the dev-mode filter added to `/api/project-contacts` in task #1883. Spins up a mock HubSpot server returning two contacts (`hw_test_user=true` and `hw_test_user=false`) and seeds one `lead_status_config` row so the IN-filter runs. **(A)** Dev mode OFF: both contacts present in the response (`total=2`). **(B)** Dev mode ON (`POST /api/admin/hubspot/dev-mode { devMode: true }`): only the `hw_test_user=true` contact is returned (`total=1`); the real contact is absent. **(C)** Dev mode OFF again: both contacts visible once more. **(D)** UI — admin + dev mode ON: Puppeteer navigates to `/projects`; asserts `#dev-mode-banner` is visible and contains "Dev mode is ON". **(E)** UI — dev mode OFF: asserts `#dev-mode-banner` is absent. Accepts `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:project-contacts-dev-mode:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:active-projects-hubspot-outage` | Regression guard for the Active Projects error branch added in task #1797. **(AP-A)** `/api/contacts-all` returns a 502 → the "Active Projects" section header is visible, the MUI Alert "Unable to retrieve customer info" is visible, and no customer cards are rendered. **(AP-B)** A Retry button is present inside the error Alert. Boots a disposable test server and overrides `window.fetch` via `evaluateOnNewDocument` so that `/api/contacts-all` resolves to a 502 while all other home-page APIs return minimal stubs. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:active-projects-hubspot-outage:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:customer-info-email-attachments` | Regression guard for the photo-attachment path in `sendAdminNotificationEmail` (`customer-info.js:199-306`). Uses `MAIL_TRANSPORT_FILE_OVERRIDE` to capture email JSON and the in-memory `@replit/object-storage` stub (from `test/customer-info/`) to avoid a live bucket. **(ATT-1)** Happy path — 2 real photos uploaded then submitted: asserts `attachments.length === 2`, each attachment has `contentType=image/jpeg` and non-empty `content`, HTML body contains "2 photos attached", HTML contains no `<a href` signed-URL links (photos travel as attachments only), and text body contains "2 attached". **(ATT-2)** Skip path — 1 real key + 1 key absent from storage: asserts `attachments.length === 1` and HTML contains "1 skipped". Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:customer-info-email-attachments:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:skipped-photo-warning` | Regression guard for the skipped-photo warning Alert added in task #1905 to `CustomerInfoSubmissionsRail.tsx`. When photos are too large to attach to the admin notification email the server stores `email_skipped_count` on the submission row; the rail must render an MUI Alert (role="alert") inside the expanded `SubmissionCard`. Five probes: **(SKP-A)** plural form (`email_skipped_count=2`) — warning Alert with "2 photos were too large…" and "they are still viewable here" is visible after expanding the card; **(SKP-B)** singular form (`email_skipped_count=1`) — Alert contains "1 photo was too large…" and "it is still viewable here"; **(SKP-C)** negative case (`email_skipped_count=0`) — no warning Alert is present after expanding the card; **(SKP-D)** anchor href — for both plural and singular cases, the `<a data-testid="skipped-photo-link">` element inside the Alert must have an `href` equal to the first entry in `photoUrls` (regression guard for the "still viewable here" link added in task #1946); **(SKP-E)** click + photo visibility (`email_skipped_count=2`) — clicks the `[data-testid="skipped-photo-link"]` anchor inside the Alert, then asserts at least one `<img>` with a non-empty `src` is rendered in the section, confirming the click target is real and the photo grid is actually populated (task #1941). Boots a disposable test server and drives `/customers/:contactId` in headless Chromium; all API calls are stubbed via Puppeteer request interception so no live HubSpot or storage connection is required. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:skipped-photo-warning:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js` so no shared-DB flag is needed. |
| `test:skipped-photo-dashboard-link` | Regression guard for task #1913: when photos are skipped (too large / not found in object storage), `sendAdminNotificationEmail` must include a clickable dashboard link so admins can still view the photos. Three probes: **(DASH-A.html-link)** admin email HTML body contains an `<a>` element whose `href` is the full customer dashboard URL (`/customers/:contactId`) with link text "View all photos on the dashboard"; **(DASH-A.text-url)** admin email plain-text body contains the full dashboard URL (no HTML markup, just the raw URL); **(DASH-B.no-link)** when all photos attach successfully (nothing skipped), the admin email does NOT contain a dashboard link — ensures the link only appears when photos are skipped. Spawns the test server with a fake object-storage stub; all API calls are stubbed via Puppeteer request interception so no live HubSpot or storage connection is required. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:skipped-photo-dashboard-link:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |

| `test:var-hex-fallbacks` | Static lint (`scripts/check-var-hex-fallbacks.mjs`). Scans `public/*.js`, `public/*.html`, and `src/react/**/*.ts`/`.tsx` (excluding `*.stories.*`, `*.d.ts`, and `theme.ts`) for CSS custom-property usages that carry a raw hex colour as a fallback — e.g. `var(--orchid, #8B2BFF)`. Because `public/tokens.css` is always loaded before any component renders, hex fallbacks are redundant and create a second source of truth that can silently drift from `src/react/theme.ts`. Numeric fallbacks (e.g. `var(--header-h, 56px)`) and named-colour fallbacks (e.g. `var(--orchid, transparent)`) are not flagged — only `#`-prefixed hex literals are an error. Suppress individual lines with a trailing `// var-hex-ok: <reason>` comment. No server, no DB, no Puppeteer — reads source files directly. |
| `test:customer-info-rail` | Puppeteer end-to-end coverage for the `CustomerInfoSubmissionsRail` React component on `/customers/:contactId`. Stubs `GET /api/customer-info/by-contact/:contactId` (and all other customer-detail API calls) via `evaluateOnNewDocument` fetch interception so no HubSpot token or real contact data is required. Five probes: **(A)** expired-pending card is hidden from the rail; the count badge reflects only the visible (non-expired) entries; **(B)** active-pending card whose `form_link` is non-null renders both `[data-testid="copy-link-btn"]` and `[data-testid="open-link-btn"]` in its header row; **(C)** clicking the Copy button briefly swaps `ContentCopyIcon` for `CheckIcon` (the icon `data-testid` changes from `"copy-icon"` to `"check-icon"`) — regression guard for the copy-success animation; **(D)** submitted card shows the Review button only (no Copy or Open); **(E)** active-pending card with `form_link: null` shows neither Copy nor Open. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:customer-info-rail:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:customer-info-conflict-warning` | Puppeteer end-to-end coverage for the active-link conflict warning in `CustomerInfoSubmissionsRail`. Stubs HubSpot-backed and other external API calls via Puppeteer request interception; inserts a real `customer_info_submissions` row and mocks `navigator.clipboard` via `evaluateOnNewDocument`. Nine probes: **(A)** link-status returns the same `expiresAt` as the displayed card → no conflict Alert, clipboard written immediately; **(B)** link-status returns a different `expiresAt` → conflict Alert appears (`data-testid="conflict-proceed-btn"` visible); **(B2)** clicking "Copy anyway" dismisses the Alert; **(B3)** clicking "Cancel" dismisses the Alert without copying; **(A-open)** same `expiresAt`, open-link button → provisional window navigated to `form_link`, no conflict Alert; **(B-open)** different `expiresAt`, open-link → conflict Alert, provisional window closed; **(B2-open)** "Open anyway" dismisses Alert and calls `window.open(formLink)`; **(B3-open)** "Cancel" dismisses Alert without an extra `window.open` call; **(A-open-blocked)** `window.open` returns `null` on first call (popup blocked), same `expiresAt` → no conflict Alert, fallback `window.open` called with the `form_link` URL. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:customer-info-conflict-warning:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:stale-bundle` | Post-build static check (`scripts/check-stale-bundle.mjs`). Parses every `React.lazy()` page import from `src/react/main.tsx`, then verifies that at least one matching hashed chunk (`<ComponentName>-*.js` or `<ComponentName>.js`) exists under `public/react/chunks/`. Exits non-zero if any chunk is missing so CI catches stale or partial builds early. No server, no DB, no Puppeteer — reads source and build output directly. |
| `test:resolve-action-label` | Unit tests (`scripts/test-resolve-action-label.mjs`) for the `resolveActionLabel` pure function (`src/react/utils/resolveActionLabel.mjs`). Ten test cases: (1) per-LS row with a label → returns that label; (2) per-LS row with `null` label → returns `''` and does NOT fall through to per-stage default; (3) no per-LS row → falls back to per-stage default; (4) no lead status on contact → per-stage default; (5) no matching row → `''`; (6) substatus `action_label` wins over per-LS label; (7) substatus with no `action_label` → falls through to per-LS label; (8) stage/LS key case-insensitivity; (9) legacy per-`substageId` fallback when `lsKey` is absent; (10) missing per-LS key → per-stage default. No server, no DB, no Puppeteer. |
| `test:typo-vars` | Static check (`scripts/check-typo-vars.mjs`). Verifies every `--typo-*` CSS custom property derived from the typography entries in `src/react/theme.ts` is present in `public/tokens.css` with the correct value. Guards the typography token sync contract so `tokens.css` never drifts from `theme.ts`. No server, no DB, no Puppeteer — reads source files directly. |
| `test:color-radius-vars` | Static check (`scripts/check-color-radius-vars.mjs`). Verifies every brand-colour, stage-colour, and radius CSS custom property defined in `BRAND_COLORS`, `STAGE_COLORS`, and `RADIUS` in `src/react/theme.ts` appears in `public/tokens.css` with the correct value. Complements `test:typo-vars` to give complete design-token coverage across `tokens.css`. No server, no DB, no Puppeteer — reads source files directly. |
| `test:mui-select-click` | Static lint (`scripts/check-mui-select-click.mjs`). Scans every `test/**/run.js` file for the anti-pattern of opening a MUI Select dropdown via `page.$(…)` → `.click()` directly, rather than the shared `clickMuiSelect` helper in `test/helpers/mui-select.js`. Detection uses a 15-line sliding window that flags any window containing a `page.$(` call, a variable `.click()` call, and a `MuiSelect` reference together. Windows that already use `clickMuiSelect(` are exempt. No server, no DB, no Puppeteer — reads source files directly. |
| `test:ci-runner-sync` | Static lint (`scripts/check-ci-runner-sync.mjs`). Asserts that every `test:*:ci` string in `scripts/run-ci.mjs` is also present in `scripts/run-ci-parallel.mjs`. Exits non-zero with a clear error message when any entry is absent, preventing drift between the sequential and parallel CI runners. No server, no DB, no Puppeteer — reads source files directly. |
| `test:bottom-nav-lint` | Static lint (`test/bottom-nav-lint/run.js`). Verifies every entry in the `NAV` array exported from `src/react/components/BottomNav.tsx` carries both an `Icon` and an `IconOutlined` field, and that each field's value is a name actually imported from `@mui/icons-material` at the top of the file. Catches copy-paste errors where the icon import is missing but the field is set, which TypeScript alone cannot detect (the field value satisfies the type but the import is wrong). No server, no DB, no Puppeteer — reads the source file directly. |
| `test:hubspot-credentials` | CI env-var validation (`scripts/check-hubspot-credentials.mjs`). Fails immediately if `HUBSPOT_APP_ID` or `HUBSPOT_CLIENT_SECRET` is absent. When `HUBSPOT_ACCESS_TOKEN` is also present, performs a live `GET` to the HubSpot webhooks subscriptions endpoint (`api.hubapi.com/webhooks/v3/{HUBSPOT_APP_ID}/subscriptions`); treats HTTP 200 and 404 as pass; treats 401/403/5xx/network error as fail. Writes `test-results/hubspot-credentials.md` summarising the outcome. |
| `test:handler-config-blocks` | Vitest unit tests (`src/react/pages/admin/HandlerConfigBlocks.test.tsx`) for five `HandlerConfigBlocks.tsx` components: `ScheduleVisitConfig`, `ShowMessageConfig`, `StartDesignVisitConfig`, `DeliveryWindowConfig`, `InstallationSlotConfig`. Each component is tested for: correct default-props render, correct render when initial props are supplied, and correct `onChange` payload shape when fields change (e.g. duration input, visit type combobox, `addToGoogleCalendar` checkbox). No server, no DB — uses Vitest + React Testing Library in jsdom. |
| `test:lead-status-delete-substatus-clear` | Integration test for `clearOrphanedSubstatusesForDeletedStatus` in `server.js`. When a lead status is deleted via `DELETE /api/admin/lead-statuses/:key`, a background job must clean up affected HubSpot contacts. Probes: **(A)** search + patch — mocked HubSpot search returns contacts with `hw_lead_substatus` set; server patches each one clearing the field; **(B)** zero results — search returns no contacts, no PATCH is issued; **(C)** search failure — search errors → `substatus_clear_failures` row written, `GET /api/admin/substatus-clear-failures` returns the failure record; **(D)** PATCH failure — contact PATCH errors after all retries → per-contact failure record written, manual retry endpoint re-triggers the job. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:lead-status-delete-substatus-clear:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:customer-info` | End-to-end coverage for the `upload_photos_and_info` card-action handler. HTTP probes: **(CI-1)** `POST /api/card-actions/upload-photos-and-info` → 200 with token in DB; **(CI-2)** `GET /api/customer-info/:token` → 200 with masked email + name; **(CI-3)** `POST /api/customer-info/:token/photos` → 200 with key list; **(CI-4)** `POST /api/customer-info/:token` (submit) → 200 with emails sent; **(CI-5)** `GET /api/customer-info/by-contact/:contactId` → list with `photoUrls`; **(CI-R1)** admin resend → 200, new DB row, old rows preserved; **(CI-R2)** viewer resend → 403; **(CI-R3)** non-numeric contactId → 400; **(CI-6–9)** expired/submitted tokens → 410. Puppeteer UI probes: **(CI-UI-A)** admin sees `[data-testid="resend-link-btn"]`; **(CI-UI-B)** viewer does not. Uses `HUBSPOT_API_BASE_OVERRIDE`, `MAIL_TRANSPORT_FILE_OVERRIDE`, and an in-memory object-storage stub. **In `test:ci` this runs via `test:customer-info:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:photo-approval-notification` | Regression guard for audit-log details on profile-photo approve/reject actions. Three passes: **(Pass 1 — SMTP via file override)** approve and reject each write `"email notification sent to <email>"` in the `admin_audit_log` details column, and the file-override mail file contains an email to that address; **(Pass 2 — no SMTP)** both actions write `"email notification skipped (SMTP not configured)"`; **(Pass 3 — broken transport)** both actions write `"email notification failed to send"`. Verifies the `details` column is always a structured object, never a plain string. Uses `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1`. **In `test:ci` this runs via `test:photo-approval-notification:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:photo-reviews` | Integration tests for `review-customer-photos` card-action-handler routes (`photo-reviews.js`). Covers: `GET` null (no submitted/unreviewed submission), `GET` ignores unsubmitted rows, `GET` detail with signed `photoUrls`; `POST outcome=not_suitable` (email + HubSpot PATCH + `photo_review_outcomes` row); `POST` 409 when already reviewed; `POST outcome=rough_estimate_sent` with missing `priceRange` → 400 and happy path; `POST` 404 for wrong `contactId` including true cross-contact ownership check; unauthenticated `GET`/`POST` → 401. Uses `HUBSPOT_API_BASE_OVERRIDE` and `MAIL_TRANSPORT_FILE_OVERRIDE`. **In `test:ci` this runs via `test:photo-reviews:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:start-design-visit` | Full end-to-end coverage for the `start_design_visit` card-action handler. **(API)** catalogue endpoints respond for admin; 401 when unauthenticated. **(A)** Wizard submit — `POST /api/design-visits` with a seeded contact; DB rows confirmed in `design_visits`, `design_visit_rooms`, and `design_visit_room_images`; status becomes `submitted`; HubSpot/QB/email side-effects skipped non-fatally. **(B)** Sign-off approve — GET summary; POST approve → `signed_off`, token nulled; second POST → 404. **(C)** Sign-off revision — POST revision → `revision_requested`; re-submit via `/submit` → `submitted`. **(D)** Token security — expired, wrong, and consumed tokens all return 404 (no oracle leakage). **(E)** Admin catalogue CRUD — handle, furniture range, door style via admin API; non-admin mutations → 403. **(F)** BroadcastChannel wizard refresh — `design_visit_handles_changed` from a second Puppeteer page is received by the first page's listener. **(G)** Privilege gates — unauthenticated `POST /api/design-visits` → 401; non-admin `DELETE /api/design-visits/:id` → 403; public sign-off routes reachable without session. **In `test:ci` this runs via `test:start-design-visit:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:design-visit` | Wizard UI and sign-off flow coverage (focused counterpart to `test:start-design-visit`). **(CRUD)** Admin catalogue CRUD for handles, furniture ranges, door styles, and T&C versions. **(WIZ)** Wizard dispatches from clicking a bound `.eq-card-action` strip and opens the multi-step wizard. **(ROOM)** Room add/remove UI validation in step 2 (add appends a card; remove drops it; "Every room needs a name" guard fires; Remove button hidden when only one room remains). **(SUB)** `POST /api/design-visits` side-effect chain — 201 with `designVisitId`, status → `submitted`, QB skipped non-fatally, email transport absent (silent skip). **(TOK)** Sign-off token generated and pinned to a 7-day expiry. **(PUB)** Public sign-off API: GET returns visit; POST approve → `signed_off` + token invalidated; POST revision → `revision_requested` + token invalidated. **In `test:ci` this runs via `test:design-visit:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:visit-edit-cancel` | Coverage for edit and cancel actions on delivery/installation visit cards in `CustomerDetailPage`. API probes: `PATCH /api/visits/:id` updates the row and returns the updated visit; `DELETE /api/visits/:id` removes the row and returns `{ success: true }`; both endpoints require authentication (anonymous → blocked). UI probes (Puppeteer): admin/manager see Edit + Cancel icon buttons on delivery and installation cards; member/viewer do NOT; Cancel flow opens `CancelVisitDialog` → "Cancel visit" removes the visit from the list and the DB; Edit flow opens `GenericVisitEditModal` pre-filled with the visit title → rename → "Save changes" refreshes the list and updates the DB row. **In `test:ci` this runs via `test:visit-edit-cancel:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:dv-catalogue-admin` | Admin catalogue modal coverage for Design Visit Handles, Furniture Ranges, and Door Styles (tasks #668/#688). For each type: add modal — fill fields, Save button shows "Saving…" while POST is in flight, modal closes, new row appears in the correct wrap `div` without a page reload; edit modal — re-opened via `openDvItemEditor`, pre-filled with existing values, rename + field change, PATCH holds "Saving…" lock, wrap refreshes in place, DB row reflects edit; delete — `window.confirm` stubbed true, Delete button → `DELETE` request, row disappears without reload, DB row gone (Handle delete also unlinks the local image file via `_deleteLocalHandleImage`). **In `test:ci` this runs via `test:dv-catalogue-admin:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:dv-catalogue-image-upload` | Handle image-upload flow (task #687). **(S) Success path** — open Add Handle modal, attach a small PNG to `#dvie-img-file`, click Save: `POST /api/admin/design-visit-handles` creates the row, a follow-up `POST /api/admin/dv-handles/:id/image` uploads the file, the modal closes, `#dv-handles-wrap` re-renders an `<img>` with the correct `/uploads/handles/…` URL, and `design_visit_handles.image_url` in the DB is non-null. **(F) Failure path** — edit modal for an existing handle, attach a PNG, intercept the image POST and force a 500: modal stays open, `#dvie-err` shows a visible "Image upload failed" message, DB `image_url` is unchanged. **In `test:ci` this runs via `test:dv-catalogue-image-upload:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:dv-catalogue-reorder` | Catalogue up/down arrow reorder for Handles, Furniture Ranges, and Door Styles (task #720). For each type: seeds two rows with known `sort_order` values; loads the Design Visit tab and asserts initial DOM order; confirms boundary disable-states (top row ▲ disabled, bottom row ▼ disabled, inner arrows enabled); clicks ▼ on the top row; asserts `window.__pageLoadToken` is unchanged (no page reload), DOM rows are in swapped order, boundary disable-states are flipped, and the two DB rows' `sort_order` values are exchanged. **In `test:ci` this runs via `test:dv-catalogue-reorder:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:window-ui-smoke` | Smoke test for static chrome mount points. Boots a server with admin credentials, then visits each dashboard route (`/`, `/customers`, `/sales`, `/survey`, `/projects`, `/calendar`, `/invoices`, `/trades`) and asserts: `#app-header-mount` exists in the DOM with React content (GlobalHeader mounted), and `nav.bottom-nav#main-content` exists on every non-admin route (BottomNav island mounted). Catches the case where a new page is added and the maintainer forgets to include the static mount `div`s. **In `test:ci` this runs via `test:window-ui-smoke:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:sign-off-stale-link` | Coverage for the "superseded" sign-off path (tasks #715/#726). A design-visit sign-off link that has been superseded (by a PUT reopen or a revision→re-submit cycle) must return `200 status=superseded` (showing the "Changes in progress" banner, hiding the action card) and `409 status=superseded` on POST, rather than 404. Probes: **(PUT-A–D)** designer reopens via `PUT /api/design-visits/:id` — old token ends up in `superseded_signoff_token_hashes`; GET → 200 superseded; POST approve/revision → 409 superseded. **(RES-A–D)** revision + re-submit path — same outcomes. **(UNK-A–B)** genuinely unknown tokens still return 404. **In `test:ci` this runs via `test:sign-off-stale-link:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:react-admin-tabs` | Smoke test for React islands in the admin panel. Confirms that the bundle built into `public/react/main.js` actually renders the Search (`#tab-search`) and Design System (`#tab-designsystem`) panels with their expected React-owned markup after login. A broken Vite build, a missed `MOUNTS` entry, or a regression on the self-mount path would surface here rather than waiting for a production admin to click the tab. **In `test:ci` this runs via `test:react-admin-tabs:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:new-customer-flow` | Coverage for the "New customer" entry point on `/customers` (task #754). Boots a local mock HubSpot server for contact creation. API probe: member `POST /api/contacts` with valid payload → 201, mock HubSpot received expected properties (`firstname`/`lastname`/`email`/`phone`/`zip`/`hs_lead_status`). Puppeteer UI probes: member fills the dialog → row appears in `#customers-results` without page reload; `/customers?new=1` auto-opens the dialog on load; viewer role has no `#new-customer-btn`. **In `test:ci` this runs via `test:new-customer-flow:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:new-customer-counts-retry` | Focused Puppeteer test for the `attemptCreatedCounts` retry logic in `CustomersPage.tsx` (tasks #840/#850). After a new customer is created, `loadLeadStatusCounts` is retried up to 3 times with 30 s gaps (collapsed to 10 ms via `setTimeout` injection). **(D)** All retries fail → Snackbar "Couldn't refresh live data — fresh results will load on your next visit" appears. **(E)** Second attempt succeeds → Snackbar does NOT appear. `/api/contacts-lead-status-counts` is controlled via Puppeteer request interception so no live HubSpot connection is needed. **In `test:ci` this runs via `test:new-customer-counts-retry:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:calendar-page` | End-to-end coverage for the React+MUI Calendar page (task #790). **(API)** `GET /api/visits` with date params returns seeded rows. **(NAV)** Week prev/next/today navigation updates day-card headers and card counts. **(PRF)** Workshop toggle persists via `PATCH /api/users/me/prefs` (`calShowWorkshop`) and the saved value is read back. **(TSK)** Personal task add → toggle done → delete round-trip from the UI, cross-checked against `/api/personal-tasks`. **(VIS)** Visit modal create, edit, and delete; DB rows reflect each step. **(GCL)** When Google Calendar status is mocked to `connected`, the "Also add to Google Calendar" toggle appears and its pref is persisted. **In `test:ci` this runs via `test:calendar-page:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:hubspot-429-retry` | HubSpot 429 retry resilience for urgency and design-visit pipelines, via a local mock HubSpot server (`HUBSPOT_API_BASE_OVERRIDE`). **(U1)** urgency assoc-batch 429 → retry → success; **(U2)** urgency task-batch 429 → retry → success; **(WS)** workflow-stages double-retry (both notes-search and assoc-batch recover within one cold-cache fetch); **(DV1)** design-visit note 429 → retry → success (exactly 2 mock calls); **(DV2)** design-visit note exhausted → non-fatal, submit returns 200; **(DV3)** lead-status PATCH 429 → retry → success; **(DV4)** lead-status PATCH exhausted → non-fatal; **(DV5)** revision-requested resubmit with note 429 → retry → success. **In `test:ci` this runs via `test:hubspot-429-retry:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:hubspot-429-retry-contacts` | 429 retry and stale-cache fallback for `GET /api/contacts-all` and `GET /api/open-leads`. **(CA)** contacts-all 429 → retry → success; **(OL)** open-leads 429 → retry → success; **(OL-R)** open-leads recovery: after a stale storm, switching HubSpot back to OK triggers a fresh fetch and subsequent requests are served from the new in-memory cache; **(OL-F)** open-leads all retries exhausted (cold cache) → 502 `HUBSPOT_RATE_LIMIT`; **(CA-F)** contacts-all exhausted (cold cache) → 502; **(CA-S)** contacts-all sustained outage with a prior good cache → served stale with `X-Cache-Status: stale`; **(CA-R)** recovery after stale storm; **(LD)** `GET /api/localdata/all` catch-block stale fallback when the stale cap has expired. **In `test:ci` this runs via `test:hubspot-429-retry-contacts:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:visits-past-time` | Past-time guard on `POST /api/visits`. **(A)** `startAt` more than 5 min in the past → 422 `START_IN_PAST`; **(B)** `startAt` 1 min ago (inside 5-min grace) → 200; **(C)** future `startAt` → 200; **(D1)** 4 min past (inside grace) → 200; **(D2)** 6 min past (outside grace) → 422; **(E)** type `delivery` — past → 422, future → 200; **(F)** type `installation` — past → 422, future → 200. **In `test:ci` this runs via `test:visits-past-time:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:hubspot-credential-audit` | Integration test for `admin_audit_log` entries on HubSpot credential changes. **(A)** Member gets 403 for both `PATCH` and `DELETE /api/admin/hubspot-credentials`. **(B)** `PATCH` writes a `set_hubspot_credential` row with the correct `admin_email` and `details = 'key=<name>'`. **(C)** `DELETE` writes a `clear_hubspot_credential` row with the correct actor and key name. **(D)** All three valid credential keys (`access_token`, `app_id`, `client_secret`) produce audit rows for both operations. **(E)** Validation errors (bad key or empty value) produce no audit rows. **In `test:ci` this runs via `test:hubspot-credential-audit:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:contacts-all-stale-fallback` | Stale-cache behaviour and hard-cap enforcement for `GET /api/contacts-all`. **(A)** HubSpot 503 after a prior good cache → contacts served with `X-Cache-Status: stale` (no 502). **(B)** Network connection drop after a prior good cache → contacts still served stale. **(C)** Stale cap exceeded (via `ALL_CONTACTS_STALE_MAX_MS_OVERRIDE=200`) → 502 instead of serving expired data. **(D)** Recovery: once HubSpot comes back online, the endpoint fetches fresh data (HubSpot is actually called) and subsequent requests are served from the new in-memory cache without hitting HubSpot again. **In `test:ci` this runs via `test:contacts-all-stale-fallback:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:contacts-stale-visibility` | Puppeteer tab-visibility pause for the stale-data banner on `CustomersPage`. **(F1)** `document.hidden=true` → intercept `/api/contacts-all` returning `X-Cache-Status: stale` and trigger a re-fetch via the search input — banner must NOT appear while hidden, but MUST appear after a synthetic `visibilitychange → visible`. **(F2)** With banner showing, set `document.hidden=true`, switch the interceptor to a fresh response and trigger a re-fetch — banner must STILL show while hidden (pending clear is deferred), and MUST disappear only after `visibilitychange → visible`. **In `test:ci` this runs via `test:contacts-stale-visibility:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:login` | Autofill crash guard for the `/login` React island (`LoginPage`). Simulates browser credential autofill via the native `HTMLInputElement.prototype` value setter + a bubbling `input` event on both `#login-email` and `#login-password` — the same vector that triggered a NaN crash in `StrengthMeter` on the set-password page (task #1153/#1258). The login form has no `StrengthMeter`, so the probe is purely a crash/error guard: autofill must not produce any console errors. UI probes: **(UI.1)** `/login` → `LoginPage` rendered with both inputs visible; **(UI-autofill)** native autofill on both inputs → zero console errors. **In `test:ci` this runs via `test:login:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:invoice-admin-controls` | Privilege-gate and data-scoping tests for QuickBooks invoice endpoints. Privilege gates: manager gets HTTP 403 on all QB invoice routes (`GET /api/quickbooks/invoices`, `GET /api/quickbooks/invoice/:id`, `POST /api/quickbooks/invoice/:id`, `GET /api/quickbooks/invoice/:id/pdf`, `POST /api/quickbooks/invoice/:id/send`); unauthenticated gets 401; admin gets a non-403 response. Data-scoping: when QB is disconnected (no tokens in DB) the response is a structured error object with no raw token fields; when a fake token is stored and the QB call fails, the error response still exposes no token fields. **In `test:ci` this runs via `test:invoice-admin-controls:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:invoice-bc-sync` | Puppeteer BroadcastChannel refresh test for invoice saves (task #1760). When an admin saves an invoice in `InvoiceDetailDrawer` (via the "Save changes" button), it posts `{ type: 'invoice-saved' }` on the `mo_invoices` BroadcastChannel. Two probes — each opens a listener page and a sender page in the same browser so BC messages route correctly: **(BC-A)** `InvoicesSection` on `CustomerDetailPage` re-fetches `GET /api/quickbooks/invoices` after save without a full page reload (confirmed via `window.__invoiceFetchCount` and `window.__pageLoadToken`); **(BC-B)** `StandaloneInvoicesPage` on `/invoices` re-fetches without reload. All API calls stubbed via `evaluateOnNewDocument` fetch interception. **In `test:ci` this runs via `test:invoice-bc-sync:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:bottom-nav-default` | Regression guard for the admin-configured `__default__` nav layout reaching users with no `job_role` (task #1892). **(DEF-API)** `GET /api/nav-role-config` for a no-role user returns `default_is_customized: false` when `__default__` is not customised. **(DEF-API-CUSTOM)** After `PATCH /api/admin/nav-role-config/__default__`, the endpoint returns the new keys and `default_is_customized: true`. **(DEF-UI)** Puppeteer: `BottomNav` renders the admin-configured `__default__` keys for a no-role user (via request interception). **(DEF-UI-RESTORE)** After the `__default__` config is reset (`is_customized=false`), the bar falls back to privilege-level defaults. **In `test:ci` this runs via `test:bottom-nav-default:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:nav-customise-reset` | Exercises the Reset-button probes from the nav customisation dialog (shares the same runner as `test:nav-customise` — `test/nav-customise/run.js`). Covered probes: **(RST-DISABLED)** Reset button is disabled when the current selection already matches the role defaults; **(RST-RESET)** clicking Reset in a dialog opened with a non-default selection pre-checks the correct default keys; **(RST-SAVE)** saving after Reset calls `PATCH /api/admin/nav-role-config` with the default `primary_keys` array; **(INHERIT-BANNER-ON/OFF)** dialog shows an "inherits the default layout" Alert when `is_customized=false` and hides it after a custom layout is saved. **In `test:ci` this runs via `test:nav-customise-reset:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:admin-tab-skeletons` | Puppeteer request-interception suite for the Permissions, Requests, and Audit Log admin tabs — in-component data skeleton layer (layer 2). Holds the relevant API endpoints open while React components are mounted by activating each tab; asserts `.MuiSkeleton-root` elements appear inside the panel while requests are held; releases the requests and confirms skeletons are gone and real content is present. Complements `test:admin-tab-skeletons-suspense` which tests layer 1 (Suspense fallback skeletons shown while the lazy JS chunk loads). **In `test:ci` this runs via `test:admin-tab-skeletons:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:sales-board` | Puppeteer end-to-end coverage for `SalesBoardPage` (the Kanban-style sales pipeline view). Stubs the contacts and workflow-stages APIs via `evaluateOnNewDocument` fetch interception so no live HubSpot token is required. **In `test:ci` this runs via `test:sales-board:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:sales-board-bootstrap` | Coverage for the initial data-load (bootstrap) path of `SalesBoardPage`: verifies `core.js` dispatches `sales-board-data-ready` after seeding `window.state` with contacts and workflow stages, and that the React component renders contact cards in the correct stage columns. **In `test:ci` this runs via `test:sales-board-bootstrap:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:room-stale-banner` | Puppeteer coverage for the room-assignments stale-data banner on the Projects page. **(STALE)** `GET /api/localdata/all` returns `X-Cache-Status: stale` → banner "Room data may be out of date" appears. **(FRESH)** endpoint returns `X-Cache-Status: fresh` → banner is absent. **(DISMISS)** clicking the dismiss (×) button causes the banner to disappear. All API calls are stubbed via Puppeteer request interception; `/api/open-leads` is stubbed to empty results so the page bootstrap completes. **In `test:ci` this runs via `test:room-stale-banner:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:permissions-ui` | Puppeteer regression guard for the correct icons in the Permissions admin tab (task #963). Seeds an admin user and a test job role, then opens `/admin` → Permissions tab. **(ICON-DELETE)** The "Remove role" icon button renders the MUI Delete (trash-can) SVG `<path d="…">`, not the Close (✕) path — guards against a copy-paste error. **(ICON-NOT-CLOSE)** Negative assertion: none of the "Remove role" buttons contain the Close icon path. **(ICON-TUNE)** The "Edit navigation layout" Tune icon is distinct from both Delete and Close. **In `test:ci` this runs via `test:permissions-ui:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:project-contacts-unknown-status` | Regression guard for the orphan-check logic added in task #1653. Verifies that contacts with `measure_once_rooms` data but an absent or unconfigured HubSpot lead status appear in `GET /api/project-contacts` rather than being silently dropped. **(PC-A)** API returns the synthetic unknown-status contact. **(PC-B)** The contact carries `_statusUnknown: true`. **(PC-C)** Contacts returned by the IN-filter (known statuses) are NOT tagged with `_statusUnknown` (regression guard: they must not appear twice). **(PC-D)** Puppeteer: the Projects page card for the unknown-status contact shows an amber "Unknown status" badge. **In `test:ci` this runs via `test:project-contacts-unknown-status:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:profile-google-calendar` | Puppeteer coverage for the `GoogleCalendarCard` on `/profile` (task #1171). Intercepts `/api/google/status` and `/auth/logout-google` via `evaluateOnNewDocument`. **(GC-A)** Disconnected state (`{ connected: false }`) → "Not connected" chip visible, "Connect Google Calendar" button visible, "Disconnect" button absent. **(GC-B)** Connected state (`{ connected: true }`) → "Connected" chip visible, "Disconnect" button visible, "Connect Google Calendar" button absent. **(GC-C)** Clicking "Disconnect" calls `POST /auth/logout-google`, then UI transitions to disconnected state. **In `test:ci` this runs via `test:profile-google-calendar:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:scheduling-past-time-guard` | Puppeteer coverage for the past-time confirmation dialog and 15-minute start-time warning across three modals (tasks #1615/#1677). For each of `InstallationSlotModal` (IS), `DeliveryWindowModal` (DW), and `VisitCalendarModal` (VCM): submitting with a past `startDt` (via a mocked `Date.now`) opens the "Schedule in the past?" confirmation dialog ([IS/DW/VCM]-PAST); "Go back" dismisses it and keeps the form open ([IS/DW/VCM]-BACK); "Schedule anyway" fires `POST /api/visits` and closes the modal ([IS/DW/VCM]-PROCEED). For IS and DW, a 15-minute warning Alert appears in edit mode when `startAt` is within 15 minutes ([WARN-IS]/[WARN-DW]); [WARN-VCM] covers the same for `VisitCalendarModal` when the `DateTimePicker` is updated to a near-future time. **In `test:ci` this runs via `test:scheduling-past-time-guard:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:upload-photos-resend-mode` | Regression guard for resend-mode detection in `UploadPhotosModal.tsx`. The modal fetches `GET /api/customer-info/by-contact/:contactId` on open and switches between send and resend modes based on whether an active (non-submitted, non-expired) submission exists. **(A)** Pending row present → title "Resend photo upload link", button "Resend link". **(B)** No pending rows → title "Send photo upload link", button "Send email". **(C)** Network error on the GET → falls back to send mode (title "Send photo upload link", button "Send email"). All customers-page API calls stubbed via `evaluateOnNewDocument` fetch interception; the modal is dispatched programmatically via `window.dispatchCardActionHandler`. **In `test:ci` this runs via `test:upload-photos-resend-mode:ci`** which automatically spins up an isolated temp database using `scripts/with-test-db.js`. |
| `test:storybook-output-clean` | Build verification (`scripts/check-storybook-output-clean.mjs`). Asserts that no application files — HTML pages (e.g. `admin.html`, `customers.html`) or JS modules (e.g. `workflow.js`, `chrome.js`) — were copied into the Storybook output root. Guards the `viteFinal: config.publicDir = false` hook in `.storybook/main.ts` against accidental regression (e.g. removing `viteFinal`, changing the output dir, or a Storybook upgrade that changes default behaviour). The set of application files to check is derived at runtime from `public/*.html` and `public/*.js`. When `STORYBOOK_OUT_DIR` points to an existing directory the build step is skipped and the existing output is inspected directly — used in CI where `build:storybook` has already run earlier in the pipeline. |

**Isolated temp-database wrapper:** `scripts/with-test-db.js` creates a fresh PostgreSQL database (named `mo_testdb_<timestamp_ms>_<hex>`), sets `DATABASE_URL_TEST` for the child process, runs the target script, then drops the temp DB on exit (including SIGINT/SIGTERM). A startup pruning step runs before each new DB is created: any existing `mo_testdb_*` database older than `TEST_DB_PRUNE_TTL_MS` (default 2 hours) is dropped automatically, guarding against orphan databases left by a force-killed CI runner. Databases using the legacy naming format (`mo_testdb_<hex>`, no timestamp) are treated as infinitely old and always pruned. All DB-dependent suites in `test:ci` run via a `:<suite>:ci` variant that invokes this wrapper, so no external `DATABASE_URL_TEST` or `PRIVTEST_ALLOW_SHARED_DB=1` flag is required when running `npm run test:ci`.

**Standalone orphan pruner:** `scripts/prune-test-dbs.js` (npm script: `prune-test-dbs`) can be run independently as a CI post-step or on-demand to drop all stale `mo_testdb_*` databases. Respects the same `TEST_DB_PRUNE_TTL_MS` env override. Logs each dropped database name and a final count summary.

**Privileges harness behaviour:** strips `TURNSTILE_SECRET_KEY`,
`HUBSPOT_TOKEN`, `SMTP_*`, `GOOGLE_*`, `QB_*` so it runs without
third-party credentials. Opt any back in with
`PRIVTEST_USE_<NAME>=1 <NAME>=… npm run test:privileges`. `ADMIN_EMAILS` is
an `optionalPassthrough` (`PRIVTEST_USE_ADMIN_EMAILS=1`).

**HubSpot-token-stripping side effect:** when `HUBSPOT_TOKEN` is stripped,
`GET /api/contacts/:id` 503s and customer-detail bootstrap replaces
`#workflow-view` with an error. Tests that need the page-level renderers
wait for that error, then re-inject the relevant mount (e.g.
`#workflow-stages`, `#design-visits-section`) and seed `state.selectedContact`
directly. Lead statuses/sub-statuses/design visits come from PostgreSQL,
so the renderer paths under test still run faithfully.

**Test-only env overrides** (never set in production): `QB_API_BASE_OVERRIDE`
(`design-visits.js`), `HUBSPOT_API_BASE_OVERRIDE`, `MAIL_TRANSPORT_FILE_OVERRIDE`.

## Required Secrets
- `DATABASE_URL`, `SESSION_SECRET` — required.
- `ADMIN_EMAILS` — comma-separated bootstrap admins.
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM` — set-password
  email. If missing, the link is logged to the server console.
- `APP_URL` (or `REPLIT_DOMAINS`) — absolute link base in emails.
- `HUBSPOT_TOKEN` — HubSpot private app token (otherwise HubSpot endpoints 503).
- `GOOGLE_*` — Google OAuth (Gmail + Calendar).
- `TURNSTILE_SECRET_KEY`, `TURNSTILE_SITE_KEY` — **required in production** for
  Cloudflare Turnstile captcha on `/api/login`, `/api/forgot-password`,
  `/api/request-access`. Absent in production → endpoints fail closed (503).
  Dev mode is a no-op. A `[SECURITY]` warning is logged at startup if unset.
- `BOOTSTRAP_ADMIN_PASSWORD` — **emergency-only** fallback for `ADMIN_EMAILS`
  accounts with no password set and no pending force-reset token. Blocked
  once an admin sets their own password, and during the window after a
  `force-password-reset`. Use a strong random value. Every use is logged as
  `[SECURITY] Bootstrap admin login used`. Omit until needed.
- `DEBUG_HUBSPOT` — optional. When truthy, enables verbose server logs for
  HubSpot rate-limit events, retry/backoff attempts in `hubspotSearchWithRetry`
  (used by `/api/open-leads` and `/api/contacts-lead-status-counts`), and
  stale-cache fallbacks in `/api/contacts-lead-status-counts`
  (e.g. `serving stale counts`). Unset in production to keep logs quiet;
  a `[DEBUG]` warning is printed at startup when this flag is on.

`REPL_ID` and Replit OIDC are no longer used.
