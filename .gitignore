

# HubSpot config file
hubspot.config.yml

# Secrets — never commit
.env

# Customer data (GDPR)
data/

# Dependencies
node_modules/

# HubSpot CLI project folders
harry-wardrobes-crm/
measure-once/

# Claude Code local settings and worktrees
.claude/settings.local.json
.claude/worktrees/

# Build outputs (regenerated by `npm run build:react` / `build:storybook`)
public/react/
public/storybook/
# Service worker (regenerated by `scripts/build-sw.mjs` on every build)
public/sw.js
storybook-static/
test-results/
!test-results/new-customer-counts-retry.md

# Security audit reports (local-only, may detail unfixed vulnerabilities)
security-audits/
