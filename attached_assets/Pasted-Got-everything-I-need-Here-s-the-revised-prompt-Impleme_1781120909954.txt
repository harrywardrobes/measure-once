Got everything I need. Here's the revised prompt:

---

## Implementation Prompt — `contactCustomer` Action (Revised)

### Codebase context

Node.js/Express + React/TypeScript + PostgreSQL app with a HubSpot CRM integration. Key patterns to follow:

- **Migrations**: `/migrations/`, files named `TIMESTAMP_description.js`, using `node-pg-migrate` (`exports.up = (pgm) => { pgm.sql(\`...\`) }`)
- **Backend**: Single `server.js` (~6500 lines). All new routes go there. `pool` is the pg client. `HS` is the HubSpot base URL. `getHubSpotHeaders()` returns auth headers. `axios` is imported. Retry/backoff pattern for HubSpot calls is at lines ~2100–2130 (3 retries, delays `[250, 750, 1500]`).
- **Action handlers**: `card_action_handlers` DB table with a `type` string. New types need a modal component + a case in `CardActionModalsHost.tsx`. Dispatched via `dispatchCardActionHandler()`.
- **Modal components**: `/src/react/components/modals/`. Multi-phase pattern with a phase string union. Study `ArrangeVisitModal.tsx` for the exact structure.
- **Upload photos action**: Already fully implemented. `POST /api/card-actions/upload-photos-and-info` + `UploadPhotosModal.tsx`. **Do not re-implement this — reuse it entirely** (see Task 3 below).
- **Cache invalidation after HubSpot status change**: always call `clearContactCache()`, `_invalidateLeadStatusCountsCache()`, `_invalidateOpenLeadsCache()`, `_invalidateProjectContactsCache()`.
- **Style**: Tailwind, no comments unless genuinely non-obvious, match semicolon/import conventions of surrounding files. No new comments.

---

### Task 1 — Database Migration

Create `/migrations/1749200000021_contact_attempt_tracking.js`:

```sql
CREATE TABLE IF NOT EXISTS contact_attempt_tracking (
  hubspot_contact_id TEXT    PRIMARY KEY,
  call_attempted     BOOLEAN NOT NULL DEFAULT FALSE,
  email_sent         BOOLEAN NOT NULL DEFAULT FALSE,
  whatsapp_sent      BOOLEAN NOT NULL DEFAULT FALSE,
  updated_at         TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

### Task 2 — Backend Routes (in `server.js`)

Add these routes near the other `/api/card-actions/` routes (around line 6490+).

**Route 1: `POST /api/card-actions/contact-customer`**
- Middleware: `isAuthenticated`, `requireHubspotToken`
- Body: `{ contactId: string }`
- Fetch from HubSpot: `firstname`, `lastname`, `email`, `phone`, `mobilephone`, `hs_whatsapp_phone_number`, `hs_lead_status`
- Query `contact_attempt_tracking` by `hubspot_contact_id`. If no row, return all-false defaults.
- Return:
```json
{
  "contact": {
    "id": "...",
    "firstname": "...",
    "lastname": "...",
    "email": "...",
    "phone": "...",
    "mobilephone": "...",
    "whatsapp": "...",
    "leadStatus": "..."
  },
  "attempts": {
    "call_attempted": false,
    "email_sent": false,
    "whatsapp_sent": false
  }
}
```

**Route 2: `PATCH /api/card-actions/contact-customer/:contactId/attempts`**
- Middleware: `isAuthenticated`, `requirePrivilege('member')`
- Body: any subset of `{ call_attempted, email_sent, whatsapp_sent }` (booleans)
- Upsert into `contact_attempt_tracking` using `ON CONFLICT (hubspot_contact_id) DO UPDATE SET <provided fields>, updated_at = NOW()`
- Re-read and return the full updated row: `{ call_attempted, email_sent, whatsapp_sent }`

**Route 3: `POST /api/card-actions/contact-customer/:contactId/advance-status`**
- Middleware: `isAuthenticated`, `requirePrivilege('member')`, `requireHubspotToken`, `hubspotMutationLimiter`
- Body: `{ currentLeadStatus: string, target: 'attempted_to_contact' | 'no_response' }`
- Resolve the HubSpot key:
  - `target === 'attempted_to_contact'` → `ATTEMPTED_TO_CONTACT`
  - `target === 'no_response'` → `NO_RESPONSE`
- Validate the key exists using `lead-status-guard.js` (`assertLeadStatusExists(key)`) before patching
- PATCH HubSpot `hs_lead_status` with the retry/backoff pattern (3 retries, delays `[250, 750, 1500]`, retry on 429/5xx/network errors)
- On success call the 4 cache-invalidation functions
- Return `{ ok: true, advancedTo: key }`

---

### Task 3 — Modal Component

Create `/src/react/components/modals/ContactCustomerModal.tsx`.

**Props:**
```typescript
interface ContactCustomerModalProps {
  contactId: string;
  contactName: string;
  contactEmail: string;
  onClose: () => void;
}
```

**Phase type:**
```typescript
type Phase = 'loading' | 'contact' | 'no_response_confirm' | 'advancing' | 'done';
```

---

**Phase: `loading`**
- Call `POST /api/card-actions/contact-customer` with `{ contactId }`
- On success → `contact` phase, storing returned `contact` and `attempts` in state

---

**Phase: `contact`** — main view

Layout (match ArrangeVisitModal call-phase style):
- Contact name as heading
- Phone numbers listed (phone, mobilephone, whatsapp if present)

Checklist section — 3 toggle buttons/checkboxes styled as interactive items:
| Flag | Label |
|---|---|
| `call_attempted` | Phone Call |
| `email_sent` | Email |
| `whatsapp_sent` | WhatsApp |

Checkbox behaviour:
- On each tick/untick, **immediately** call `PATCH /api/card-actions/contact-customer/:contactId/attempts` with the changed flag. Disable the checkbox while the request is in-flight (prevent double-tap). Update local state optimistically; revert on error and show a brief inline error.

Action buttons (below checklist):

1. **"Send Upload Link"** button — always visible. On click:
   - Close this modal (`onClose()`)
   - Then dispatch the existing `upload_photos_and_info` handler for this contact using `dispatchCardActionHandler`. You need the handler object — get it from the handlers already loaded in context/hook (`useCardActionHandlers` or the modal registry). Look up the handler by `type === 'upload_photos_and_info'` from the available handlers. Pass context `{ contactId, contactName, contactEmail }`. This triggers the full existing UploadPhotosModal flow including the verification/confirmation step and the AWAITING_PHOTOS status advance — nothing extra needed here.

2. **"No Response"** confirm button — only visible when all 3 checkboxes are ticked. On click → `no_response_confirm` phase.

3. **"Done"** button — always visible. On click:
   - If `contact.leadStatus` is null/empty/`'__NULL__'` AND at least one checkbox is ticked → call `POST /api/card-actions/contact-customer/:contactId/advance-status` with `{ currentLeadStatus: contact.leadStatus, target: 'attempted_to_contact' }`, then close
   - Otherwise → just close (`onClose()`)
   - Show a spinner on the button while the request is in-flight

---

**Phase: `no_response_confirm`**

Show a confirmation card:
- Heading: "Mark as No Response?"
- Body: "This will set the lead status to No Response. This means no contact was made after all attempts."
- "Confirm" button → `advancing` phase, call `POST /api/card-actions/contact-customer/:contactId/advance-status` with `{ currentLeadStatus: contact.leadStatus, target: 'no_response' }`
- "Cancel" → back to `contact` phase

---

**Phase: `advancing`**

Loading spinner while the status update request is in-flight. On success → `done` phase. On error → show inline error and return to `contact` phase.

---

**Phase: `done`**

Brief success message (e.g. "Status updated"). Auto-close after 1.5 s via `setTimeout(() => onClose(), 1500)`.

---

### Task 4 — Register the Modal

In `CardActionModalsHost.tsx`:

1. Add to the `ModalState` discriminated union:
```typescript
| { type: 'contact_customer'; contactId: string; contactName: string; contactEmail: string }
```

2. Add a case in the handler switch:
```typescript
case 'contact_customer':
  return (
    <ContactCustomerModal
      contactId={modal.contactId}
      contactName={modal.contactName}
      contactEmail={modal.contactEmail}
      onClose={closeModal}
    />
  );
```

3. Import `ContactCustomerModal` at the top of the file.

---

### Task 5 — Seed Handler + Bindings

In the server startup/seed block where other `card_action_handlers` rows are inserted (search for the existing `INSERT INTO card_action_handlers` seed), add:

```sql
INSERT INTO card_action_handlers (name, type, config)
VALUES ('Contact Customer', 'contact_customer', '{}')
ON CONFLICT DO NOTHING;
```

Then two bindings in `card_action_handler_bindings` (join on `name` to get the handler ID, matching existing seed pattern):
- `stage_key = 'SALES'`, `status_key = '__NULL__'`
- `stage_key = 'SALES'`, `status_key = 'ATTEMPTED_TO_CONTACT'`

---

### Task 6 — Action Labels

The handler should display different labels depending on which binding triggered it. After seeding the handler, seed two rows into `stage_action_labels` (look at the existing seed in `/migrations/1749200000013_seed-stage-action-labels.js` for the exact column names and upsert pattern):

- `stage_key = 'SALES'`, `status_key = '__NULL__'` → `label = 'Call Customer'`
- `stage_key = 'SALES'`, `status_key = 'ATTEMPTED_TO_CONTACT'` → `label = 'Call Again'`

---

### Summary of status transition logic

| Current status | User action | Result |
|---|---|---|
| NULL / `__NULL__` | Ticks ≥1 checkbox, clicks Done | Advance to `ATTEMPTED_TO_CONTACT` |
| NULL or `ATTEMPTED_TO_CONTACT` | Ticks all 3, clicks "No Response" → confirms | Advance to `NO_RESPONSE` |
| NULL or `ATTEMPTED_TO_CONTACT` | Clicks "Send Upload Link" | Opens existing UploadPhotosModal → advances to `AWAITING_PHOTOS` |
| `ATTEMPTED_TO_CONTACT` | Ticks checkboxes, clicks Done | No status change (already correct status) |