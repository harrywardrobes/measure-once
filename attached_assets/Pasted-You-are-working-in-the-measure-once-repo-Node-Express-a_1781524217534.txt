You are working in the "measure-once" repo (Node/Express + a React/MUI admin app mounted into a legacy EJS page, Postgres via node-pg-migrate). Do the tasks in order. Do NOT introduce any company, brand, product, or customer names anywhere — keep all content generic and all spec values blank.

================================================================
TASK 0 — Flatten the admin "Visits" area to a SINGLE row of tabs (do this first)

Current structure is three nested layers:
- Group tabs: People / Visits / Configuration / Developer (src/react/components/AdminGroupedTabsBar.tsx, TAB_GROUPS).
- Under "Visits", two sub-tabs: "Visits" (→ DesignVisitPage) and "Survey Visit" (→ SurveyVisitPage, an empty placeholder).
- Inside DesignVisitPage, an inner TabBar: Catalogues / Questionnaire / Design Visit / Survey.

Target: under the Visits group there must be exactly ONE row of tabs — Catalogues · Questionnaire · Terms — with no redundant middle sub-tab layer and no empty Survey Visit page.

0.1 src/react/components/AdminGroupedTabsBar.tsx
  - Set the visits group to a single tab: tabIds: ['designvisit'] (remove 'surveyvisit').
  - In AdminGroupedTabsBarInner, when the active group resolves to 1 or 0 visible sub-tabs, do NOT render the sub-tab TabBar at all (that single page supplies its own one-row tabs). The group's single tab must still be activated via the existing activation logic so its panel shows. Groups with multiple sub-tabs (People/Configuration/Developer) keep their sub-tab row exactly as now.

0.2 views/admin.ejs
  - Remove the `surveyvisit` tab button (the one labelled "Survey Visit") and its `<div class="tab-panel" id="tab-surveyvisit">` panel.
  - Remove 'surveyvisit' from the `validTabs` array.
  - Keep the `designvisit` button (labelled "Visits").

0.3 src/react/main.tsx
  - Remove the SurveyVisitPage lazy import and its mount registry entry `{ id: 'tab-surveyvisit', render: () => <SurveyVisitPage /> }`.

0.4 Delete src/react/pages/admin/SurveyVisitPage.tsx.

0.5 src/react/pages/admin/DesignVisitPage.tsx
  - Reduce the inner VISITS_SUBTABS to exactly: { catalogues: 'Catalogues' }, { questionnaire: 'Questionnaire' }, { terms: 'Terms' }.
  - Rename the existing 'designvisit' subtab (the Terms & Conditions card) to key 'terms', label 'Terms', and render the terms card there.
  - Remove the standalone 'survey' subtab. Fold its explanatory notes into the others as short helper text (no brand names): on Catalogues — "Catalogue (door styles, handles, ranges) is shared with survey visits."; on Questionnaire — "Survey questions are the ones tagged with 'survey' in Applies to."; on Terms — "The same versioned terms are reused for survey visits."
  - Default subtab 'catalogues'. When reading the persisted ADMIN_VISITS_SUBTAB_KEY, validate against the new set and fall back to 'catalogues' for any stale value (e.g. old 'designvisit' or 'survey').

0.6 src/react/components/CommandPalette.tsx
  - Make the 'Admin · Design Visit' command navigate to the 'visits' group (relabel to 'Admin · Visits'); remove any command that targets the removed survey-visit tab.

0.7 src/react/stories/AdminGroupedTabsBar.stories.tsx — remove the 'surveyvisit' entry from ALL_TABS.

0.8 Grep the repo for remaining 'surveyvisit' references (tests under test/, etc.) and update or remove them. Run the test suite and fix fallout.

Verify: under Visits there is exactly one tab row (Catalogues / Questionnaire / Terms); no empty Survey Visit tab anywhere; command-palette / deep-link to Visits still works; the saved subtab restores correctly.

================================================================
TASK 1 — Migration `1783400000000_visit-questions-collection-and-checks.js`
(timestamp must sort AFTER 1783300000000_survey-visits.js; pure SQL via pgm.sql; idempotent / re-runnable, matching the guarded style of 1783200000000_questionnaire-tables.js).

Background: questions live in `visit_questions` — scope ('room'|'visit'), applies_to TEXT[], label, type ('yesno'|'choice'|'text'|'number'), options JSONB, required BOOLEAN, active, sort_order. scope='visit' = whole-visit ("house specific", renders on Step 1 of the wizard); scope='room' = per-room. applies_to controls which wizard shows it ('design' and/or 'survey').

NEW CONCEPT — required PER visit type: some checks must be REQUIRED on survey but optional/skippable on design. Add a `required_on TEXT[]` column listing the visit types for which a question is required (e.g. ['survey']). The member endpoint returns an effective `required` computed for the requested visit type so the wizard gating keeps working unchanged.

1.1 Schema + backfill:
  - ALTER TABLE visit_questions ADD COLUMN IF NOT EXISTS required_on TEXT[] NOT NULL DEFAULT '{}'.
  - UPDATE visit_questions SET required_on = applies_to WHERE required = TRUE AND cardinality(required_on) = 0;

1.2 Make existing DESIGN questions also apply to SURVEY (no duplicate rows):
  UPDATE visit_questions SET applies_to = array_append(applies_to,'survey'), updated_at = NOW()
   WHERE 'design' = ANY(applies_to) AND NOT ('survey' = ANY(applies_to));

1.3 Insert rows, each idempotent per-row (skip when a question with the same label AND scope exists), including required_on:
  INSERT INTO visit_questions (scope, applies_to, label, type, options, required_on, sort_order)
  SELECT v.scope, v.applies_to, v.label, v.type, v.options, v.required_on, v.sort_order
    FROM (VALUES (...rows...)) AS v(scope, applies_to, label, type, options, required_on, sort_order)
   WHERE NOT EXISTS (SELECT 1 FROM visit_questions q WHERE q.label = v.label AND q.scope = v.scope);
  choice rows → valid JSONB options; non-choice → '[]'::jsonb. active=TRUE.

  (A) COLLECTION — applies_to ARRAY['design','survey']:
   scope='visit', required_on ARRAY['design','survey'], sort_order 0:
     "Which Collection is the customer choosing?" — choice —
       ["DIY — MDF cabinetry, supplied ready for the customer to paint",
        "Standard — pre-finished materials such as melamine and MFC",
        "Plus — painted MDF cabinetry, with a wider selection of colours and finishes"]
   scope='room', required_on '{}', sort_order 0:
     "Collection for this room (if different from the visit selection)" — choice —
       ["Same as whole-visit selection",
        "DIY — MDF cabinetry, supplied ready for the customer to paint",
        "Standard — pre-finished materials such as melamine and MFC",
        "Plus — painted MDF cabinetry, with a wider selection of colours and finishes"]

  (B) GENERIC PER-ROOM SPEC FIELDS — scope='room', type text, applies_to ARRAY['design','survey'], required_on '{}':
   1 "Range / collection name"   2 "Door / drawer colour"   3 "Exterior panel / trim colour"
   4 "Interior colour"   5 "Worktop colour"   6 "Handle choice"

  (C) GENERIC SURVEY LIBRARY — applies_to ARRAY['survey'], required_on '{}' (keep alongside the real doc checks):
   scope='visit':
    10 "Property type?" — choice — ["Detached","Semi-detached","Terraced","Flat / apartment","Bungalow","Other"]
    20 "Approximate age of the property?" — choice — ["New build","Modern (post-2000)","Late 20th century","Early-mid 20th century","Period / pre-1900","Unsure"]
    30 "Which floor is the work on?" — text
    40 "Is there step-free access to the work area?" — yesno
    50 "Are there access restrictions (narrow doorways, tight stairs, low ceilings)?" — text
    60 "Is parking available for the installation team?" — choice — ["Driveway","Free on-street","Permit / paid on-street","No nearby parking"]
    70 "Does a congestion or low-emission charge apply at this address?" — yesno
    80 "Is there a working mains power supply on site?" — yesno
    90 "Will the property be occupied during installation?" — yesno
    100 "Are there pets on site to be aware of?" — yesno
    110 "Any known building hazards (asbestos, damp, structural)?" — text
    120 "Is the property listed or in a conservation area?" — yesno
    130 "Preferred installation timeframe?" — text
    140 "General access / site notes for the whole property" — text
   scope='room':
    10 "Room type?" — choice — ["Master bedroom","Bedroom","Dressing room","Box / small room","Landing","Loft room","Under-stairs","Other"]
    20 "Ceiling height (mm)" — number
    30 "Is the ceiling sloped or coombed?" — yesno
    40 "Is the floor level?" — yesno
    50 "Current floor covering?" — choice — ["Carpet","Laminate","Wood","Tile","Vinyl","Bare / subfloor"]
    60 "When will the final flooring be fitted?" — choice — ["Before installation","After installation","Existing flooring stays"]
    70 "Wall construction on the relevant walls?" — choice — ["Solid masonry","Stud / plasterboard","Mixed","Unsure"]
    80 "Skirting board height (mm)" — number
    90 "Is there a picture rail or dado rail on the relevant walls?" — yesno
    100 "Are there radiators or pipework on the relevant walls?" — yesno
    110 "Do any radiators or pipes need moving?" — yesno
    120 "Are there sockets or switches on the relevant walls?" — yesno
    130 "Do any sockets or switches need moving or adding?" — text
    140 "Is there a window on the relevant wall?" — yesno
    150 "Window reveal depth (mm)" — number
    160 "Any obstructions (chimney breast, boiler, meter, soil pipe)?" — text
    170 "Is there a loft hatch in the room?" — yesno
    180 "Is integrated lighting required (LED / spots)?" — yesno
    190 "Is there existing furniture to remove or dispose of?" — yesno
    200 "Wall A length (mm)" — number
    210 "Wall B length (mm)" — number
    220 "Are the walls out of square or not plumb?" — yesno
    230 "Have photos been taken of each wall?" — yesno
    240 "Notes / special requirements for this room" — text

  (D) REAL DOCUMENT CHECKS — scope='room', applies_to ARRAY['design','survey'], required_on ARRAY['survey'] (required on survey, optional/skippable on design):
   Customer advice notes — product:
    300 "Are internal wardrobe tops, backs and sides being installed?" — choice —
        ["No — our construction does not rely on internal backs, sides or tops, so the ceiling and walls will be visible inside; we recommend these surfaces are decorated before installation","Yes"]
    310 "Are there any closed corners, stable doors or other restricted areas within the design?" — yesno
    320 "Does the new furniture restrict any window or door openings?" — yesno
    330 "Are any un-fitted four-drawer chests included in the order?" — yesno
    340 "Does the design have any walkthrough areas below 500mm in width?" — yesno
    350 "Are we installing the same range or colour in a room with existing furniture?" — yesno
    360 "Are there any mixed product types with similar finishes?" — yesno
    370 "Are any of the customer's own products being incorporated into the design?" — yesno
    380 "Is an adjacent working area required for the installer to work in?" — yesno
    390 "Are any fixtures (switches, sockets, lights, radiators, etc.) being moved or installed?" — yesno
    400 "Are any existing doors or windows being blocked?" — yesno
    410 "Are there any wall vents or airbricks being covered by the furniture?" — yesno
    420 "Do we recommend any doorstops are fitted by the customer to avoid damage to the furniture from room doors etc.?" — yesno
    430 "Are there any other customer responsibilities not listed above?" — yesno
   Customer advice notes — responsibilities:
    440 "Are any existing fitted wardrobes being removed?" — yesno
    450 "Is there, or will there be, carpet in the room?" — yesno
    460 "If there is carpet, is it to be left in place, rolled back or removed?" — choice —
        ["Left in place — as the carpet settles under the furniture small gaps could appear between furniture and walls/ceilings and door alignment could be affected; this does not affect the stability of the installation and we cannot be held responsible for any remedial work","Rolled back","Removed","Not applicable"]
    470 "Is there, or will there be, a hard floor covering in the room?" — yesno
    480 "Is the design dependent on existing carpet, flooring or decoration lines?" — yesno
    490 "Is there, or will there be, a bed in the room of installation?" — yesno
    500 "If there is a bed, what will happen to it?" — choice —
        ["Moved — left in the room; please provide a clear working area, cover the bed with dustsheets, and be aware the installer may place small or light items on it; we do not accept claims for cleaning, repair or replacement of the bed or mattress","Removed","Not applicable"]
   Survey checks:
    510 "Room location / description?" — text
    520 "What floor level is the room on?" — choice — ["Basement","Ground","First","Second","Third or higher"]
    530 "What access is there to this room?" — text
    540 "Has access been checked for sliding doors?" — choice — ["Yes","No","N/A"]
    550 "Is there a maximum panel size?" — yesno
    560 "Maximum panel length (mm)" — number
    570 "Maximum panel width (mm)" — number
    580 "Back wall run-out (mm)" — number
    590 "Side wall run-out (mm)" — number
    600 "Floor run-out (mm)" — number
    610 "Ceiling run-out (mm)" — number
    620 "Is underfloor heating present in the installation area?" — yesno
    630 "Is Artex present in the room?" — yesno
    640 "Is there a boiler being enclosed by the furniture?" — yesno
    650 "Are there any templated items?" — yesno
    660 "Is there a Product NCR?" — yesno
    670 "Are there any other considerations?" — text

1.4 exports.down — remove only what this migration added: delete the rows from 1.3 by exact label; reverse 1.2 with array_remove(applies_to,'survey') where the row still contains 'design'; ALTER TABLE visit_questions DROP COLUMN IF EXISTS required_on. Do not drop tables.

================================================================
TASK 2 — Backend in design-visits.js
- Admin POST/PATCH /api/admin/visit-questions[/:id]: accept and persist `required_on` as TEXT[] (reuse the applies_to array-normalising helper). When required_on is provided also set required = (cardinality(required_on) > 0) to keep the legacy column consistent.
- Admin GET /api/admin/visit-questions: confirm it returns required_on (SELECT *).
- Member GET /api/visit-questions: return an EFFECTIVE `required`:
  * with ?applies_to=<type>: required = (<type> = ANY(required_on))  [reuse the param already bound for the WHERE clause]
  * with no applies_to: required = (cardinality(required_on) > 0)
  Also include required_on in the selected columns. Don't change the answer-save paths.

================================================================
TASK 3 — Admin Questionnaire builder (src/react/pages/admin/QuestionnaireBuilder.tsx)
- Add required_on: string[] to AdminQuestion and to BLANK (default []).
- Visit-type filter above the two section cards: "All" / "Design" / "Survey". "Design" → applies_to includes 'design'; "Survey" → includes 'survey'; "All" → no filter. Apply inside renderScope so both Whole-visit and Per-room sections respect it. When adding a question while a type is selected, default the new question's applies_to to that type (keep 'design' default for "All").
- Replace the single "Required" checkbox in the editor with per-visit-type required: a "Required on design visits" and a "Required on survey visits" checkbox that build required_on. Send required_on (and required = required_on.length > 0).
- Row display: replace the single "Required" chip with chips showing where required, e.g. "Required: survey" (omit when empty). Keep applies_to chips.
- Match existing MUI styling and TypeScript types.

================================================================
CONSTRAINTS & VERIFICATION
- No company, brand, product, or customer names anywhere in code, comments, seeds, or commit messages. Spec fields in (B) are blank labels only.
- Migration must be idempotent / re-runnable.
- After all tasks, run the migration and the test suite locally and confirm:
  * Visits shows ONE tab row (Catalogues / Questionnaire / Terms); no empty Survey Visit tab.
  * The Collection question is first on Step 1 and required on BOTH design and survey wizards.
  * The document checks (D) appear per room on both wizards, are REQUIRED on survey (block advancing when unanswered) and OPTIONAL on design (can be left blank / half-complete).
  * The generic survey library (C) still loads for survey.
  * The admin Questionnaire Design/Survey filter and per-visit-type required editor + chips work.
- Confirm SurveyVisitWizard enforces required whole-visit and per-room questions like DesignVisitWizard (via missingRequired); make consistent if not.
- Extend tests (test/questionnaire/run.js): required_on=['survey'] → required=true for ?applies_to=survey and false for ?applies_to=design; Collection required at visit scope; existing design questions also return for ?applies_to=survey.