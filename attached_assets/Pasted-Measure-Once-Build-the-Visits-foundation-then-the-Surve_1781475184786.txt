Measure Once — Build the "Visits" foundation, then the Survey Visit wizard
You will complete TWO tasks in order, in one run. Finish Task 1 completely — including passing tests/linter and a working build — before starting Task 2. Task 2 depends on the shared catalogues, questionnaire engine, and reusable components created in Task 1. Do not start Task 2 until Task 1 is done; do not pause for my approval between them — run straight through.

Shared context (read first)
This is a React 18 + TypeScript + Vite app (MUI v9), Node/Express REST backend, PostgreSQL via pg raw SQL with node-pg-migrate migrations, QuickBooks (Intuit) integration, Nodemailer + EJS emails, Replit object storage for images. Do not introduce new frameworks.

There is an existing Start Design Visit feature to mirror and refactor. Study these before writing anything:

Wizard: src/react/components/DesignVisitWizard.tsx, DesignVisitStep1.tsx, DesignVisitRoomsStep.tsx, DesignVisitStep3.tsx
Backend: design-visits.js
Migration: migrations/1749200000004_design-visits.js
Admin: src/react/pages/admin/DesignVisitPage.tsx, HandlerConfigBlocks.tsx (StartDesignVisitConfig)
Handler registry: shared/handler-outcomes.ts and its CJS mirror shared/handler-outcomes.cjs (drift-guard test test/card-action-handlers/drift-guard.js asserts they agree — keep both in sync)
Lead statuses: STAGE_LEAD_STATUSES in server.js
Skill reference: .agents/skills/add-card-action-handler/references/start-design-visit.md
Follow existing conventions exactly: sessionStorage drafts (new) vs React-state (edit), offline queue, pending-uploads/orphan-recovery for images, BroadcastChannel catalogue reloads, raw-SQL pool.query, REST patterns, auth tiers (member+ / admin), and the QB sparse-update estimate flow.

Ask me before: deleting data, or changing existing Design Visit user-facing behaviour beyond what's specified.

TASK 1 — "Visits" foundation (shared catalogues + questionnaire engine + Design Visit re-org)
Goal: turn the Design-Visit-specific admin area into a shared "Visits" foundation that both Design and (later) Survey visits build on. No loss of existing functionality.

1a. Neutral shared catalogue tables (data migration)
Create new node-pg-migrate migration(s) that create neutral, shared catalogue tables and migrate existing data into them, then repoint all foreign keys and code:

catalog_handles — id, name, style, image_url, supplier_name, supplier_code, price_pence (nullable), notes, sort_order, created_at, updated_at
catalog_doors — id, name, image_url, colour (nullable), finish (nullable), supplier_name, supplier_code, notes, sort_order, created_at, updated_at
catalog_finishes — id, name, material_type (e.g. board/laminate/veneer/paint), image_url, supplier_name, supplier_code, notes, sort_order, created_at, updated_at
catalog_ranges — furniture ranges: id, name, description, sort_order, created_at, updated_at
catalog_pairings — admin-curated "these go well together": id, door_id FK→catalog_doors, handle_id FK→catalog_handles, finish_id FK→catalog_finishes (nullable), recommended (bool), notes, sort_order. Use this to suggest matching handles/finishes when a door is chosen.
Add any other catalogue you find is currently hard-coded or duplicated and would sensibly be shared (flag what you added and why).
Migration steps:

Migrate rows: design_visit_handles → catalog_handles, design_visit_door_styles → catalog_doors, design_visit_furniture_ranges → catalog_ranges (preserve ids/sort order/images).
Repoint FKs: design_visits.handle_id → catalog_handles, design_visits.furniture_range_id → catalog_ranges, design_visit_rooms.door_style_id → catalog_doors.
Replace the old design_visit_* catalogue admin + wizard endpoints with /api/admin/catalog/handles|doors|finishes|ranges|pairings (CRUD + image upload) and /api/catalog/... for in-wizard reads. Keep the old routes as thin aliases that 308/redirect or proxy to the new ones for one release, to avoid breaking anything mid-deploy; mark the aliases with a // TODO remove after migration comment.
Update all React references, types, and BroadcastChannel keys (e.g. catalog_handles_changed).
Drop the old catalogue tables only after data is confirmed migrated (do this in the same migration's up, with a correct down). Do not drop anything you haven't copied first.
1b. Shared questionnaire engine (used by Design Visit now, Survey later)
Tables: visit_questions (id, scope ['room'|'visit'], applies_to text[] e.g. {'design','survey'}, label, type ['yesno'|'choice'|'text'|'number'], options JSONB (for choice: list + single/multi flag), required bool, active bool, sort_order, created_at, updated_at) and visit_answers (id, visit_type, visit_id, room_id (nullable, for visit-level), question_id FK, answer JSONB, created_at).
Admin questionnaire builder UI (add/edit/reorder/remove, set scope, type, options, required, and which visit types it applies_to). Optional cheap conditional show-if logic only if low-effort.
Build a reusable <QuestionnaireRenderer> React component that renders a question set and captures answers, plus a server helper to load/save answers.
Wire it into the Design Visit wizard now: visit-level questions render in Step 1, per-room questions render per room in the rooms step; answers save with the design visit. Seed a few sensible starter questions but keep them all editable.
1c. Admin "Visits" tab re-org
Rename the admin "Design Visit" tab to "Visits" with sub-sections: Catalogues (handles, doors, finishes, ranges, pairings — shared), Questionnaire (the builder above), Design Visit (existing design-visit-specific settings: handler config, T&C, durations, statuses). Leave room for a future Survey Visit sub-section (Task 2 adds it).
Keep the existing T&C versioning (terms_conditions_versions) shared.
1d. Extract reusable components
Refactor the Design Visit wizard so the survey can reuse, without changing its behaviour: a generic wizard/drawer shell, a room editor card, the room-image uploader (pending-uploads + orphan recovery), catalogue dropdowns (handles/doors/finishes with pairing suggestions), and the <QuestionnaireRenderer>. Keep DesignVisitWizard working by composing these extracted pieces.

1e. Task 1 quality gate
Existing Design Visit tests still pass; add tests for the catalogue migration (data preserved, FKs repointed) and the questionnaire engine.
shared/handler-outcomes.ts/.cjs stay in drift-guard sync. Build is green. Update docs/TEST_SUITES.md. Only when all green, continue to Task 2.
TASK 2 — Start Survey Visit wizard (depends on Task 1)
Build a Start Survey Visit wizard, a continuation of the Design Visit, reusing everything from Task 1 (shared catalog_* tables, the questionnaire engine + <QuestionnaireRenderer>, and the extracted wizard/room/image components). Add a Survey Visit sub-section to the "Visits" admin tab.

2a. Handler & lead-status wiring
Add card-action handler start_survey_visit (mirror start_design_visit) in both handler-outcomes.ts and .cjs, with one terminal outcome submitted ("Survey visit submitted") using the handler-configured submittedLeadStatus. Update the drift-guard test.
Handler bindable on both SURVEY_SCHEDULED and SURVEY_IN_PROGRESS.
Config block StartSurveyVisitConfig with: defaultDurationMin, intermediateLeadStatus (set on open; default SURVEY_IN_PROGRESS), submittedLeadStatus (set on submit; default SURVEY_SENT), shared termsAndConditions.
Opening the wizard PATCHes hs_lead_status to intermediateLeadStatus, exactly like the design visit.
2b. Continuation from the signed-off Design Visit
Launch against a contact with a signed-off design visit; load it and pre-fill all rooms (room name, door/finish from catalog_*, width/height/depth, unit count/price, layout, photos) — every value editable.
Pull all design-visit rooms; surveyor can remove any not going ahead. Store design_visit_id (source) and per-room source_design_visit_room_id.
Future sub-task (mark as TODO, don't build): the authoritative "rooms going ahead" set will later come from the open-deal step (customer accepts e.g. 3 of 4 rooms → estimate updated + 10% deposit taken). The survey wizard will then read that set and offer to "bring back" a dropped room. For now, all rooms + manual removal.
2c. Wizard (3-step right-slide drawer, reusing extracted components)
Step 1 — Visit details: date/time, duration (from defaultDurationMin), structured address (reuse Google autocomplete + structuredAddressSchema), surveyor name, T&C acceptance, and visit-level questionnaire questions where applies_to includes survey.
Step 2 — Rooms: per-room cards pre-filled from the design visit; confirm/edit measurements, door/finish (with pairing suggestions from catalog_pairings), layout, units/price, notes, photos; answer per-room survey questions (e.g. underfloor heating, bed in room during construction, carpet: fitting on top / pulling back / replacing after). Surveyor can remove rooms.
Step 3 — Review & submit: full read-only summary incl. questionnaire answers; submit disabled in demo mode.
2d. Refund option (only in SURVEY_SCHEDULED)
When the lead is SURVEY_SCHEDULED, surface a "Customer changed their mind — refund" action (hidden once SURVEY_IN_PROGRESS). It: captures reason/amount, records the refund request, emails the admin all details (contact, deal, deposit invoice ref, reason, amount) via a new email-templates.js + EJS template for manual processing in QuickBooks (no automatic QB refund), and sets lead status to DECLINED_DEAL.

2e. Submit / completion (sign-off + QB)
Mirror the design visit's side-effects: generate a customer sign-off link/page (reuse the crypto token + sha256 hash + 7-day expiry + superseded-token tracking + public sign-off endpoints/page), update the existing QuickBooks estimate via the existing sparse-update path (fallback to new estimate if locked; log to qb_estimate_history), and set lead status to submittedLeadStatus (default SURVEY_SENT, with READY_FOR_PRODUCTION as the manual next step).

2f. Data model & API
Migration: survey_visits (mirror design_visits, + design_visit_id FK), survey_visit_rooms (mirror design_visit_rooms, + source_design_visit_room_id, FKs to catalog_*), survey_visit_room_images. Reuse visit_questions/visit_answers (with visit_type='survey'). Do not clone catalogues.
Endpoints under /api/survey-visits* mirroring /api/design-visits*: CRUD, in-progress list, submit (side-effects), revision, public sign-off GET/POST, image upload/delete/sign-url, plus /api/survey-visits/:id/refund. Reuse /api/catalog/* reads and visit_questions admin endpoints.
2g. Quality
Add test/start-survey-visit/run.js mirroring test/start-design-visit/run.js; keep drift-guard in sync; update Storybook handler enumerations and docs/TEST_SUITES.md; build green.

Defaults I've assumed (override if wrong): open sets SURVEY_IN_PROGRESS, submit sets SURVEY_SENT; refund only catches pre-survey cancellations; old catalogue routes kept as temporary aliases during the rename to avoid mid-deploy breakage.