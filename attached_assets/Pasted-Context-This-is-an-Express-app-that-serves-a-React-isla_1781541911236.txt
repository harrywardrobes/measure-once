Context: This is an Express app that serves a React island built by Vite (vite.config.ts, output to public/react/). A Lighthouse/performance audit flagged forced reflows and main-thread work. Before changing anything, note that several flagged items come from browser extensions running in my dev profile, not my code — specifically readability.js (Adobe Acrobat / Reader-mode extension), the Adobe Acrobat extension, and "Peek." Do not try to find or edit readability.js or any extension script; they aren't in this repo. Please confirm this distinction in your analysis and only change code I actually own/bundle.

Do the following:

Forced reflows in our own code. Search the React source (src/react/**) and any client JS in public/ for layout-thrashing patterns — reading layout properties (offsetWidth, offsetHeight, clientWidth, getBoundingClientRect, scrollTop, innerHeight, getComputedStyle) inside loops, or interleaved with DOM writes (style/class/content changes), or immediately after a DOM mutation. Where found, batch reads before writes, move reads into requestAnimationFrame or ResizeObserver/IntersectionObserver where appropriate, and cache values that don't change inside a loop. List each fix with file and line.
Re-verify code splitting. Review the existing manualChunks strategy in vite.config.ts and the React.lazy() page imports in src/react/main.tsx. Run npm run build:react and report the per-chunk gzip sizes from scripts/check-bundle-sizes.mjs. Identify anything in the always-loaded entry (main.js) or the eagerly-loaded vendor chunks that is only needed by specific pages and could be moved behind a dynamic import. Don't restructure chunks that are already correctly lazy — only flag genuine wins.
Audit our third-party scripts. Check what <script> tags and third-party SDKs we actually load in the served HTML (views/, public/*.html) and in the React bundle. For any non-critical third party, recommend defer/async, lazy-loading on interaction, or removal. Be explicit about which scripts are ours vs. injected by browser extensions (the latter we can't control).
Don't change behavior or break the bundle-size thresholds. After changes, run npm run typecheck and npm run build:react and confirm both pass. Summarize: what was a real fix, what was extension noise, and the before/after bundle sizes.