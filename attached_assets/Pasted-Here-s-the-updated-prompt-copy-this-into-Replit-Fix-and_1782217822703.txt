Here's the updated prompt — copy this into Replit:

---

**Fix and redesign address input: postcode-first lookup using Google Places API (React)**

This app is written in **React** and uses the **Google Places API**. I need the address input behaviour standardized and fixed across the **entire application — every page or form that asks for an address** (Upload Info page, checkout, account/profile, billing, shipping, settings, etc.). Search the codebase for all address forms/components and apply these changes everywhere. If addresses are built as a shared component, update it there so the fix propagates; if they're implemented inconsistently, refactor toward a single reusable `<AddressInput>` component and use it everywhere.

**1. New address-entry flow: postcode first**

Every address input should follow this flow:

- **Step 1 — Postcode:** Ask the user for their **postcode first**, before any other address fields.
- **Step 2 — Address dropdown:** Once a postcode is entered, show a **dropdown list of matching addresses** so the user can simply select theirs. Selecting an address auto-populates all fields (line 1, line 2, town/city, county, postcode, country).
- **Step 3 — Manual fallback:** Always show a **"Manually enter address"** option. The user can click this to type their address by hand if their address isn't in the dropdown, or if the API isn't working for any reason. Manual entry must always work as a reliable backup even when the lookup service is completely unavailable — never leave the user stuck.

**Implementation with Google Places API:**

- Use the Google Places API to drive the postcode → address-list flow. A clean approach is to use **Place Autocomplete** with the input restricted to the relevant region/country and types (e.g. `componentRestrictions: { country: 'gb' }`), then call **Place Details** to retrieve the full structured address when the user selects a result. Map the returned `address_components` (street_number, route, locality, postal_town, administrative_area_level_2, postal_code, country) into the form fields.
- Prefer the newer **`@react-google-maps/api`** loader or the **`google.maps.places.AutocompleteService` / `PlacesService`** APIs rather than attaching a raw widget directly to the DOM, so it plays well with React's render lifecycle.
- Load the Google Maps script **once** at the app level (don't inject the script tag multiple times across components), include `libraries=places`, and read the API key from an environment variable (e.g. `import.meta.env.VITE_GOOGLE_MAPS_API_KEY` or `process.env.REACT_APP_GOOGLE_MAPS_API_KEY`) — never hard-code it.

**2. The Google Places lookup is currently broken — diagnose and fix it**

The address lookup is not working (e.g. on the Upload Info page typing shows no suggestions). Find the actual root cause rather than guessing. Check the common causes:

- Open the browser console and look for errors like `Google Maps JavaScript API error`, `InvalidKeyMapError`, `ApiNotActivatedMapError`, `RefererNotAllowedMapError`, or `ApiTargetBlockedMapError`.
- Confirm the **Places API (New)** (and Maps JavaScript API) is **enabled** in the Google Cloud project for the key in use, and that billing is enabled on the project.
- Confirm `libraries=places` is included when loading the Maps script, and that the API key env var is actually defined at build/runtime (not `undefined`).
- Confirm the key's HTTP-referrer restrictions allow the app's domain (including the Replit dev/preview domain).
- Common React bug: the Autocomplete service is initialized **before** the script finishes loading, or the listener detaches on re-render. Make sure initialization waits for the script `load` event / loader's `isLoaded` flag, and is stable across re-renders (use refs / `useEffect` with correct dependencies).

Report back what the actual root cause was.

**3. Field cleanup**

- Remove the existing **"+ add address line"** control.
- Replace it with a standard, always-visible **"Address line 2"** field (optional).

**4. When done**

Report back: the root cause of the broken lookup, and a list of every file and component you changed.

---

This version assumes UK-style postcodes (`country: 'gb'`) — if your customers are international, tell me and I'll adjust the country restriction note.