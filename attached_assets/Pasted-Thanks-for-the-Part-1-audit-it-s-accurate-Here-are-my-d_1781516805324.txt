Thanks for the Part 1 audit — it's accurate. Here are my decisions on your 12 questions.
Please fold these into the task plan(s) for my approval, then implement once approved.

=================== DECISIONS ===================

DRAFT STORAGE (your Q3 + cross-cutting finding)
- Switch drafts to localStorage for BOTH wizards (Survey AND Design Visit), so a
  half-finished visit survives a full tab close/reopen, not just reload/refresh/nav.
- Hard bar for both wizards: NO data lost on reload, refresh, in-app navigation, tab
  close/reopen, crash, going offline, or coming back online — at ANY step, including
  photos and per-room answers.

PARTIAL SUBMIT (your Q7)
- Must complete all required steps before a survey visit can be queued/submitted.
- Drafts still save partial progress continuously, but ONLY a complete visit enters the
  outbox / sendOrQueue pipeline.

PERMISSIONS (your Q12 + gating in Q10/Q11)
- No change. Leave existing visibility/permission rules as they are for both wizards.

VISITS AS A MAIN TAB (your Q10/Q11 — I was misunderstood earlier)
- This is about the ADMIN PANEL, not the customer detail page or BottomNav.
- Today the visit admin tab (`designvisit`, and any survey-visit equivalent) is a SUB-TAB
  nested under the "Configuration" group in src/react/components/AdminGroupedTabsBar.tsx
  (TAB_GROUPS).
- Promote Visits to its own TOP-LEVEL group, alongside People, Configuration and
  Developer. Move the visit-related sub-tab(s) out of Configuration into this new "Visits"
  group. Update TAB_GROUPS, the GroupId type, any group-ordering/visibility logic, the
  legacy DOM tab registration these groups read from, and the AdminGroupedTabsBar stories.
- If a survey-visit admin page doesn't exist yet, just move the existing design-visit tab
  and tell me — don't invent a new admin page without asking.

DEFAULTS — mirror the Design Visit wizard exactly (override only if you flag a conflict):
- Draft scope: per-device only. One in-progress NEW draft per contact, plus separate
  drafts for edits — same keying as DV's draftKey(contactId, editId).
- Draft lifetime: clear on successful submit and on explicit discard. Keep the existing
  discard-confirmation dialog.
- Restore UX: auto-restore with the same restore banner/notice the DV wizard shows.
- Conflict model: same stale-write model as DV (baseVersion / baseUpdatedAt /
  conflictCheckUrl), surfaced in ConflictsReview with the same side-by-side diff UI.
- Offline photos: inline Base64 fallback exactly like DesignVisitRoomsStep.tsx. Apply a
  sensible size/count cap and WARN the user if local storage is near full — never silently
  drop photos.
- Sync ordering: replay queued items in the order they were queued.

=================== SCOPE OF WORK ===================

PART 2 — Survey Visit offline hardening (task #2911), bring SurveyVisitWizard.tsx to full
parity with the Design Visit reference:
  1. Switch its draft persistence to localStorage (per decisions above), keep the restore
     banner.
  2. Replace the plain fetch submit (~SurveyVisitWizard.tsx:642-646) with sendOrQueue,
     including baseVersion / baseUpdatedAt / conflictCheckUrl conflict metadata.
  3. Add the inline Base64 offline photo fallback (mirror DesignVisitRoomsStep.tsx) so
     photos no longer require a live endpoint.
  4. Wire optimistic pending/failed cards with retry into the survey visit list, count
     queued survey visits toward the SyncPill, and surface survey-visit conflicts in
     ConflictsReview.
  5. Enforce "must complete all steps" before anything enters the outbox.
  6. Register any new localStorage keys in src/react/constants/localStorageKeys.ts.
  7. The survey-visits backend + migration are already sync-ready — confirm the payload
     matches and don't change schema unless something's genuinely missing (flag it first).

PART 2b — Design Visit: switch its draft from sessionStorage to localStorage too, so both
wizards meet the no-data-loss bar. Don't regress any existing DV offline behaviour.

PART 3 — Promote Visits to a top-level admin group (see above).

=================== PROCESS ===================
- You're in planning mode: produce the task plan(s) for my approval first. I expect likely
  three: (a) Survey Visit offline hardening, (b) move both wizards' drafts to localStorage,
  (c) promote Visits to a main admin tab — but split them however makes sense and tell me
  why.
- Call out any remaining ambiguity before coding. Once I approve, implement, then commit
  with clear messages.