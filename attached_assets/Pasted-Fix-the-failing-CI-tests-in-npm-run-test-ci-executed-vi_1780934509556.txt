Fix the failing CI tests in npm run test:ci (executed via node scripts/run-ci.mjs). There are three distinct, unrelated issues — please fix each independently and verify the relevant check passes before moving to the next.
1. Missing public pages referenced by the bootstrap config
src/react/lib/publicIslands.ts (lines ~37-40) declares BOOTSTRAP_ONLY_IDS entries not-found-root (annotated against public/404.html) and access-restricted-root (annotated against public/access-restricted.html), but neither HTML file exists in public/. This causes both npm run test:mount-ids (scripts/check-mount-id-conflicts.mjs) and npm run test:public-island-bootstrap (scripts/check-public-island-bootstrap.mjs) to fail with “file does not exist” / “no element with that id was found” errors.
	•	Investigate whether these pages were renamed/moved elsewhere in public/ (in which case update the paths/IDs in publicIslands.ts to match reality), or whether they were simply never created (in which case create public/404.html and public/access-restricted.html with a mount element bearing id="not-found-root" and id="access-restricted-root" respectively, following the structure/conventions of other files in public/).
	•	Run npm run test:mount-ids and npm run test:public-island-bootstrap to confirm both pass after your fix.
2. CI runner references npm scripts that no longer exist
scripts/run-ci.mjs (and its sibling scripts/run-ci-parallel.mjs, which test:ci-runner-sync enforces stays in sync) lists these steps in STEPS, but the corresponding scripts are missing from package.json:
	•	test:sales-board:ci
	•	test:sales-board-bootstrap:ci
	•	test:sales-board-stage-labels:ci
	•	test:sales-board-error-state:ci
	•	test:survey-board:ci
This causes immediate npm error Missing script failures for each. Investigate whether these correspond to renamed suites still present under different names (npm itself suggests test:sales-board-error-state may now be test:calendar-empty-state:ci — check test/ directory for sales-board*/survey-board* folders that may have been renamed to calendar-* or similar during a refactor) or whether they were removed entirely (in which case remove the stale entries from both run-ci.mjs and run-ci-parallel.mjs, keeping them in sync). Do NOT just delete these references without checking — if the underlying test suites still exist under new names, update the references to point to the correct current script names instead, so coverage isn’t silently lost.
	•	Run npm run test:ci-runner-sync to confirm run-ci.mjs and run-ci-parallel.mjs remain in sync after your edits.
3. Document the DATABASE_URL requirement for local CI runs (informational, not a code bug)
Every DB-backed suite piped through scripts/with-test-db.js (dozens of suites — test:photo-storage-errors:ci, test:photo-reviews:ci, test:start-design-visit:ci, etc.) fails immediately with [with-test-db] DATABASE_URL is required when run without an admin Postgres connection string set. This isn’t a code bug, but if there’s no clear documentation of this requirement (check README.md and any CONTRIBUTING/test docs), add a short note explaining that DATABASE_URL must point at an admin Postgres connection (e.g. postgresql://postgres:postgres@localhost:5432/postgres) before running npm run test:ci, since with-test-db.js uses it to spin up/tear down temporary mo_testdb_* databases per suite.
After fixing all three, run the full npm run test:ci (with DATABASE_URL set) and confirm it completes without the errors described above. Report back any new/different failures you encounter — don’t silently skip or disable failing tests to make the suite “pass.”